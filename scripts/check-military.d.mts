/**
 * Typed surface of scripts/check-military.mjs (the standalone military gate).
 * Imported by tests/military-absence.test.ts so the vitest gate shares the
 * single source of truth with `npm run check:military`.
 */
export const FORBIDDEN_TOKENS: readonly string[];
export function scanMilitarySources(): string[];
