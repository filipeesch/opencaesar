---
phase: 02-data-catalogs-military-absence-gate
plan: 02-03
type: auto
wave: 3
depends_on: [02-02]
files_modified:
  - scripts/check-military.mjs
  - package.json
  - .github/workflows/ci.yml
  - tests/military-absence.test.ts
autonomous: true
requirements: [DATA-03]
must_haves:
  truths:
    - "npm run check:military runs a standalone scanner over src/ and data/ only and exits non-zero when a forbidden military token appears on any line not carrying the --NO-MILITARY-- label."
    - "npm run check:military exits 0 on the current clean tree."
    - "CI (.github/workflows/ci.yml quality job) includes an explicit npm run check:military step independent of the unit-test suite."
    - "The military gate scans only src/ and data/ (not tests/, e2e/, or docs/) and honors the --NO-MILITARY-- labeled-doc allowance."
    - "The FORBIDDEN_TOKENS list has a single source of truth shared by the script and the vitest gate."
---

<objective>
Deliver the DATA-03 independent military gate (decision 3). The token list and scan logic
already exist inside tests/military-absence.test.ts but only run through `npm test`. The work
is: (1) extract a standalone Node scanner (`scripts/check-military.mjs`) that exits non-zero on
violations over `src/` + `data/` only, honoring the `--NO-MILITARY--` line allowance; (2) expose
it as `npm run check:military` and add an explicit step to the CI quality job so the gate fails
loudly independent of unit tests; (3) refactor the vitest gate to import the tokens + scan from
the shared script (single source of truth). Depends on plan 02-02.

Output: `.planning/phases/02-data-catalogs-military-absence-gate/02-03-PLAN.md` plus the tasks below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-catalogs-military-absence-gate/02-CONTEXT.md
@.planning/phases/02-data-catalogs-military-absence-gate/02-RESEARCH.md
@.planning/phases/02-data-catalogs-military-absence-gate/02-VALIDATION.md

Existing implementation: military gate in tests/military-absence.test.ts (FORBIDDEN_TOKENS of 11
tokens at lines 13-25; recursive src/+data/ scan at lines 30-42; per-line --NO-MILITARY--
allowance at lines 51-66). package.json scripts at package.json:7-18 (no check:military). CI
quality job at .github/workflows/ci.yml:7-20 (steps npm ci, lint, typecheck, test). Standalone
script precedent: scripts/export-art.mjs. Baseline: `npm run typecheck` clean, `npm run test`
green (273 tests). Per-task sampling follows 02-VALIDATION.md.
</context>

<tasks>

<task type="auto">
  <name>Extract the standalone military scanner script</name>
  <files>scripts/check-military.mjs, tests/military-absence.test.ts</files>
  <action>Read tests/military-absence.test.ts (the existing token list + scan logic) and
  scripts/export-art.mjs (the scripts/ .mjs pattern) before editing. Create
  scripts/check-military.mjs that: (1) exports a const FORBIDDEN_TOKENS array containing exactly
  the 11 tokens currently at tests/military-absence.test.ts:13-25 (military, army, legion,
  soldier, fort, barracks, weapon, enemy, invasion, combat, damageFromUnit); (2) exports a
  function scanMilitarySources() that recursively walks src/ and data/ (relative to the repo
  root resolved via import.meta.url), collects files matching the extension set
  /\.(ts|tsx|js|cjs|mjs)$/ while skipping node_modules and test-results, and for each line
  returns an offender string `relativepath:lineno (token)` whenever the case-insensitive
  word-boundary regex for a token matches and the line does NOT include the substring
  '--NO-MILITARY--'; (3) runs a main() entry only when the module is executed directly
  (guard on import.meta.url equalling the process entry), printing a summary and, on any
  offender, each offender line plus exit code 1, otherwise a clean summary and exit code 0.
  Keep the scan scope strictly src/ + data/ (do not scan tests/, e2e/, or docs/).</action>
  <verify>
    <automated>node scripts/check-military.mjs</automated>
  </verify>
  <done>scripts/check-military.mjs exists, exports FORBIDDEN_TOKENS (11 tokens) + scanMilitarySources,
  and running `node scripts/check-military.mjs` on the clean tree prints a summary and exits 0.</done>
</task>

<task type="auto">
  <name>Wire npm run check:military + CI step; dedupe the token list into the script</name>
  <files>package.json, .github/workflows/ci.yml, tests/military-absence.test.ts, scripts/check-military.mjs</files>
  <action>Read package.json, .github/workflows/ci.yml, and tests/military-absence.test.ts before
  editing. Add a script entry to package.json (inside the scripts object, after the existing test
  entries): `"check:military": "node scripts/check-military.mjs"`. In .github/workflows/ci.yml's
  quality job, add a step `- run: npm run check:military` between the existing `npm run
  typecheck` step and the `npm test` step (ci.yml:19-20) so the gate fails loudly before and
  independently of the unit-test suite. Refactor tests/military-absence.test.ts to import
  FORBIDDEN_TOKENS and scanMilitarySources from '../../scripts/check-military.mjs' (single source
  of truth) and drive its assertions from those imports, preserving the existing behaviors: it
  scans src/ + data/, files.length is greater than 0, and it allows lines containing
  '--NO-MILITARY--'. Confirm importing the script does NOT run main() (the direct-entry guard
  must hold) and that `npm test` still passes with the refactor.</action>
  <verify>
    <automated>npm run check:military && npm run typecheck && npm test</automated>
  </verify>
  <done>package.json has check:military; ci.yml quality job runs npm run check:military as an
  explicit step; tests/military-absence.test.ts imports tokens + scan from the script;
  `npm run check:military` exits 0 and full suite stays green; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR/commit → CI | A military token introduced into src/ or data/ must block the merge regardless of unit-test status |
| script token list ↔ vitest token list | Both gate layers must share one token list so the source of truth can never diverge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | forbidden military content in src/data | high | mitigate | standalone `npm run check:military` (exit-code gate) + explicit CI step (ci.yml) + vitest gate importing the same FORBIDDEN_TOKENS |
</threat_model>

<verification>
Run `npm run test` (full suite, golden + determinism included), `npm run typecheck`, and
`npm run check:military` (must exit 0). Spot-check: `node scripts/check-military.mjs` prints a
clean summary. Per-task sampling follows `.planning/phases/02-data-catalogs-military-absence-gate/02-VALIDATION.md`.
</verification>

<success_criteria>
- `npm run check:military` exists and exits 0 on the clean tree; exits non-zero when a forbidden token appears outside a --NO-MILITARY-- line.
- ci.yml quality job includes an explicit `npm run check:military` step.
- tests/military-absence.test.ts imports FORBIDDEN_TOKENS + scan from scripts/check-military.mjs.
- Full suite (273 tests) and typecheck remain green.
</success_criteria>

<output>
Create `.planning/phases/02-data-catalogs-military-absence-gate/02-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- New file: `scripts/check-military.mjs` (exports `FORBIDDEN_TOKENS` + `scanMilitarySources`, guarded `main()` CLI, exit codes 0/1).
- `package.json`: new `check:military` npm script.
- `.github/workflows/ci.yml`: new `npm run check:military` step in the quality job.
- Modified: `tests/military-absence.test.ts` (imports tokens + scan from the shared script).
