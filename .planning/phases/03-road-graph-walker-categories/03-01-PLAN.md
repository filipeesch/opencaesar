---
phase: 03-road-graph-walker-categories
plan: 03-01
type: tracer
wave: 1
depends_on: []
files_modified:
  - src/sim/roadNet.ts
  - tests/unit/roadnet.test.ts
autonomous: true
requirements: [ROAD-01]
must_haves:
  truths:
    - "Adding a road tile with no road neighbors still assigns it a component so connected(tile, tile) is true and connectivity reflects the change (ROAD-01)."
    - "affectedTiles() reports the recomputed (dirty) region touched by an add/remove, not only the single changed tile (ROAD-01 'recomputes only the affected region')."
    - "Adding/demolishing a road recomputes only the affected region; connectivity reflects the change (ROAD-01)."
    - "Two independent road components bridged by one tile then cut stay internally connected while the cross link breaks; a third far region is unaffected (ROAD-01 multi-region)."
---

<objective>
Tracer plan that establishes the Phase-3 baseline (typecheck + 289 tests) and closes the
two genuine ROAD-01 gaps in `src/sim/roadNet.ts` found by research: (1) an isolated
`addRoad` never assigns a component, so `connected(tile, tile)` returns false — the fallback
flood branch at roadNet.ts:145 is dead when `seeds` is empty (probe: `addRoad(0,0)` →
nodeCount 1 but `connected` false); (2) `affectedTiles()` fails its own header contract by
returning only the single changed tile (`[{x,y}]` at roadNet.ts:75/86) instead of the region
actually re-computed (recolorRegion re-floods the whole touched component at roadNet.ts:149-160).
It also extends tests to multi-region disconnect/reconnect with region isolation.

Output: `.planning/phases/03-road-graph-walker-categories/03-01-PLAN.md` plus the tasks below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-road-graph-walker-categories/03-CONTEXT.md
@.planning/phases/03-road-graph-walker-categories/03-RESEARCH.md
@.planning/phases/03-road-graph-walker-categories/03-VALIDATION.md

Behavior already verified in RESEARCH: `RoadNetwork` add/remove handles bridging and
cutting correctly for the multi-tile case; the isolated-add case and the dirty-region
observability are the two genuine bugs. Baseline: `npm run typecheck` clean and
`npm run test` green at 289 tests / 46 files (re-confirm the count on task 1).
</context>

<tasks>

<task type="tracer">
  <name>Baseline + fix isolated-add connectivity and dirty-region observability in RoadNetwork</name>
  <files>src/sim/roadNet.ts, tests/unit/roadnet.test.ts</files>
  <read_first>src/sim/roadNet.ts (constructor 37-39, addRoad 64-76, removeRoad 79-87, affectedTiles 42-44, recolorRegion 135-161, floodComponent 163-176), tests/unit/roadnet.test.ts, 03-CONTEXT.md decision 1, 03-RESEARCH.md section 1</read_first>
  <action>Read src/sim/roadNet.ts and tests/unit/roadnet.test.ts before editing. Establish the baseline first: run `npm run typecheck` and `npm run test` and record the count (289 tests / 46 files as of 2026-08-03; re-confirm and note any delta).
  Fix the isolated-add connectivity bug: in `recolorRegion(seeds)` (roadNet.ts:135), when `touchedCompIds.size === 0` the current fallback loop (line 145) iterates the caller-provided `seeds`, but `addRoad` passes the new tile's road-neighbor keys, which is the EMPTY array for an isolated tile. Change `addRoad` (roadNet.ts:64) so that for the no-neighbor case the new tile itself is passed as the flood seed (e.g. seeds = neighbors, and when neighbors is empty, seed the recolor with the new tile's own key so `floodComponent` runs and assigns it a fresh component id). Keep the existing behavior for the neighbor case unchanged. After the fix, `connected({x,y},{x,y})` must be true immediately after an isolated `addRoad(x,y)`.
  Widen the dirty-region observability: make the recolor path return the set of tiles it actually re-colored (the affected tiles whose component ids were cleared/re-flooded, plus any new tile), and have `addRoad`/`removeRoad` store that whole region into `lastAffected` instead of only `[{x,y}]` (roadNet.ts:75/86). The header contract (lines 34-35, 42) — "the dirty set from the most recent add/remove (affected region)" — must now hold: a bridge between two components yields a dirty region containing both old components plus the new tile; a cut yields the old component's tiles. Keep `affectedTiles()` returning a copy (`[...this.lastAffected]`).
  Update `tests/unit/roadnet.test.ts`: the existing assertion at lines 50-53 (`affectedTiles()` toEqual `[{x: 0, y: 0}]` after an isolated add) must be corrected to assert the widened region contains the added tile and at least the local region; add an assertion that an isolated `addRoad(0,0)` leaves `connected({x:0,y:0},{x:0,y:0})` true and `nodeCount()` equal to 1. Do not add multi-region cases here — task 2 owns those.</action>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 and `npm run test` reports the recorded baseline count (289) green before any fix.
    - `connected({x,y},{x,y})` returns true after an isolated `addRoad(x,y)` with no road neighbors.
    - `affectedTiles()` contains more than the single changed tile after an add/remove that merges or splits a component (size >= 2 in the bridge case).
    - `tests/unit/roadnet.test.ts` passes with the updated dirty-region assertion (`npx vitest run tests/unit/roadnet.test.ts` exits 0).
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/roadnet.test.ts</automated>
  </verify>
  <done>Baseline recorded; isolated `addRoad` assigns a component (connected(tile, tile) true); `affectedTiles()` reports the recomputed dirty region, not just the single tile; updated roadnet tests + typecheck green.</done>
</task>

<task type="auto">
  <name>Multi-region disconnect/reconnect + region-isolation tests for RoadNetwork</name>
  <files>src/sim/roadNet.ts, tests/unit/roadnet.test.ts</files>
  <read_first>src/sim/roadNet.ts (addRoad 64-76, removeRoad 79-87, connected 101-106, nodeCount 109-111, affectedTiles 42-44), tests/unit/roadnet.test.ts, 03-RESEARCH.md Q1/Q2</read_first>
  <action>Read src/sim/roadNet.ts and tests/unit/roadnet.test.ts (including the changes from task 1) before editing. Extend `tests/unit/roadnet.test.ts` with a 'multi-region disconnect/reconnect (ROAD-01)' describe block containing deterministically-scripted cases. Build an 8x8 earth map, add a left cluster (three connected tiles near the origin) and a right cluster (three connected tiles far right), leaving them initially disconnected. Assert: (1) before any bridge, `connected` between a left and a right tile is false, and `nodeCount()` equals 6. (2) Add a single bridge row of tiles linking the clusters (e.g. 4 consecutive tiles along one row such that there is exactly one articulation tile that, when removed, separates them); assert `connected(left, right)` becomes true and the articulation tile's removal (`removeRoad`) flips it back to false while both halves remain internally connected (`connected` within left is true and within right is true). (3) A third far-away disconnected region (a single isolated tile or small cluster placed far from both) is never colored into either cluster: assert `connected(third, left)` false and `connected(third, right)` false after the bridge is created, proving localized recompute does not touch it. (4) Reconnect by re-adding the articulation tile and assert connectivity returns to true. If any of these four cases fails against the current implementation (for example a split where the far region is accidentally merged), fix the corresponding logic in src/sim/roadNet.ts so the isolate-merge-split-remerge cycle is correct.
  Also assert the dirty-region isolation property alongside the multi-region cases: after a change confined to the left cluster, `affectedTiles()` must not include any tile in the far-away third region.</action>
  <acceptance_criteria>
    - `tests/unit/roadnet.test.ts` contains the four multi-region assertions (disconnected→bridged→cut→re-bridged) plus the third-region isolation assertion.
    - All assertions exercise only real RoadNetwork methods (addRoad, removeRoad, connected, nodeCount, affectedTiles) over an 8x8 `Map` constructed with the pattern in roadnet.test.ts:5-11.
    - `npx vitest run tests/unit/roadnet.test.ts` exits 0 with both the task-1 fixes and the new multi-region block green.
    - `npm run test` (full suite) exits 0, confirming no regression in the other 46 files.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/roadnet.test.ts && npm run test</automated>
  </verify>
  <done>Multi-region disconnect/reconnect, re-bridge, and third-region isolation tests added to tests/unit/roadnet.test.ts; the isolate-merge-split-remerge cycle is correct; full suite (289+ tests) and typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| map terrain → RoadNetwork | Connectivity queries must reflect actual road presence after every add/remove; a stale component id is a correctness fault, not just a perf issue |
| RoadNetwork → consumers | Sim systems rely on O(1) `connected`; an unassigned component silently yields false for reachability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | isolated-road add | high | mitigate | isolated `addRoad` now floods the new tile's own component (task 1); `connected(tile, tile)` true |
| T-03-02 | Tampering | dirty-region observability | medium | mitigate | `affectedTiles()` returns the true recomputed region; multi-region isolation test (task 2) |
</threat_model>

<verification>
Run `npm run test` (full suite, golden + determinism + property included) and `npm run typecheck`.
Spot-check: `npx vitest run tests/unit/roadnet.test.ts` is green.
Per-task sampling follows `.planning/phases/03-road-graph-walker-categories/03-VALIDATION.md`.
</verification>

<success_criteria>
- An isolated `addRoad` assigns a component (connected(tile, tile) true).
- `affectedTiles()` reports the genuinely recomputed (dirty) region.
- Multi-region bridge/cut/re-bridge cycles stay correct and a third far-away region is never touched.
- Full suite and typecheck remain green; baseline re-recorded at task 1.
</success_criteria>

<output>
Create `.planning/phases/03-road-graph-walker-categories/03-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/sim/roadNet.ts`: isolated-add component fix (flood seed for the empty-neighbor case); `recolorRegion` returns the re-colored affected region; `lastAffected` / `affectedTiles()` now carry the true dirty region.
- `tests/unit/roadnet.test.ts`: updated dirty-region assertion; new `multi-region disconnect/reconnect` block (bridged, cut, re-bridged, third-region isolation, dirty-region isolation).
