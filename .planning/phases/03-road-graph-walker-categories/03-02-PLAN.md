---
phase: 03-road-graph-walker-categories
plan: 03-02
type: auto
wave: 2
depends_on: [03-01]
files_modified:
  - src/sim/tile.ts
  - src/sim/map.ts
  - src/sim/runner.ts
  - src/sim/walkers.ts
  - src/sim/housing.ts
  - tests/unit/road-type-wiring.test.ts
  - tests/unit/road-type-effects.test.ts
  - tests/golden/fixtures/food-chain-golden.json
  - tests/golden/fixtures/paused-commands-golden.json
autonomous: true
requirements: [ROAD-02]
must_haves:
  truths:
    - "ROAD_TYPES covers the seven required road types (dirt, paved, plaza, bridge, service roadblock, wharf access, stairs) with distinct movement and desirability (ROAD-02)."
    - "Road types affect walker speed: walker per-tick road progress on a typed road tile scales by roadSpeedMultiplier(effectiveType), and paved is faster than dirt (ROAD-02)."
    - "Road types affect desirability: a house adjacent to a road tile gains that road type's desirability contribution (plaza adds more than dirt) in desirabilityOf (ROAD-02)."
    - "The public per-tile snapshot exposes each road tile's effective roadType via SimRunner.getTileState (ROAD-02)."
---

<objective>
Wire the existing 7-type `ROAD_TYPES` table (src/sim/roadTypes.ts) into real behavior.
Research found the table is complete but completely unwired: `roadTypes.ts` has zero src/
consumers, `move()` uses the flat `CONFIG.walkerSpeedPerTick` (walkers.ts:263), and
`desirabilityOf` ignores roads (housing.ts:17-52). There is no per-tile road type at all
(`TileType` is a bare 'road', types.ts:9; `TileState` has no roadType field, tile.ts:9-25).
Deliver the CORE-03-pattern side-channel (`roadType` on TileState + Map
`roadTypeAt`/`setRoadType` + `getTileState` exposure), then wire walker speed and house
desirability to it. Because adjacent-road desirability is a genuine mechanic change, the
two food-city goldens are regenerated intentionally in this plan.

Output: `.planning/phases/03-road-graph-walker-categories/03-02-PLAN.md` plus the tasks below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-road-graph-walker-categories/03-CONTEXT.md
@.planning/phases/03-road-graph-walker-categories/03-RESEARCH.md
@.planning/phases/03-road-graph-walker-categories/03-VALIDATION.md

Wired by plan 03-01 (parent): `src/sim/roadNet.ts` correctness + dirty-region fixes.
ROAD-02 facts from RESEARCH section 1: `ROAD_TYPES` has all 7 types
(src/sim/roadTypes.ts:29-37); walker speed is flat (walkers.ts:263); `desirabilityOf`
(housing.ts:17-52) reads only the house tile's terrain; no per-tile road type exists.
Baseline: 289 tests / 46 files green.
</context>

<tasks>

<task type="auto">
  <name>Per-tile road-type side-channel on TileState + Map + SimRunner snapshot</name>
  <files>src/sim/tile.ts, src/sim/map.ts, src/sim/runner.ts, tests/unit/road-type-wiring.test.ts</files>
  <read_first>src/sim/tile.ts (TileState 9-25, defaultTileState 27-45), src/sim/map.ts (constructor 20-35, tileState 128-131, mutateTileState 134-137, get 117-125, set 122-125), src/sim/roadTypes.ts (RoadType 9-16, ROAD_TYPES 29-37), src/sim/runner.ts (getTileState 495-498), 03-CONTEXT.md decision 2</read_first>
  <action>Read src/sim/tile.ts, src/sim/map.ts, and src/sim/runner.ts before editing. Add the road-type side-channel following the CORE-03 TileState pattern:
  - src/sim/tile.ts: add `roadType: RoadType | null;` to the `TileState` interface and set it to `null` in `defaultTileState()` (import `type { RoadType } from './roadTypes'`).
  - src/sim/map.ts: add a public `roadTypeAt(x: number, y: number): RoadType | null` that returns the in-bounds tile's `states[y][x].roadType` (null when out of bounds or never set) and a public `setRoadType(x: number, y: number, type: RoadType | null): void` that no-ops out of bounds and otherwise writes `states[y][x].roadType` (import `type { RoadType } from './roadTypes'`). Terrain authority is untouched — typed roads are still 'road' terrain; the road type is a refinement only.
  - src/sim/runner.ts: in `getTileState` (line 495-498) include `roadType: this.map.roadTypeAt(x, y)` in the returned object alongside the existing `road` flag.
  Create `tests/unit/road-type-wiring.test.ts` asserting: `defaultTileState().roadType === null`; on a fresh 8x8 earth `Map`, `roadTypeAt(2,2) === null`; after `map.setRoadType(2, 2, 'paved')`, `roadTypeAt(2,2) === 'paved'`; after `map.setRoadType(2, 2, null)`, `roadTypeAt(2,2) === null`; `roadTypeAt(99,99) === null` (out of bounds); a road placed via `map.set(2,2,'road')` still reads `map.get(2,2) === 'road'` with `roadTypeAt(2,2) === 'paved'` (terrain and side-channel coexist); and a `SimRunner` over that map (import from ../../src/sim/runner) exposes `getTileState(2,2).roadType === 'paved'` after `setRoadType`.</action>
  <acceptance_criteria>
    - `defaultTileState().roadType` is `null`; `TileState` has the `roadType: RoadType | null` field.
    - `Map.roadTypeAt`/`Map.setRoadType` round-trip a typed value and reset to null; out-of-bounds reads return null.
    - Setting a road type does not change `Map.get` terrain ('road' stays 'road').
    - `SimRunner.getTileState(x, y).roadType` reflects the side-channel value.
    - `tests/unit/road-type-wiring.test.ts` passes (`npx vitest run tests/unit/road-type-wiring.test.ts` exits 0) and typecheck is clean.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/road-type-wiring.test.ts</automated>
  </verify>
  <done>TileState.roadType (default null), Map.roadTypeAt/setRoadType, and SimRunner.getTileState.roadType exist and are unit-tested; typecheck clean.</done>
</task>

<task type="auto">
  <name>Wire road types into walker speed and house desirability; regenerate goldens</name>
  <files>src/sim/walkers.ts, src/sim/housing.ts, src/sim/roadTypes.ts, tests/unit/road-type-effects.test.ts, tests/golden/fixtures/food-chain-golden.json, tests/golden/fixtures/paused-commands-golden.json</files>
  <read_first>src/sim/walkers.ts (move 249-271, SimInternals 82-92, DIRS 94-99), src/sim/housing.ts (desirabilityOf 17-52), src/sim/roadTypes.ts (roadSpeedMultiplier 43-45, roadDesirability 47-49), src/sim/map.ts (roadTypeAt added in task 1), tests/unit/road-types.test.ts, 03-CONTEXT.md decision 2 + goldens note</read_first>
  <action>Read src/sim/walkers.ts, src/sim/housing.ts, and src/sim/roadTypes.ts before editing. Use the task-1 side-channel:
  - src/sim/walkers.ts: import `roadSpeedMultiplier` from './roadTypes'. In `move()` (line ~263), compute `const rt = sim.map.roadTypeAt(w.x, w.y) ?? 'dirt';` and change `w.progress += CONFIG.walkerSpeedPerTick;` to `w.progress += CONFIG.walkerSpeedPerTick * roadSpeedMultiplier(rt);`. On the default (bare 'road' -> 'dirt') tile the multiplier is 1, so existing behavior is unchanged; a 'paved' tile moves the walker 1.25x faster.
  - src/sim/housing.ts: in `desirabilityOf` (lines 17-52), after the existing terrain base and before the clamp, add an adjacent-road desirability contribution: import `roadDesirability` from './roadTypes', iterate the 4 orthogonal neighbors of (x, y), and for each neighbor whose terrain is 'road' add `roadDesirability(map.roadTypeAt(nx, ny) ?? 'dirt')` to `total`. Keep the existing `[0, 200]` clamp and rounding unchanged. (service_roadblock's desirability is 0, so blocking tiles contribute nothing.)
  Create `tests/unit/road-type-effects.test.ts` using an 8x8 earth `Map` with a road strip, asserting: (1) speed — a `createWalker('well', ...)` standing on a 'paved' road tile advances `progress` by `CONFIG.walkerSpeedPerTick * roadSpeedMultiplier('paved')` after one `updateWalker` call via a stub `SimInternals` (reuse the makeStub pattern from tests/unit/walkers.test.ts), which is strictly greater than the same case on a 'dirt' tile; (2) desirability — a house tile (x, y) with earth base has `desirabilityOf(map, x, y, policy, false)` equal to the baseline (30) with no road neighbors, and equals `baseline + roadDesirability('plaza')` (i.e. +4) after `map.setRoadType` on an orthogonally adjacent road tile to 'plaza', while a 'service_roadblock' neighbor adds 0.
  Regenerate the intentionally-changed goldens: run `GOLDEN_UPDATE=1 npm run test:golden:update` so `tests/golden/fixtures/food-chain-golden.json` and `tests/golden/fixtures/paused-commands-golden.json` capture the new adjacent-road desirability, then run the full suite; it must be green (this is a documented ROAD-02 mechanic change, not a test weakening).</action>
  <acceptance_criteria>
    - `move()` computes progress using `roadSpeedMultiplier` of the current tile's effective road type; bare 'road' tiles keep a 1x multiplier (no default-speed regression).
    - `desirabilityOf` adds `roadDesirability(effectiveType)` for each orthogonally adjacent road tile, still clamped to [0, 200].
    - `tests/unit/road-type-effects.test.ts` passes, including the paved-vs-dirt speed delta and the plaza-adjacency +4 desirability delta.
    - `npm run test:golden:update` (GOLDEN_UPDATE=1) regenerates both golden fixtures and the resulting full `npm run test` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/road-type-effects.test.ts && npx vitest run tests/unit/road-types.test.ts && GOLDEN_UPDATE=1 npm run test:golden:update && npm run test</automated>
  </verify>
  <done>Walker per-tick speed scales by roadSpeedMultiplier(tile); desirabilityOf adds adjacent-road desirability; road-type-effects tests green; goldens regenerated intentionally and full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROAD_TYPES → walker movement | A wrong passability/speed value changes movement without breaking on-road legality; speed is a multiplier only |
| ROAD_TYPES → house desirability | Desirability drives evolution thresholds; adjacent-road contribution must stay clamped and deterministic |
| TileState side-channel → public snapshot | roadType must never contradict terrain ('road' only), and must round-trip save/load deterministically |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | per-tile roadType side-channel | medium | mitigate | roadTypeAt/setRoadType + getTileState wiring test (task 1) |
| T-03-04 | Tampering | speed/desirability wiring | medium | mitigate | speed delta + plaza +4 desirability + golden regen tests (task 2) |
</threat_model>

<verification>
Run `npm run test` (full suite, golden + determinism + property included) and `npm run typecheck`.
Spot-check: `npx vitest run tests/unit/road-type-wiring.test.ts` and `npx vitest run tests/unit/road-type-effects.test.ts` are green.
Per-task sampling follows `.planning/phases/03-road-graph-walker-categories/03-VALIDATION.md`.
</verification>

<success_criteria>
- TileState/Map/runner expose a per-tile roadType side-channel (default null == dirt).
- Walker per-tick speed scales with the current road tile's speedMultiplier (paved > dirt).
- House desirability includes adjacent road tiles' desirability (plaza > dirt), clamped to [0, 200].
- Both golden fixtures regenerated intentionally; full suite + typecheck green.
</success_criteria>

<output>
Create `.planning/phases/03-road-graph-walker-categories/03-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/sim/tile.ts`: `TileState.roadType: RoadType | null` (default null in `defaultTileState()`).
- `src/sim/map.ts`: new `roadTypeAt(x, y)` and `setRoadType(x, y, type)` methods.
- `src/sim/runner.ts`: `getTileState` now returns `roadType` from `map.roadTypeAt`.
- `src/sim/walkers.ts`: `move()` speed multiplied by `roadSpeedMultiplier` of the current road tile.
- `src/sim/housing.ts`: `desirabilityOf` adds adjacent-road desirability via `roadDesirability`.
- New test files: `tests/unit/road-type-wiring.test.ts`, `tests/unit/road-type-effects.test.ts`.
- Regenerated golden fixtures: `tests/golden/fixtures/food-chain-golden.json`, `tests/golden/fixtures/paused-commands-golden.json`.
