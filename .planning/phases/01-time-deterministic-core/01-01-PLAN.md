---
phase: 01-time-deterministic-core
plan: 01-01
type: tracer
wave: 1
depends_on: []
files_modified:
  - src/sim/time.ts
  - tests/unit/time.test.ts
  - tests/determinism/determinism.test.ts
autonomous: true
requirements: [CORE-01]
must_haves:
  truths:
    - "Player can pause and select 0.5x/1x/2x/4x/8x speeds; the sim advances on a fixed timestep independent of frame rate (CORE-01)."
    - "The TimeSystem produces tick counts by integer division of accumulated simulated ms, so the total tick count over a wall-clock window is independent of how the window is sliced into frames."
    - "Pausing returns zero ticks and does not accumulate simulated time."
    - "Stepping the same seed/map/commands in different tick chunk sizes yields an identical final state."
---

<objective>
Verify-as-built the CORE-01 time core (TimeSystem + pause/speed wiring) against the existing
implementation, lock the frame-rate-independence argument into documentation and tests, and
fill the small test gaps (individual 0.5x/4x/8x rates, exact-boundary/accumulator carry-over,
chunked-stepping determinism). This is the tracer plan: it establishes the baseline and the
determinism contract every later phase relies on.

Output: `.planning/phases/01-time-deterministic-core/01-01-PLAN.md` plus the tasks below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-time-deterministic-core/01-CONTEXT.md
@.planning/phases/01-time-deterministic-core/01-RESEARCH.md
@.planning/phases/01-time-deterministic-core/01-VALIDATION.md

Existing implementation: `TimeSystem` in src/sim/time.ts (advance = integer division of
accumulated ms by stepMs; pause returns 0; speed multiplies accumulation; SPEED_PRESETS
[0.5,1,2,4,8]); wired in MainScene.update (src/game/scenes/MainScene.ts:135-142) and speed
buttons in HUDScene (src/game/scenes/HUDScene.ts:159-170). Baseline: `npm run typecheck` clean,
`npm run test` green (253 tests). Per-task sampling follows 01-VALIDATION.md.
</context>

<tasks>

<task type="tracer">
  <name>Baseline audit + document the integer-division frame-rate-independence argument</name>
  <files>src/sim/time.ts, tests/unit/time.test.ts, src/game/scenes/MainScene.ts, src/game/scenes/HUDScene.ts, tests/determinism/determinism.test.ts</files>
  <action>Read src/sim/time.ts, tests/unit/time.test.ts, src/game/scenes/MainScene.ts, and
  src/game/scenes/HUDScene.ts before editing. Establish the baseline: run `npm run typecheck`
  and `npm run test` and confirm both are green (record the pass count). Audit CORE-01 against
  the as-built implementation: TimeSystem.advance (src/sim/time.ts:28-41) must accumulate
  `realDtMs * speed` and divide by `stepMs` with integer division (pause returns 0 at line 29;
  speed scales at line 30; maxCatchupSteps caps catch-up at lines 32-39); MainScene.update
  (src/game/scenes/MainScene.ts:135-142) must tick the runner exactly the returned count;
  HUDScene (src/game/scenes/HUDScene.ts:157-170) must bind all five presets plus pause/resume.
  Then extend the TimeSystem header docstring (src/sim/time.ts:1-10) with the explicit
  argument: total ticks for a wall-clock window of T ms at speed S = floor((T*S)/stepMs),
  which is a function of total simulated ms only — not of how T is sliced into frame deltas —
  so identical state results at any frame rate. Add a chunked-stepping determinism test to
  tests/determinism/determinism.test.ts: run the same seed/map/command script (reuse
  buildFoodCity + setPolicy from tests/helpers.ts) ticking in chunk sizes [1, 7, 50] and assert
  all three produce an identical getStateJson() — this makes the frame-rate-independence
  contract observable at the sim level.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/time.test.ts && npx vitest run tests/determinism/determinism.test.ts</automated>
  </verify>
  <done>Baseline recorded (typecheck clean, full `npm run test` green with the pass count noted
  in the task), TimeSystem docstring documents the floor(T*S/stepMs) integer-division argument,
  and the chunked-stepping determinism test passes.</done>
</task>

<task type="auto">
  <name>Fill CORE-01 test gaps: per-preset speeds, pause at 8x, exact boundary, accumulator carry</name>
  <files>tests/unit/time.test.ts</files>
  <action>Read tests/unit/time.test.ts first. Add cases to the existing `TimeSystem` describe
  block covering: 0.5x — `new TimeSystem(250)` at speed 0.5 over 1000ms returns exactly 2 ticks
  (half rate); 4x — 250ms returns 4 ticks; 8x — 250ms returns 8 ticks; paused at 8x —
  `setPaused(true)` then `advance(1000)` returns 0; exact boundary — `advance(stepMs)` returns
  1 tick; accumulator carry-over — two `advance(125)` calls at stepMs 250 total exactly 1 tick
  and `pendingMs()` reflects the leftover; each preset in SPEED_PRESETS (src/sim/time.ts:58) is
  accepted by setSpeed without throwing.</action>
  <verify>
    <automated>npx vitest run tests/unit/time.test.ts && npx vitest run tests/determinism/determinism.test.ts && npm run typecheck</automated>
  </verify>
  <done>New TimeSystem tests pass for 0.5x/2x/4x/8x, paused-at-8x, exact boundary, accumulator
  carry-over, and all presets; determinism suite still green; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frame loop → TimeSystem | Phaser delta must not leak into sim state; only integer tick counts cross the boundary |
| TimeSystem → SimRunner | tick() is the only sim-stepping entry; pause/speed must never mutate sim state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | frame-rate coupling | medium | mitigate | integer-division arg documented in src/sim/time.ts + chunked-stepping determinism test proves slice-independence |
| T-01-02 | Denial of Service | spiral-of-death burst | low | mitigate | maxCatchupSteps cap already in TimeSystem; existing catch-up test stays green |
</threat_model>

<verification>
Run `npm run test` (full suite, determinism + golden included) and `npm run typecheck`.
Spot-check: `npx vitest run tests/unit/time.test.ts` and `npx vitest run tests/determinism/determinism.test.ts` are green.
Per-task sampling follows `.planning/phases/01-time-deterministic-core/01-VALIDATION.md`.
</verification>

<success_criteria>
- TimeSystem docstring documents the floor(T*S/stepMs) frame-rate-independence argument (CORE-01).
- Per-preset speed tests (0.5x/1x/2x/4x/8x), pause, exact-boundary, and carry-over are green.
- Chunked-stepping determinism test proves identical state across tick chunk sizes.
- Full suite (253 tests) and typecheck remain green.
</success_criteria>

<output>
Create `.planning/phases/01-time-deterministic-core/01-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- No new runtime symbols (documentation-only change to `TimeSystem` docstring in `src/sim/time.ts`).
- New test cases: `tests/unit/time.test.ts` (speed/pause/boundary/carry cases); `tests/determinism/determinism.test.ts` (chunked-stepping case).
