---
phase: 04-water-system
plan: 04-03
type: feature
wave: 3
depends_on: [04-02]
files_modified:
  - src/sim/water.ts
  - src/sim/advisors.ts
  - tests/unit/baths.test.ts
  - tests/unit/advisors.test.ts
autonomous: true
requirements: [WATR-05, WATR-06]
must_haves:
  truths:
    - "Public baths require reservoir water and workers; they improve health (wellness) and desirability in their radius and consume a small amount of water while active only (WATR-05)."
    - "Baths without reservoir water or without workers provide no wellness/desirability and consume no water (WATR-05)."
    - "The water overlay advisor DATA exposes sources, reservoir filled/level, aqueduct flow (present vs flowing), well/fountain coverage, house water classes, and desirability as per-tile grids (WATR-06)."
    - "Housing consumes water through the service-cooldown path (waterCooldown) unchanged; WaterClass is exposed as overlay/advisor data (WATR-06 judgment)."
---

<objective>
Close the WATR-05 and WATR-06 gaps found in RESEARCH. WATR-05: `computeBathCoverage`
(water.ts:252-275) grants wellness/desirability for any `active` bath but never
enforces the §14.5 preconditions — reservoir water and workers — nor accounts for the
small water consumption, and is not wired into a health/desirability surface
(decisions 1/2). Add `BathDef { supplied, staffed, waterCostPerTick? }` +
`resolveBaths` (active = supplied && staffed) and `assignBathEffects`
(wellness/desirability/waterConsumed), reusing the existing `computeBathCoverage`.
WATR-06 (decision 5): the advisor data layer (`src/sim/advisors.ts`) exposes only a
scalar water percent (runner.ts:273-278, 292) — add `waterOverlayData` producing the
per-tile WATR-06 grids (sources, reservoir filled/level, aqueduct present vs flowing,
well/fountain coverage, house water classes, desirability) and extend the advisor
tests. Visual rendering stays in Phase 18.

Output: `.planning/phases/04-water-system/04-03-PLAN.md` plus the tasks below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-water-system/04-CONTEXT.md
@.planning/phases/04-water-system/04-RESEARCH.md
@.planning/phases/04-water-system/04-VALIDATION.md
@.planning/phases/04-water-system/04-01-PLAN.md
@.planning/phases/04-water-system/04-02-PLAN.md

Existing implementation: `src/sim/water.ts` — `PublicBathDef` (244-249),
`computeBathCoverage` (252-275), plus ReservoirState/reservoir helpers (04-01) and
fountain activity (04-02); `TileWater.desirability` exists. `src/sim/advisors.ts` —
`SimSnapshot.hasWater` (19), `advisorsFrom` (44-60), `overlaysFrom` (63-79);
tests/unit/advisors.test.ts (40 lines). Housing consumes water via `waterCooldown`
(housing.ts:93-98; walkers.ts:156-170). Baseline: 316 tests / 52 files green,
typecheck clean. Per-task sampling follows 04-VALIDATION.md.
</context>

<tasks>

<task type="auto">
  <name>Public baths: reservoir-water + workers wiring to health/desirability (WATR-05)</name>
  <files>src/sim/water.ts, tests/unit/baths.test.ts</files>
  <read_first>src/sim/water.ts (PublicBathDef 244-249, computeBathCoverage 252-275), tests/unit/water.test.ts (baths describe block 60-75), 04-CONTEXT.md decisions 1/2, 04-RESEARCH.md section 1 WATR-05</read_first>
  <action>Read src/sim/water.ts and the existing baths tests in tests/unit/water.test.ts before editing. Add the WATR-05 preconditions without changing the existing `PublicBathDef` / `computeBathCoverage` signatures (they are used by water.test.ts:60-75): (1) export `BATH_DEFAULT_WATER_COST = 1` and `export interface BathDef { x: number; y: number; radius: number; supplied: boolean; staffed: boolean; waterCostPerTick?: number }`; (2) export `resolveBaths(defs: BathDef[]): { active: PublicBathDef[]; waterConsumed: number }` where a def is active only when `supplied &amp;&amp; staffed`, mapped to `{ x, y, radius, active: true }`, and `waterConsumed` sums `waterCostPerTick ?? BATH_DEFAULT_WATER_COST` over the active baths only; (3) export `assignBathEffects(defs: BathDef[], width: number, height: number): { wellness: number[][]; desirability: number[][]; waterConsumed: number }` that calls `resolveBaths`, feeds `active` into the existing `computeBathCoverage`, and returns `{ wellness, desirability, waterConsumed }` (wellness grid = health input, desirability grid = sim desirability input — decision 2).
  Create tests/unit/baths.test.ts with a 'public baths wiring (WATR-05)' describe block importing resolveBaths, assignBathEffects, and BATH_DEFAULT_WATER_COST. Assert: (1) a supplied-and-staffed bath at (3,3) radius 2 on a 7x7 map gives `wellness[3][3] === 1`, `desirability[3][3] === 4`, `waterConsumed === BATH_DEFAULT_WATER_COST`, and a tile at (0,0) (distance 6) is zero on both grids; (2) the same bath with `staffed:false` (workers removed) yields all-zero wellness/desirability and `waterConsumed === 0`; (3) with `supplied:false` (no reservoir water) it likewise yields zeros and no consumption; (4) two active baths default to `waterConsumed === 2 * BATH_DEFAULT_WATER_COST` while a mixed supplied-but-unstaffed bath contributes no cost; (5) `resolveBaths` returns an empty `active` array when every def is unstaffed or unsupplied. The existing computeBathCoverage tests in tests/unit/water.test.ts must still pass unchanged.</action>
  <acceptance_criteria>
    - src/sim/water.ts exports `BATH_DEFAULT_WATER_COST`, `BathDef`, `resolveBaths`, and `assignBathEffects`; `PublicBathDef`/`computeBathCoverage` signatures unchanged.
    - Baths gate on supplied AND staffed; wellness/desirability only inside the radius when active; waterConsumed sums only active baths (default cost 1).
    - tests/unit/baths.test.ts exists with the five assertions; `npx vitest run tests/unit/baths.test.ts` exits 0 and `npx vitest run tests/unit/water.test.ts` (existing baths tests) still exits 0; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/baths.test.ts && npx vitest run tests/unit/water.test.ts</automated>
  </verify>
  <done>BathDef + resolveBaths gate baths on reservoir water and workers; assignBathEffects returns wellness (health) and desirability grids plus waterConsumed; bath wiring tests + full water suite + typecheck green.</done>
</task>

<task type="auto">
  <name>Water overlay advisor data: sources, flow, coverage, house water classes (WATR-06)</name>
  <files>src/sim/advisors.ts, src/sim/water.ts, tests/unit/advisors.test.ts</files>
  <read_first>src/sim/advisors.ts (SimSnapshot 10-28, overlaysFrom 63-79), src/sim/water.ts (WaterSystem.compute 46-91, TileWater 25-32 + desirability, ReservoirState from 04-01, AqueductSystem key convention y*100000+x 128-130, fountain activity from 04-02), src/sim/housing.ts (waterCooldown 93-98), tests/unit/advisors.test.ts, 04-CONTEXT.md decision 5, 04-RESEARCH.md Q2/Q3</read_first>
  <action>Read src/sim/advisors.ts, src/sim/water.ts, src/sim/housing.ts lines 93-98, and tests/unit/advisors.test.ts before editing. Confirm the decision-5 housing audit is recorded (housing consumes water via `waterCooldown`, walkers.ts:156-170 — the live path is left untouched; WaterClass is an overlay classification).
  Add the WATR-06 advisor data: (1) in src/sim/advisors.ts import `TileWater`, `ReservoirState` from './water' (types only) and export `export interface WaterOverlayInput { width: number; height: number; grid: TileWater[][]; aqueductTiles: Set&lt;number&gt;; flowing: Set&lt;number&gt;; reservoirStates: ReservoirState[] }`; (2) export `waterOverlayData(input: WaterOverlayInput): Record&lt;string, number[][]&gt;` that returns per-tile grids (each a width by height number[][] initialized to 0, using the same `y * 100000 + x` key encoding as AqueductSystem for Set lookups): `sources` = 1 where `grid[y][x].sourceTile`; `wellCoverage` = 1 where `coveredByWell`; `fountainCoverage` = 1 where `coveredByFountain`; `houseWaterClass` = map of `grid[y][x].kind` to 0/1/2/3 for none/basic/clean/grand (with 'grand' reachable when a grand-class source exists, defaulting to 'clean' for now); `aqueductPresent` = 1 for every tile whose key is in `aqueductTiles`; `aqueductFlow` = 1 for tiles whose key is both in `aqueductTiles` and `flowing`; `reservoirFilled` = 1 for every footprint tile of a filled ReservoirState (the x..x+size-1, y..y+size-1 footprint) else 0; `reservoirLevel` = the reservoir's `level` on its footprint tiles else 0; `desirability` = `grid[y][x].desirability`. Do not reuse `overlaysFrom` — build the grids directly from the water-model inputs so the advisor surface is testable in isolation.
  Extend tests/unit/advisors.test.ts with a 'water overlay data (WATR-06)' describe block importing waterOverlayData, WaterSystem, and the constants from src/sim/water. Construct a 5x5 map with `new WaterSystem()` fed both an active well at (1,1) radius 2 and an active fountain at (2,2) radius 2 via setSources; run `compute(5,5,()=>0)` to get the grid; then call waterOverlayData with an explicit `aqueductTiles` Set containing keys for (2,2) and (3,2), a `flowing` Set containing only (2,2), and `reservoirStates` with one filled 3x3 state at (0,0).   Assert: `wellCoverage[1][1] === 1` and `fountainCoverage[2][2] === 1`; `houseWaterClass[1][1] === 2` (clean — the fountain at (2,2) radius 2 also covers (1,1) at distance 2, and fountain outranks well in `waterClassAt`) and `houseWaterClass[2][2] === 2` (clean); `sources[1][1] === 1`; `aqueductPresent[3][2] === 1` with `aqueductFlow[3][2] === 0` (present, not flowing) and `aqueductFlow[2][2] === 1`; `reservoirFilled[1][1] === 1` and `reservoirLevel[1][1] ===` the filled state's level; `desirability[2][2]` equals the fountain bonus combined with the (2,2) well overlap from the same grid. Keep all existing advisors tests in the file passing.</action>
  <acceptance_criteria>
    - src/sim/advisors.ts exports `WaterOverlayInput` and `waterOverlayData` returning grids for sources, wellCoverage, fountainCoverage, houseWaterClass, aqueductPresent, aqueductFlow, reservoirFilled, reservoirLevel, desirability.
    - The housing audit note is recorded (housing uses waterCooldown; live path unchanged).
    - tests/unit/advisors.test.ts contains the water-overlay block with the enumerated assertions; `npx vitest run tests/unit/advisors.test.ts` exits 0 and `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/advisors.test.ts</automated>
  </verify>
  <done>waterOverlayData in src/sim/advisors.ts exposes the WATR-06 per-tile grids (sources, well/fountain coverage, water classes, aqueduct present vs flow, reservoir filled/level, desirability) from the water model; advisor tests + typecheck green; visual rendering deferred to Phase 18.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| water model → advisor overlay data | waterOverlayData must be a pure projection of the model grids — no fabricated values, every painted tile traceable to model state |
| bath activity → health/desirability | wellness/desirability must derive only from active (supplied && staffed) baths; inactive baths must not contribute and must not consume water |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Spoofing | bath grants health/desirability while unstaffed or unsupplied | high | mitigate | assignBathEffects gates on resolveBaths (supplied && staffed); zero-grid + zero-cost cases asserted |
| T-04-06 | Information Disclosure | overlay data fabricated / not traceable to model state | medium | mitigate | waterOverlayData is a pure projection; tests pin well/fountain/reservoir/flow/class grids to explicit model inputs |
</threat_model>

<verification>
Run `npm run test` (full suite, golden + determinism + property included) and `npm run typecheck`.
Spot-check: `npx vitest run tests/unit/baths.test.ts` and `npx vitest run tests/unit/advisors.test.ts` are green.
Per-task sampling follows `.planning/phases/04-water-system/04-VALIDATION.md`.
</verification>

<success_criteria>
- Baths require reservoir water and workers; they yield wellness (health) and desirability in radius and account water consumption while active only (WATR-05).
- The advisor data layer exposes the WATR-06 water overlay grids (sources, reservoir filled/level, aqueduct flow, well/fountain coverage, house water classes, desirability); visual rendering is Phase 18.
- Full suite (316 tests) and typecheck remain green.
</success_criteria>

<output>
Create `.planning/phases/04-water-system/04-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/sim/water.ts`: `BATH_DEFAULT_WATER_COST` const; `BathDef` interface; `resolveBaths()` and `assignBathEffects()` helpers.
- `src/sim/advisors.ts`: `WaterOverlayInput` interface and `waterOverlayData()` (WATR-06 per-tile grids).
- New test file: `tests/unit/baths.test.ts`; `tests/unit/advisors.test.ts` extended.
