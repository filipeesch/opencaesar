---
phase: 04-water-system
plan: 04-01
type: tracer
wave: 1
depends_on: []
files_modified:
  - src/sim/water.ts
  - tests/unit/water.test.ts
  - tests/unit/reservoir.test.ts
autonomous: true
requirements: [WATR-01, WATR-02]
must_haves:
  truths:
    - "Houses receive water classes from fountains/wells with coverage (WATR-01 basic well water + sanitary risk; WATR-04 clean fountain water). (goal-baseline)"
    - "A well provides local basic water with a slight desirability penalty and sanitary risk in polluted regions (WATR-01)."
    - "A reservoir is a 3x3 footprint that fills when it touches map water or receives a flowing aqueduct and exposes storage level plus inlet/outlet connectivity (WATR-02)."
    - "Sanitary risk appears only where well coverage meets pollution; water class stays 'basic' under well-only coverage (WATR-01)."
---

<objective>
Tracer plan that establishes the Phase-4 baseline (typecheck + 316 tests) and closes the
two genuine gaps RESEARCH found in `src/sim/water.ts` for WATR-01 and WATR-02: (1)
reservoirs have no observable storage/level/inlet/outlet state — `ReservoirDef`
(water.ts:99-106) is only `{x,y,size,active}` and nothing exposes §14.2 "armazena água /
mostra entrada, saída e nível"; (2) wells have no desirability penalty — `TileWater`
(water.ts:25-32) carries `wellness`/`sanitaryRisk` but no desirability, so the §14.1
"reduz levemente desejabilidade" behavior is absent from model and tests.

Output: `.planning/phases/04-water-system/04-01-PLAN.md` plus the tasks below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-water-system/04-CONTEXT.md
@.planning/phases/04-water-system/04-RESEARCH.md
@.planning/phases/04-water-system/04-VALIDATION.md

Existing implementation: `src/sim/water.ts` — `WaterSystem` (water.ts:38-97,
`compute()` at 46-91, `sanitaryRisk` at 83), `ReservoirDef` (99-106), `TileWater`
(25-32). `tests/unit/water.test.ts` has 7 tests. Baseline: `npm run typecheck` clean,
`npm run test` green at 316 tests / 52 files (re-confirm the count on task 1).
Per-task sampling follows 04-VALIDATION.md.
</context>

<tasks>

<task type="tracer">
  <name>Baseline + reservoir storage/inlet/outlet/level state (WATR-02)</name>
  <files>src/sim/water.ts, tests/unit/reservoir.test.ts</files>
  <read_first>src/sim/water.ts (ReservoirDef 99-106, AqueductSystem.computeFlow 147-209, reservoirTouchesWater 215-234, acceptableFountainSpots 237-242), tests/unit/water.test.ts, 04-CONTEXT.md decisions 1/3, 04-RESEARCH.md sections 1/3</read_first>
  <action>Read src/sim/water.ts and tests/unit/water.test.ts before editing. Establish the baseline first: run `npm run typecheck` and `npm run test`, record 316 tests passing (re-confirm and note any delta).
  Add to src/sim/water.ts the WATR-02 reservoir storage/level/inlet/outlet surface without changing any existing export signature: (1) export a module const `RESERVOIR_STORAGE_CAPACITY = 256`; (2) export an interface `ReservoirState { x: number; y: number; size: number; capacity: number; level: number; filled: boolean; inletConnected: boolean; outletToAqueduct: boolean }`; (3) export a pure helper `reservoirTouchesMapWater(r: ReservoirDef, width: number, height: number, hasMapWater: (x: number, y: number) => boolean): boolean` that replicates the perimeter-neighbor scan currently private in AqueductSystem.reservoirTouchesWater (water.ts:222-232): for every tile of the r.size by r.size footprint, any orthogonally adjacent in-bounds tile where hasMapWater returns true yields true; (4) export `computeReservoirStates(width: number, height: number, hasMapWater: (x: number, y: number) => boolean, reservoirs: ReservoirDef[], flowing: Set&lt;number&gt;): ReservoirState[]` — for each reservoir: `touchesWater = reservoirTouchesMapWater(r, width, height, hasMapWater)`; `feedsFlow` = any tile in the `flowing` set that is orthogonally adjacent to the reservoir footprint (use the same key encoding as AqueductSystem.key — `y * 100000 + x`); `filled = r.active &amp;&amp; (touchesWater || feedsFlow)`; `inletConnected = touchesWater || feedsFlow`; `outletToAqueduct = feedsFlow`; `capacity = RESERVOIR_STORAGE_CAPACITY`; `level = filled ? capacity : 0`.
  Create tests/unit/reservoir.test.ts with a 'reservoir storage state (WATR-02)' describe block that imports these exports and asserts: an active 3x3 reservoir at (2,2) touching a map water tile at (1,2) on a 8x8 map yields one state with `filled true`, `level === RESERVOIR_STORAGE_CAPACITY`, `inletConnected true`, `outletToAqueduct false`; the same reservoir with an explicit `flowing` set containing the key of an adjacent aqueduct tile (e.g. tile (5,2) when the reservoir spans (2..4,2..4)) yields `outletToAqueduct true` and `inletConnected true`; an active 3x3 reservoir isolated from both map water and the `flowing` set yields `filled false`, `level 0`, `inletConnected false`; an inactive reservoir even while touching map water yields `filled false` and `level 0`; and `overflowing`/`capacity` is constant at RESERVOIR_STORAGE_CAPACITY regardless of input. Assert `reservoirTouchesMapWater` returns false for an out-of-bounds-adjacent-only reservoir (e.g. at the map corner (0,0) with no land water).</action>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 and `npm run test` reports 316 tests green at the recorded baseline before any source change.
    - src/sim/water.ts exports `RESERVOIR_STORAGE_CAPACITY`, `ReservoirState`, `reservoirTouchesMapWater`, and `computeReservoirStates`; all existing water.ts exports are unchanged.
    - `computeReservoirStates` reports `filled`/`level`/`inletConnected`/`outletToAqueduct` per the five documented cases.
    - `npx vitest run tests/unit/reservoir.test.ts` exits 0 (the new file exists and its tests pass).
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/reservoir.test.ts</automated>
  </verify>
  <done>Baseline recorded (316 tests); ReservoirState + computeReservoirStates expose 3x3 reservoir storage (capacity/level), inlet (map water or flowing aqueduct) and outlet-to-aqueduct; reservoir tests + typecheck green.</done>
</task>

<task type="auto">
  <name>Well desirability penalty + tests (WATR-01)</name>
  <files>src/sim/water.ts, tests/unit/water.test.ts</files>
  <read_first>src/sim/water.ts (TileWater 25-32, WaterSystem.compute 46-91, source loop 63-77, waterClassAt 94-96), tests/unit/water.test.ts, 04-CONTEXT.md decision 1 (WATR-01), 04-RESEARCH.md section 1</read_first>
  <action>Read src/sim/water.ts and tests/unit/water.test.ts (including the reservoir additions from task 1) before editing. Add the WATR-01 well desirability penalty at the model/overlay layer: (1) export `WELL_DESIRABILITY_PENALTY = 4` as a module const; (2) add a `desirability: number` field to the `TileWater` interface (water.ts:25-32) and initialize it to 0 in the grid fill loop (water.ts:56-57, next to `wellness: 0`); (3) in `WaterSystem.compute()`, after the existing coverage loop, add a second pass over active sources of kind 'well' that, for every tile within Manhattan `dist(x, y, src.x, src.y) <= src.radius`, subtracts WELL_DESIRABILITY_PENALTY from `cell.desirability` (so overlapping wells accumulate, e.g. two wells covering a tile give -8). Do not change `wellness`, `sanitaryRisk`, or the water-class kind assignment.
  Extend tests/unit/water.test.ts with a 'wells desirability penalty (WATR-01)' describe block: a single active well at (2,2) with radius 2 on a 5x5 pollution-free map gives `grid[2][2].desirability === -WELL_DESIRABILITY_PENALTY` and `grid[1][1].desirability === -WELL_DESIRABILITY_PENALTY` (tile at distance 2), while `grid[0][0].desirability === 0` (distance 4, outside radius); two active wells at (2,2) and (2,3) both radius 2 give `grid[2][2].desirability === -2 * WELL_DESIRABILITY_PENALTY` (overlap); an inactive well gives 0 everywhere; and the water class of the well tile is still 'basic' (`waterClassAt(grid, 2, 2) === 'basic'`) with `sanitaryRisk` still reflecting pollution (reuse the pollution callback from the existing first test).</action>
  <acceptance_criteria>
    - `TileWater` has a required `desirability` field initialized to 0 and `WELL_DESIRABILITY_PENALTY` is exported from src/sim/water.ts.
    - Wells subtract the penalty per tile within radius, accumulating on overlap; off-radius tiles and inactive wells are zero; water class and sanitary risk semantics are unchanged.
    - Existing tests in tests/unit/water.test.ts still pass unchanged (7 tests) plus the new penalty block.
    - `npx vitest run tests/unit/water.test.ts` exits 0 and `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/water.test.ts</automated>
  </verify>
  <done>WELL_DESIRABILITY_PENALTY subtracted per well-covered tile (accumulating on overlap, zero off-radius/inactive); water class + sanitary risk unchanged; well tests + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| water model state → advisor data | TileWater/ReservoirState grids must be pure derivations of injected inputs — no hidden process/clock state leaks into coverage or reservoir level |
| inputs → AqueductSystem/computeReservoirStates | `flowing` and `hasMapWater` are caller-provided; reservoir filled/level must be consistent with those inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | reservoir level reported filled when not connected | medium | mitigate | `filled` requires active AND (map water OR flowing aqueduct neighbor); isolated/inactive cases asserted |
| T-04-02 | Tampering | well desirability penalty not applied / leaks off-radius | medium | mitigate | overlap accumulation and off-radius-zero cases asserted; waterClass/sanitaryRisk unchanged by regression surface |
</threat_model>

<verification>
Run `npm run test` (full suite, golden + determinism + property included) and `npm run typecheck`.
Spot-check: `npx vitest run tests/unit/reservoir.test.ts` and `npx vitest run tests/unit/water.test.ts` are green.
Per-task sampling follows `.planning/phases/04-water-system/04-VALIDATION.md`.
</verification>

<success_criteria>
- Reservoirs expose storage (capacity/level), filled state, inlet (map water / flowing aqueduct) and outlet-to-aqueduct (WATR-02).
- Wells apply a desirability penalty within radius while water class and sanitary risk stay correct (WATR-01).
- Full suite (316 tests) and typecheck remain green; baseline re-recorded at task 1.
</success_criteria>

<output>
Create `.planning/phases/04-water-system/04-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/sim/water.ts`: `WELL_DESIRABILITY_PENALTY` const; `TileWater.desirability` field; well-penalty pass in `WaterSystem.compute()`.
- `src/sim/water.ts`: `RESERVOIR_STORAGE_CAPACITY` const; `ReservoirState` interface; `reservoirTouchesMapWater()` and `computeReservoirStates()` helpers.
- New test file: `tests/unit/reservoir.test.ts`.
