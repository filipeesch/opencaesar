---
phase: 07-warehouses-logistics
plan: 07-03
type: feature
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/sim/advisors.ts
  - src/sim/runner.ts
  - tests/unit/logistics-advisor.test.ts
  - tests/determinism/warehouse-logistics-determinism.test.ts
autonomous: true
requirements: [WARE-03]
must_haves:
  truths:
    - "The logistics advisor view (stock, production, consumption, in-transit, bottlenecks, stopped buildings) is derived live from a running sim — never fabricated (§33-23): stock = warehouse stock + workshop held output, production = produced-last-tick × 30, consumption = catalog input draw of staffed workshops, in-transit = loads pending porter dispatch, bottlenecks = rows with a bottleneck, stopped = inactive staffed logistics/production buildings (WARE-03, decision 4)."
    - "A SimRunner.getLogisticsAdvisor() accessor exposes the live LogisticsAdvisorView without changing the SimState shape, so a click-through UI can read real numbers (WARE-03)."
    - "The warehouse/logistics chain is deterministic: same seed + same map + same commands → byte-identical getStateJson under chunk sizes 1/7/50, and ReservationPool expiry outcomes are identical for identical (now, window) inputs (decision 5)."
    - "All additions are additive: the existing hardcoded logisticsAdvisor unit test (logistics.test.ts:94-102), the per-row production advisor, and all 467 existing tests stay green."
  artifacts:
    - path: src/sim/advisors.ts
      provides: "logisticsAdvisorFromState(state, rows) — pure projection of LogisticsAdvisorView from a SimState + production advisor rows (stock/production/consumption/in-transit/bottlenecks/stopped)"
      min_lines: 30
    - path: src/sim/runner.ts
      provides: "getLogisticsAdvisor(): LogisticsAdvisorView accessor delegating to logisticsAdvisorFromState(this.getState(), this.getProductionAdvisorRows())"
      min_lines: 5
    - path: tests/unit/logistics-advisor.test.ts
      provides: "Exact-number unit assertions on logisticsAdvisorFromState against a hand-built state + live assertions on a real production city (stock/production/consumption/in-transit/bottlenecks/stopped)"
      min_lines: 25
    - path: tests/determinism/warehouse-logistics-determinism.test.ts
      provides: "Chunked-tick determinism (1/7/50 → identical getStateJson) for the warehouse/logistics chain through the runner, plus ReservationPool expiry determinism"
      min_lines: 25
  key_links:
    - from: src/sim/advisors.ts
      to: src/sim/logistics.ts
      via: "LogisticsAdvisorView type (logistics.ts:144-151) filled by logisticsAdvisorFromState instead of the hardcoded-arg compose (logistics.ts:153-163)"
    - from: src/sim/advisors.ts
      to: src/sim/advisors.ts
      via: "productionAdvisorRows (advisors.ts:547-597) supplies the per-building stock/output/bottleneck rows the projection aggregates"
    - from: src/sim/runner.ts
      to: src/sim/advisors.ts
      via: "getLogisticsAdvisor() calls logisticsAdvisorFromState + getProductionAdvisorRows (runner.ts:330-332)"
    - from: src/sim/logistics.ts
      to: src/sim/logistics.ts
      via: "ReservationPool expiry determinism (07-01) is re-asserted by the chunked logistics determinism test"
---

<objective>
Close the final two gaps RESEARCH found for WARE-03 and decision 5: (1) the
`LogisticsAdvisorView` aggregates (stock, production, consumption, in-transit,
bottlenecks, stopped buildings) exist only as a hardcoded-arg compose function
(logistics.ts:144-163) exercised by logistics.test.ts:94-102 — add a pure
`logisticsAdvisorFromState` projection in `src/sim/advisors.ts` and a
`SimRunner.getLogisticsAdvisor()` accessor (runner.ts) that derive every field
live from SimState + the production advisor rows, never fabricated (§33-23), and
cover them with exact-number unit tests plus live production-city assertions
(decision 4); (2) no determinism test covers the warehouse/logistics chain
through the runner and no test re-asserts the decision-5 ReservationPool expiry —
add a chunked-tick determinism test (chunk sizes 1/7/50 → identical
`getStateJson()`, plus pool-expiry identity) in
`tests/determinism/warehouse-logistics-determinism.test.ts`. Depends on 07-01
(order semantics + pool expiry) and 07-02 (road-reachable warehouse candidates).

Output: `.planning/phases/07-warehouses-logistics/07-03-PLAN.md` plus the tasks
below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-warehouses-logistics/07-CONTEXT.md
@.planning/phases/07-warehouses-logistics/07-RESEARCH.md
@.planning/phases/07-warehouses-logistics/07-VALIDATION.md
@.planning/phases/07-warehouses-logistics/07-01-PLAN.md
@.planning/phases/07-warehouses-logistics/07-02-PLAN.md

Existing implementation: `LogisticsAdvisorView` + `logisticsAdvisor` compose
(logistics.ts:144-163); hardcoded test at logistics.test.ts:94-102. Production
advisor rows are live-derived (`productionAdvisorRows` advisors.ts:547-597,
`productionAdvisorSummary` 610-626); `SimRunner.getProductionAdvisorRows()` /
`getProductionAdvisor()` (runner.ts:330-347). `EXTRACTION_BUILDING_TYPES`,
`WORKSHOP_BUILDING_TYPES`, `RAW_OLIVE_GRAPE`, `WORKSHOPS` imported into
advisors.ts (14-16). Determinism pattern: tests/determinism/production-chain-
determinism.test.ts (chunked 44-63). ReservationPool additive
reserveWithExpiry/expireReservations from 07-01. Baseline: 467 tests / 63 files
green, typecheck clean. Per-task sampling follows 07-VALIDATION.md.
</context>

<tasks>

<task type="auto">
  <name>Live logistics advisor: logisticsAdvisorFromState + getLogisticsAdvisor() (WARE-03, decision 4)</name>
  <files>src/sim/advisors.ts, src/sim/runner.ts, tests/unit/logistics-advisor.test.ts</files>
  <read_first>src/sim/advisors.ts (imports 13-16, productionAdvisorRows 547-597, ProductionAdvisorRow 515-527), src/sim/logistics.ts (LogisticsAdvisorView 144-151), src/sim/runner.ts (getProductionAdvisorRows 330-333, getProductionAdvisor 336-347, tickProduction 837-982), tests/unit/advisors.test.ts (production advisor block 307-405), tests/unit/logistics.test.ts (logistics advisor hardcoded 94-102), 07-CONTEXT.md decision 4, 07-RESEARCH.md Q5</read_first>
  <action>Read src/sim/advisors.ts, src/sim/logistics.ts and src/sim/runner.ts before editing. Add to src/sim/advisors.ts an exported pure function `logisticsAdvisorFromState(state: SimState, rows: ProductionAdvisorRow[]): LogisticsAdvisorView` (import LogisticsAdvisorView type from './logistics'). Derive every field from live inputs, never fabricated: (1) stock — start with workshop-held output from rows where kind === 'workshop' (stock[commodity] += row.output), then add every warehouse building's b.stock values (b.type === 'warehouse'); (2) production — per commodity, sum row.producedLastTick × 30 across extraction and workshop rows (the live monthly rate); (3) consumption — per commodity, count staffed workshops (b.active true and b.type in WORKSHOP_BUILDING_TYPES) whose WORKSHOPS[kind].inputs includes the commodity, × 30 (the catalog one-unit-per-tick input draw — the honest live proxy for monthly consumption; tickWorkshop consumes exactly one unit per input); (4) inTransit — sum row.output across workshop rows (loads held in workshop output awaiting porter dispatch); (5) bottlenecks — count of rows with a non-null bottleneck; (6) stopped — count of state.buildings that are logistics/production types (b.type === 'warehouse' or in WORKSHOP_BUILDING_TYPES or in EXTRACTION_BUILDING_TYPES or in RAW_OLIVE_GRAPE) with b.active === false. Return the assembled LogisticsAdvisorView. Keep all existing exports (advisors.ts and productionAdvisor...) unchanged.
  In src/sim/runner.ts add `getLogisticsAdvisor(): LogisticsAdvisorView` that returns `logisticsAdvisorFromState(this.getState(), this.getProductionAdvisorRows())`, importing `logisticsAdvisorFromState` from './advisors' and the `LogisticsAdvisorView` type from './logistics' (extend the existing './logistics' import at runner.ts:30). Do not change any SimState shape.
  Create tests/unit/logistics-advisor.test.ts with two describe blocks. Block 'pure projection (exact numbers)': hand-build a minimal SimState (one warehouse with stock { pottery: 3 }, one kitchen state building active:false, and rows = [{ kind: 'workshop', commodity: 'pottery', output: 2, producedLastTick: 0.3, bottleneck: null, ... }]) and assert logisticsAdvisorFromState returns exactly stock.pottery 5, production.pottery 9 (0.3×30), inTransit 2, bottlenecks 0, and stopped equal to the count of inactive logistics/production buildings in the state (build the state so that count is a known small number). Block 'live accessor (WARE-03)': build a real runner on productionChainMap via buildProductionCity and tick 400; read r.getLogisticsAdvisor(); (1) assert stock.pottery > 0 and equals the warehouse b.stock.pottery plus the workshop row output (reconcile against r.getProductionAdvisor() rows and the buildingById registry like advisors.test.ts:309-311); (2) assert production has a clay key and a pottery key, each >= 0 and pottery production reflects the live producedLastTick; (3) assert consumption.clay is 30 when the pottery workshop is active and stocked with clay (mirroring buildProductionCity staffing) or 0 when it is not; (4) assert inTransit >= 0 and bottlenecks >= 0; (5) starve the workshop (set workshop.production.inputs.clay = 0 via the internals registry as in advisors.test.ts:354-358) and one tick later assert bottlenecks rises and the view still reflects live state; (6) assert stopped matches the count of inactive warehouse/workshop/extraction buildings in the runner (expected 0 in the fully staffed buildProductionCity run, or the exact count otherwise). Keep the existing pure-compose test (logistics.test.ts:94-102) and all advisors.test.ts production-advisor tests green.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/logistics-advisor.test.ts && npx vitest run tests/unit/advisors.test.ts && npx vitest run tests/unit/logistics.test.ts</automated>
  </verify>
  <done>logistics-advisor.test.ts passes: the pure projection returns exact stock/production/consumption/in-transit/bottlenecks/stopped on a hand-built state, and the live runner accessor reconciles stock.pottery to warehouse+workshop rows, reports production from producedLastTick, consumption from staffed workshop input draw, raises bottlenecks when the workshop is starved, and reports stopped from inactive logistics/production buildings; logistics/advisor suites + typecheck green.</done>
</task>

<task type="auto">
  <name>Warehouse/logistics chunked determinism + reservation-pool expiry identity (decision 5)</name>
  <files>tests/determinism/warehouse-logistics-determinism.test.ts</files>
  <read_first>tests/determinism/production-chain-determinism.test.ts (chunked pattern 44-63, same-seed identity 40-42, different-seed 65-75), tests/helpers.ts (productionChainMap 64-77, buildProductionCity 86-111), src/sim/logistics.ts (ReservationPool 56-81 + reserveWithExpiry/expireReservations from 07-01), 07-CONTEXT.md decision 5, 07-RESEARCH.md Q7</read_first>
  <action>Read the existing production-chain determinism test (tests/determinism/production-chain-determinism.test.ts) and src/sim/logistics.ts (the 07-01 ReservationPool additive expiry) before writing. This is a test-only acceptance gap-fill: the warehouse/logistics chain has no chunked determinism coverage, and decision 5 requires the reservation-pool expiry to be proven deterministic. Create tests/determinism/warehouse-logistics-determinism.test.ts importing SimRunner, productionChainMap, buildProductionCity from '../helpers', and ReservationPool / reserveWithExpiry / expireReservations from '../../src/sim/logistics'. Implement a `warehouseRunJson(seed, ticks)` that builds a production city (buildProductionCity) on productionChainMap, sets policy (0, 0.5), ticks, and returns getStateJson() — mirroring production-chain-determinism.test.ts:13-19. Assert: (1) same-seed identity — warehouseRunJson(1234, 600) equals itself; (2) chunk batching order-independence — run chunk sizes 1/7/50 to 600 ticks and assert getStateJson() is byte-identical across the three; (3) different seeds run and serialize without crashing at tick 600 (buildings array non-empty, tick 600) — mirrors 65-75; (4) ReservationPool expiry determinism — construct two pools with identical reserved state via reserveWithExpiry('pottery', 1, 10, 30), call expireReservations(40) on both, and assert both report the same consumed/available outcome and the same expired count; also assert that the same (now, expiresIn) inputs on two pools produce identical availability at three sampled ticks (30, 40, 50); (5) reserving with the plain `reserve` API (no expiry) is not touched by expireReservations.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/determinism/warehouse-logistics-determinism.test.ts && npm run test</automated>
  </verify>
  <done>warehouse-logistics-determinism.test.ts passes: same seed reproduces byte-identical getStateJson under chunk sizes 1/7/50 for the warehouse chain through the runner, different seeds run cleanly, and ReservationPool expiry is identical across pools for identical (now, window) inputs (sampled at 30/40/50) while plain reserve() is untouched; all previously-existing 467 tests remain green plus the new tests (record the measured delta); typecheck clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Advisor numbers → UI | The logistics advisor must report only values traceable to live sim state (warehouse/workshop stock, catalog input draw, produced-last-tick, inactive-building count); no number may be invented (§33-23) |
| Runner accessor → SimState | getLogisticsAdvisor must read live state without mutating or restructuring SimState (no golden/determinism churn) |
| Determinism of logistics chain | The production→warehouse chain and reservation-pool expiry must be driven only by the seeded RNG / injected tick, never the wall clock or Math.random |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Tampering | fabricated advisor numbers | high | mitigate | logisticsAdvisorFromState derives every LogisticsAdvisorView field from SimState + rows; exact-number + live reconciliation tests in logistics-advisor.test.ts |
| T-07-08 | Tampering | non-deterministic warehouse chain (clock/RNG) | high | mitigate | chunked-tick 1/7/50 identity test over the production→warehouse chain; ReservationPool expiry identity across pools |
| T-07-05 | Privacy | none (pure model) | low | accept | no secret/user data in the advisor projection |
</threat_model>

<verification>
Run `npm run test` (full suite, golden + determinism + property included) and
`npm run typecheck`. Spot-check: `npx vitest run tests/unit/logistics-advisor.test.ts`
and `npx vitest run tests/determinism/warehouse-logistics-determinism.test.ts`
are green; `tests/unit/advisors.test.ts`, `tests/unit/logistics.test.ts` and
`tests/determinism/production-chain-determinism.test.ts` unchanged and green.
Per-task sampling follows `.planning/phases/07-warehouses-logistics/07-VALIDATION.md`.
</verification>

<success_criteria>
- SimRunner.getLogisticsAdvisor() returns a LogisticsAdvisorView with every aggregate (stock, production, consumption, in-transit, bottlenecks, stopped) live-derived from sim state (WARE-03, decision 4).
- Pure logisticsAdvisorFromState proven with exact numbers; live accessor reconciles to real production-city state (decision 4).
- Warehouse/logistics chain chunked determinism (1/7/50 → identical state) and ReservationPool expiry identity locked (decision 5).
- Full suite (467 tests), goldens, and determinism remain green; no existing export renamed or resemantized.
</success_criteria>

<output>
Create `.planning/phases/07-warehouses-logistics/07-03-SUMMARY.md` when done.
</output>
