---
phase: 19.1-population-labor
plan: 19.1-plan
type: execute
wave: 1
depends_on: [19-PLAN]
files_modified:
  - src/sim/walkers.ts
  - src/sim/population.ts
  - src/sim/labor.ts
  - src/sim/runner.ts
  - src/sim/types.ts
  - src/sim/saveCodec.ts
  - src/sim/advisors.ts
  - src/game/advisors.ts
  - src/game/scenes/HUDScene.ts
  - tests/unit/labor-sectors.test.ts
  - tests/determinism/population-determinism.test.ts
  - tests/unit/population.test.ts
  - tests/unit/advisors.test.ts
autonomous: true
requirements: [POP-01, POP-02, POP-03, POP-04]

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 7
  confidence: low

must_haves:
  truths:
    - "POP-01: every residence has a live per-resident population — class (plebeian/patrician from tierOfLevel), age bands with children/elderly, employment, taxable income, service access, sentiment, crime/health risk context — stored as internal-only HouseInstance state (never serialized into BuildingState/SimState), derived deterministically from level/effectivePopulation/tick history, and consistent with the invariant residents.length === effectivePopulation(b); it reproduces byte-identical after save/load replay (fromSaveData re-ticks) with NO SaveData growth and NO golden change."
    - "POP-01: the residence inspector surfaces the real per-residence class/age/employment/sentiment via the read-only getInspector→residenceInspection seam (append fields only when defined — original minimal calls keep the old shape), and HUDScene.renderBuildingRows stops fabricating ('plebeian', full occupancy) and feeds real internals-derived values, while getStateJson() stays byte-identical."
    - "POP-02: migration is modeled — netMigration(population.ts) runs on the month cadence (tickCount % 40, right after tickHousingMerge) with live attractiveness inputs (desirability, civicCoverage health/literacy/entertainment, 1−unemploymentRate, vacancy), applying internal-only occupancy deltas bounded by capacityAvailable (0 when the city is full ⇒ zero golden delta); famine foodShortageEffects().emigration creates vacancy → next migration months refill it; homelessness is counted; NO immigration/emigration walker spawning; workerPool/tax/populationOf stay capacity-based (Pitfall 3 — occupancy is a derived layer, never a balance effect this phase)."
    - "POP-03: labor allocates scarce workers by sector priority 1-5 via allocateWorkers over workerPool — replacing the greedy placement-order tickLabor — preserving every labor.test.ts invariant (assigned ≤ pool, assigned ≤ required, surplus pool fills all jobs, staffed windows don't oscillate); pin = guaranteed minimum (pinned sectors have their prior assigned reserved before the general pool split — the weaker pure-function pinned branch is NOT relied upon, Pitfall 2); pause ⇒ sector needed=0; restore-auto reverts to default; sector priorities/tunings are module-local constant map (balance-parity)."
    - "POP-03: pin/pause/restore-auto is a replayable SaveCommand setLaborSectorState {kind, sector, pinned?, paused?} — union member in types.ts, exhaustive applyCommand branch (never-guard), setLaborSectorState handler storing into a private per-sector map (marketConfigs precedent) with push-on-accept + paused-enqueue, and a validateCommand case in saveCodec.ts rejecting unknown sector / non-string sector / non-boolean pinned/paused BEFORE replay — a malformed payload is {ok:false,error}, never a raw throw."
    - "POP-04: the urban wage policy reports wageBand vs a module-local imperial wage reference (inside (0,1) — setPolicy clamps wageRate to 0..1) and an unemploymentBand — both pure total functions projected into DerivedSnapshot (golden-safe: getStateJson serializes getState, not getDerived) and surfaced as 'Wage vs Imperial' + 'Unemployment Band' rows in the labor advisor panel reading source.getDerived(); empty-city (population === 0) composes without throwing; ADVISOR_TAB_ORDER unchanged."
    - "Determinism & golden integrity: no Math.random/Date.now/new Date in any new sim path (only getSaveData's non-state savedAt metadata), all new mutation confined to tick() %-40 hooks and the setLaborSectorState handler (getters read-only), toBuildingState/toWalkerState/getStateJson untouched, and the phase closes with the full suite + typecheck + check:military green together and tests/golden byte-unchanged."
  artifacts:
    - path: src/sim/walkers.ts
      provides: "MODIFY — add internal-only fields to HouseInstance (:88-122) after the civic/combinedPopulation precedent: residents?: Resident[] and nextResidentId?: number (import the Resident type into src/sim/walkers.ts as a type-only import — population.ts has no imports so no runtime cycle), with the exact comment discipline 'Internal only — never serialized to BuildingState, so goldens/SimState stay byte-identical'. toBuildingState must NOT copy them."
      min_lines: 14
    - path: src/sim/population.ts
      provides: "EXTEND (never rewrite; keep the 5 pure exports + Residence verbatim) — add pure deterministic derive/age helpers: residentsForHouse(house, seeded) → Resident[] (count === effectivePopulation; class from tierOfLevel(level) — patrician share at tier ≥ 3; ages via a seeded mulberry32 RNG salt — rng.ts: NEVER Math.random; children 0-15, workforce 16-60, elderly 61+; employment started false) and an ageOnMonth(residents) / workforce() helper using the isEligible vocabulary (#priority), plus module-local unemploymentBand(rate) → {label, rate} tiers and IMPERIAL_WAGE_REFERENCE (inside (0,1)); tuning stays module-local (balance-parity)."
      min_lines: 60
    - path: src/sim/labor.ts
      provides: "NEW pure module (housingLive.ts analog, no RNG/wall-clock) — export const LABOR_SECTOR_PRIORITY: Record<string, SectorPriority> = { food:1, water:2, utility:3, commerce:4, culture:5 } [ASSUMED A3] + export const SECTOR_IDS (the known sector set the command validates against), buildLaborSectors(buildings) → LaborSector[] grouping labor-connected buildings by BUILDINGS[b.type].category (needed = Σ workersRequired) with paused sectors reporting needed=0, and applySectorAssignments(buildings, sectors) redistributing each sector's assigned to its buildings within unchanged per-building caps."
      min_lines: 70
    - path: src/sim/runner.ts
      provides: "MODIFY — (1) tickResidency() at tickCount % 40 right after tickHousingMerge: init/sync b.house.residents consistent with effectivePopulation, monthly age + new-resident booking, cleared/re-derived on level change; (2) tickPopulationMigration() at the same cadence applying netMigration + famine vacancy/refill + homeless count; (3) DerivedSnapshot interface (:136-163) + derivedSnapshot() (:1327-1343) gain residentCount, residentsByClass?, immigration?/emigration?/homeless?, wageBand, unemploymentBand (total functions); (4) tickLabor rewrite (:2729-2744) to sector allocateWorkers with pinned reserve semantics + paused needed=0; (5) getLaborSectors() read-only getter; (6) setLaborSectorState handler + applyCommand branch (:3255-3292). All mutation ONLY in tick()%40 hooks or the command handler; getters read."
      min_lines: 120
    - path: src/sim/types.ts
      provides: "MODIFY — append to the SaveCommand union (:98-99) | { kind: 'setLaborSectorState'; sector: string; pinned?: boolean; paused?: boolean } with a comment explaining replay semantics (pin/pause reconstruct from the replayed stream; no SaveData schema field)."
      min_lines: 6
    - path: src/sim/saveCodec.ts
      provides: "MODIFY — add the validateCommand (:90-147) case 'setLaborSectorState': sector must be a string, pinned/paused when present must be booleans (else 'malformed-command'); unknown kind already falls through to 'unknown-command-kind' so a hostile payload is rejected by validateSave before fromSaveData replays."
      min_lines: 8
    - path: src/sim/advisors.ts
      provides: "MODIFY — residenceInspection (:283-310) appends the per-residence projection when internals.house.residents is defined (aggregated { count, classBreakdown, ageBands, employed } or a shallow-cloned residents array) in the same additive guard style as civic/foodInventory."
      min_lines: 6
    - path: src/game/advisors.ts
      provides: "MODIFY — labor panel (:188-202) adds { label: 'Wage vs Imperial', value: '<below|at|above> (NN%)' } and { label: 'Unemployment Band', value: derived.unemploymentBand.label } guarded for undefined (empty-city total function, Pitfall 5); ADVISOR_TAB_ORDER (:71-85) unchanged."
      min_lines: 8
    - path: src/game/scenes/HUDScene.ts
      provides: "MODIFY — renderBuildingRows house branch (:897-937): replace the fabricated residenceInspection call ('plebeian', full occupancy) with internals-derived real class/occupancy, and add Class / Age Bands / Employed appendRow gated on typeof insp.residents === 'object' (mirrors the foodInventory guard)."
      min_lines: 12
    - path: tests/unit/labor-sectors.test.ts
      provides: "NEW (Wave 0) — labor.test.ts buildTown harness; pure allocateWorkers priority ordering; scarcity allocation across sectors via runner getLaborSectors(); pin = guaranteed minimum under a shrunken pool; pause ⇒ needed=0; restore-auto; surplus-pool totals; setLaborSectorState SaveCommand round-trip (live → getSaveData → fromSaveData → loaded.getLaborSectors() equals original); security cases (unknown sector → {ok:false})."
      min_lines: 110
    - path: tests/determinism/population-determinism.test.ts
      provides: "NEW (Wave 0) — determinism.test.ts:29-42 round-trip blueprint: seed town, tick 500 (spans %40), getStateJson → migrateSave → validateSave → fromSaveData → byte-identical getStateJson; assert residency getters (getInspector residents, getDerived().residentCount) survive; famine-vacancy migration refill case byte-identical across load; chunked 1/7/50 (civic-determinism.test.ts:46-56) + no-wall-clock source audit (:114-123)."
      min_lines: 90
    - path: tests/unit/population.test.ts
      provides: "EXTEND (Wave 0) — keep the 5 existing describes green; add residency consistency (residentsForHouse count === effectivePopulation), migration wiring against live inputs/vacancy/famine, class/age/employment projection through residenceInspection internals, and wageBand/imperialReference cases."
      min_lines: 40
    - path: tests/unit/advisors.test.ts
      provides: "EXTEND (Wave 0) — labor panel contains 'Wage vs Imperial' + 'Unemployment Band' rows whose values trace to getDerived(); empty-city (population === 0) compose-without-throw case (Pitfall 5)."
      min_lines: 30
  key_links:
    - "tickResidency/tickPopulationMigration ↔ tickHousingMerge %40 cadence: the residency+migration mutations MUST run inside tick() at tickCount % 40 right after tickHousingMerge so replay re-derives them byte-identically and they see the tick's final house levels before walkers move — any mutation outside tick()/command handlers diverges on load (Pitfall 4); verified by population-determinism.test.ts byte-identity + chunked replay → 19.1-01-01/19.1-02-01."
    - "residents.length === effectivePopulation ↔ level/merge changes (housingLive.ts:103-109): the invariant holds every tick — level change/merge re-derives the cohort; the count feeds DerivedSnapshot.residentCount; migration fills vacancy = effectivePopulation − residents.length (0 when full ⇒ goldens untouched, Pitfall 3); consumer reads route through effectivePopulation, never a bare HOUSING_LIVE_STATS index."
    - "tickLabor ↔ labor.ts buildLaborSectors ↔ allocateWorkers ↔ workerPool (economy.ts): the greedy placement-order loop (runner.ts:2729-2744) is replaced so priority 1-5 ordering under scarcity is behavioral, but the pool accounting (workerPool), per-building caps, setActive(b, give>=want), and the labor.test.ts invariants (assigned ≤ pool / ≤ required / surplus fills all) must all survive the rewrite; run the full suite + winnability probe after the rework (A3) → 19.1-03-01."
    - "setLaborSectorState ↔ types.ts union ↔ applyCommand (runner.ts:3255-3292 never-guard) ↔ validateCommand (saveCodec.ts:90-147) ↔ marketConfigs precedent (private map + push-on-accept openTradeRoute runner.ts:968-973 + paused-enqueue setPolicy runner.ts:1893-1906): the complete command surface must land together — a new union member without the applyCommand branch fails typecheck, and without the saveCodec case a malformed save slips past replay into the raw 'unknown command kind' throw → 19.1-03-02 (T-19.1-01)."
    - "wageBand/unemploymentBand ↔ DerivedSnapshot (runner.ts:136-163, 1327-1343) ↔ game/advisors.ts labor panel (:188-202) via getDerived(): the report is a pure projection of policy + unemploymentRate — advisors consume getDerived() and never fabricate strings (composer rule game/advisors.ts:99-101); total functions over population===0 and imperialReference||1 → 19.1-04-01 (Pitfall 5)."
    - "getInspector (runner.ts:2597-2625) → residenceInspection (sim/advisors.ts:283-310) → HUDScene.renderBuildingRows (:897-937): the inspector seam grows only via internals (append-when-defined), never BuildingState/WalkerState; HUDScene replaces the fabricated ('plebeian', full) call with real internals values → 19.1-01-01 (UI-04)."
    - "Golden-byte contract: getStateJson = JSON.stringify(getState()) (runner.ts:2554-2556) and toBuildingState (:3187-3228) must remain byte-identical — new per-residence/migration/wage state lives on HouseInstance internals and DerivedSnapshot ONLY; goldens (tests/golden) untouched; verified per-wave → 19.1-05-01 close gate."
---
<objective>
Deliver Phase 19.1 — wire the ORPHANED pure module src/sim/population.ts into the live sim so the v1.0 milestone's last unwired requirements (POP-01..04, flagged by the milestone audit) become real, observable behavior: per-residence population (class/age/employment/children/elderly/tax/service/sentiment/crime-health context) layered over the level-based population, month-cadence migration (attractiveness, vacancy-immigration/emigration, homelessness), labor sectors with priority 1-5 allocation + pin/pause/restore-auto, and an urban wage policy with imperial-reference comparison + unemployment band reporting.

Purpose: population.ts today is dead code — imported only by tests/unit/population.test.ts — while the live sim models population EXCLUSIVELY through HOUSING_LIVE_STATS[level].population and effectivePopulation (no resident identity, no migration, no vacancy), labor is a greedy placement-order loop (runner.ts:2729-2744) with no sectors/priorities/pinning, and wage policy is a bare policy.wageRate 0..1 scalar with no band report. This phase wires every one of those capabilities in WITHOUT breaking the deterministic byte-identity contract: all new state is internal-only (HouseInstance fields like combinedPopulation/civic — never serialized), reported through the non-serialized DerivedSnapshot (getStateJson serializes getState, not getDerived), surfaced through the read-only getInspector/residenceInspection seam, and player actions become replayable SaveCommands. NO SaveData schema growth, NO golden regeneration, NO new external packages.
Output: internal per-house residents state (walkers.ts + population.ts derive helpers), tickResidency/tickPopulationMigration %40 hooks in runner.ts, labor.ts sector module + tickLabor rewrite + getLaborSectors(), the setLaborSectorState SaveCommand across types.ts/runner.ts/saveCodec.ts, DerivedSnapshot resident/migration/wage/unemployment fields, residenceInspection + HUDScene real residency rows, wage-band labor-advisor rows, NEW tests/unit/labor-sectors.test.ts + tests/determinism/population-determinism.test.ts, EXTENDED tests/unit/population.test.ts + tests/unit/advisors.test.ts, and the close gate (full suite + typecheck + check:military green, goldens byte-unchanged).
</objective>

<execution_context>
@/Users/filipe.esch/projects/pessoal/opencaesar/.opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/projects/pessoal/opencaesar/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-CONTEXT.md
@.planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-RESEARCH.md
@.planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-PATTERNS.md
@.planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-VALIDATION.md

# POP-01 seams (read before Wave 1):
@src/sim/population.ts (orphaned deliverable — 5 pure exports preserved verbatim)
@src/sim/walkers.ts (HouseInstance internals — combinedPopulation/civic precedent, :87-132)
@src/sim/housingLive.ts (effectivePopulation :103-109, tierOfLevel :82-86 — residency source of truth)
@src/sim/rng.ts (mulberry32/randInt — the deterministic RNG for resident derivation)
@src/sim/advisors.ts (residenceInspection additive-enrichment, :283-310)

# POP-02 seams (read before Wave 2):
@src/sim/housing.ts (foodShortageEffects().emigration :388-412 — the only existing vacancy source)

# POP-03 seams (read before Wave 3):
@src/sim/economy.ts (workerPool/assignedWorkers/totalJobs)
@src/sim/saveCodec.ts (validateCommand switch :90-147 — new case target)
@src/sim/types.ts (SaveCommand union :74-99)

# POP-04 seams (read before Wave 4):
@src/game/advisors.ts (labor panel :188-202, composer rule :99-101, ADVISOR_TAB_ORDER :71-85)

# Tests (read before editing the matching wave scaffolds):
@tests/unit/labor.test.ts (buildTown harness :12-44 + invariants :125-140)
@tests/unit/population.test.ts (5 existing describes preserved)
@tests/unit/advisors.test.ts (inspectors-enriched + labor/empty-city describes)
@tests/determinism/determinism.test.ts (round-trip :29-42)
@tests/determinism/civic-determinism.test.ts (chunked 1/7/50 + no-wall-clock audit :114-123)
</context>

# Execution order (waves are sequential within this single plan — shared files force this order: EVERY wave touches src/sim/runner.ts, and run was deliberately constructed as one multi-wave PLAN.md following the 18/19-PLAN.md precedent):

- **Wave 0** — validation test scaffolds: NEW tests/unit/labor-sectors.test.ts (POP-03), NEW tests/determinism/population-determinism.test.ts (POP-01/02), EXTEND tests/unit/population.test.ts (POP-01/02/04 residency+migration+wage), EXTEND tests/unit/advisors.test.ts (POP-04 wage/unemployment rows + empty-city). The new/extended cases are RED until their implementing waves land (write them against the TARGET API surface: residentsForHouse, HouseInstance residents, DerivedSnapshot.residentCount/wageBand/unemploymentBand, getLaborSectors, setLaborSectorState, labor.ts exports).
- **Wave 1 (POP-01) — tracer** 19.1-01-01: the thinnest end-to-end per-residence slice — HouseInstance residents internal state (walkers.ts) + residentsForHouse pure derivation (population.ts) + tickResidency %40 reconcile in runner.ts + DerivedSnapshot.residentCount/residentsByClass + residenceInspection append (sim/advisors.ts) + HUDScene real residency rows replacing fabricated 'plebeian'. Flips the Wave-0 residency unit + determinism cases green; goldens untouched.
- **Wave 2 (POP-02)** 19.1-02-01: tickPopulationMigration %40 — attractiveness from live inputs, netMigration as internal occupancy deltas bounded by vacancy (0 when full ⇒ golden-neutral), famine emigration → vacancy → refill, homeless count, DerivedSnapshot.immigration/emigration/homeless. Flips the migration unit + determinism cases green.
- **Wave 3 (POP-03)** 19.1-03-01: src/sim/labor.ts (LABOR_SECTOR_PRIORITY/SECTOR_IDS/buildLaborSectors/applySectorAssignments) + tickLabor rewrite to allocateWorkers with pinned reserve semantics + getLaborSectors(); then 19.1-03-02: the setLaborSectorState SaveCommand (types.ts union + runner handler + applyCommand branch + saveCodec validateCommand case). Flips labor-sectors.test.ts green; labor.test.ts invariants + full suite + winnability probe re-verified (A3).
- **Wave 4 (POP-04)** 19.1-04-01: module-local IMPERIAL_WAGE_REFERENCE + unemploymentBand in population.ts, wageBand + unemploymentBand entered into DerivedSnapshot, 'Wage vs Imperial' + 'Unemployment Band' rows in the labor advisor panel. Flips advisors.test.ts + population.test.ts wage cases green.
- **Wave 5 (close)** 19.1-05-01: full suite + typecheck + check:military green together, tests/golden byte-unchanged (git status --porcelain tests/golden empty), determinism replay for residents+migration+sectors proven — the phase gate before /gsd-verify-work.

# Typecheck sequencing note: the Wave-0 scaffolds reference APIs that land later (residentsForHouse → 19.1-01-01; DerivedSnapshot.wageBand/unemploymentBand → 19.1-04-01; getLaborSectors/setLaborSectorState/labor.ts → 19.1-03-01/02). Consequently `npm run typecheck` is RED at Waves 0-1 by design. Executor MUST handle this: for Wave 0 keep the gate to file-existence + targeted vitest runs on the compiled-on-the-fly scaffolds (add @ts-expect-error / type-only stubs only if a scaffold cannot parse), per-wave gates FLIP typecheck green once their imported modules exist (Wave 2 after 19.1-01-01, Wave 4 after 19.1-03-01/02, and the FINAL Wave-5 gate is authoritative — full typecheck green).

# Locked decisions honored (19.1-CONTEXT.md + 19.1-RESEARCH.md + 19.1-PLAN-DISPATCH.md):
- POP-01: Residence/Resident (class/age/employment/children/elderly/tax/inventories/service access/sentiment/crime-health) becomes the live per-house population model, DERIVED from and CONSISTENT with level-based HOUSING_LIVE_STATS population + effectivePopulation; deterministic from tick history (no wall-clock), save/load replay byte-identical, NO new SaveData fields; surfaced via the read-only getInspector seam — no serialized-state growth.
- POP-02: migration attractiveness from live derived state (desirability, services, unemployment, ratings) with immigration/emigration + homelessness; netMigration wired on %40 month cadence; effects are derived deltas NEVER in getState(); zero golden delta (houses always-full); famine emigration (housing.ts) is the only vacancy source; NO walker spawning this phase (would enter SimState.walkers and break goldens).
- POP-03: allocateWorkers + LaborSector from population.ts wired into the existing workerPool/labor system; priority-1..5 allocation; pin/pause/restore-auto; deterministic allocation; pinned sectors persist via a replayable setLaborSectorState SaveCommand (union + exhaustive applyCommand + validateCommand); pin is implemented as a WIRING-LAYER guaranteed minimum (the pure allocateWorkers pinned branch does NOT reserve pool under scarcity — Pitfall 2); labor.test.ts invariants preserved; sector priority map module-local (balance-parity — never BALANCE/CONFIG keys).
- POP-04: wageBand reused verbatim + module-local imperial reference (inside (0,1) — setPolicy clamps wageRate to 0..1) + unemploymentBand projected into DerivedSnapshot + labor advisor panel (Open Question 4 recommendation — finance panel left unchanged to avoid advisor-test churn); pure projection, never stored.
- Security (ASVS V5, security_enforcement enabled): setLaborSectorState payload validated in the handler (unknown sector → {ok:false,error}, mirroring openTradeRoute unknown-city) AND in validateCommand (saveCodec) before fromSaveData replays; exhaustive dispatch throws on unknown kind as the last-resort net; all UI strings via textContent (never innerHTML).
- the agent's Discretion (resolved concretely in each task): resident age-band/children/elderly derivation formula (mulberry32 salt of house id + level + tick), migration attractiveness input blend + weights [ASSUMED A5], sector default priority map [ASSUMED A3], imperial wage reference + unemployment band thresholds [ASSUMED A1/A2], wage-band rows placement (labor panel).

# Multi-source coverage audit (all COVERED):
- GOAL (wire the orphaned population module: per-residence, migration, labor sectors, wage policy — milestone audit POP-01..04) → Wave 1 (per-residence), Wave 2 (migration), Wave 3 (labor sectors), Wave 4 (wage policy), Wave 5 (phase gate proof).
- REQ POP-01 → 19.1-01-01 (HouseInstance residents + residentsForHouse + tickResidency reconcile + DerivedSnapshot.residentCount + inspector/HUD surface). REQ POP-02 → 19.1-02-01 (tickPopulationMigration %40 + attractiveness + vacancy/refill + homeless + DerivedSnapshot deltas). REQ POP-03 → 19.1-03-01 (labor.ts + tickLabor allocateWorkers + reserve semantics + getLaborSectors) + 19.1-03-02 (setLaborSectorState SaveCommand + validateCommand). REQ POP-04 → 19.1-04-01 (wageBand/unemploymentBand DerivedSnapshot + labor advisor rows). Every POP-01..04 ID appears in this plan's frontmatter `requirements`.
- RESEARCH: orphaned population.ts → all waves; golden byte-identity (Pitfall 1) → 19.1-01-01/05; allocateWorkers pinned semantics (Pitfall 2) → 19.1-03-01 reserve semantics; occupancy/economy coupling (Pitfall 3) → 19.1-02-01 capacity-based behavior; replayable-mutation-only (Pitfall 4) → 19.1-01-01/02-01 %40-in-tick; empty-city advisor totals (Pitfall 5) → 19.1-04-01; no-wall-clock (determinism) → 19.1-01-01 residentsForHouse + 19.1-05-01 audit; balance-parity module-local → 19.1-03-01/04-01; A6 (goldens untouched ⇒ no workerPool/tax/populationOf occupancy effect) honored by 19.1-02-01 and enforced by 19.1-05-01.
- CONTEXT: every locked decision above has a task (traced in task actions); discretion areas resolved concretely per task.
- Exclusions checked: no deferred ideas (CONTEXT section explicitly empty); items scoped to other phases (full §10 immigration/emigration walkers — explicitly deferred by A7/anti-pattern; Phase 20 UI redesign) are out of scope here.

<tasks>

<!-- ===================== WAVE 0 — validation test scaffolds ===================== -->

<task type="auto">
  <name>Task 19.1-00-01: Wave 0 — create/extend validation test scaffolds (labor-sectors unit, population-determinism, population residency/migration/wage, advisors wage rows)</name>
  <files>tests/unit/labor-sectors.test.ts, tests/determinism/population-determinism.test.ts, tests/unit/population.test.ts, tests/unit/advisors.test.ts</files>
  <read_first>
    - tests/unit/labor.test.ts:12-44 (buildTown harness + `r['buildings']`/`r['tickLabor']()` private access the new labor-sectors test must reuse), :46-56 (workerPool pure), :58-69 (post-connection assignment), :125-140 (the assigned ≤ pool / assigned ≤ required invariants the allocator MUST preserve)
    - tests/unit/population.test.ts (49 lines — the 5 existing describes to keep green: residence, eligibility, netMigration, allocateWorkers ordering, wageBand)
    - tests/determinism/determinism.test.ts:29-42 (the seed round-trip blueprint: new SimRunner(777), place roads+house, setPolicy, tick 500, getStateJson → migrateSave → validateSave → fromSaveData → byte-identical)
    - tests/determinism/civic-determinism.test.ts:46-80 (chunked 1/7/50 byte-identity) and :114-123 (no-Math.random/Date.now/new-Date source audit of src/sim files)
    - tests/unit/advisors.test.ts:154-227 (the inspectors-enriched describe + the empty-city/food-HUD composer describes to mirror for the wage-band rows)
    - 19.1-RESEARCH.md Validation Architecture (Wave 0 Gaps + Phase Requirements → Test Map) and 19.1-VALIDATION.md (Per-Task Verification Map + the full-suite command)
  </read_first>
  <action>
    Create the validation scaffolds as RED tests pinned to the Phase-19.1 target APIs (they fail until Waves 1-4 implement the features — expected, and how the Nyquist gate tracks them). Write against the TARGET surface, not today's surface.

    1. NEW tests/unit/labor-sectors.test.ts (REQ POP-03). Target imports: { buildLaborSectors, LABOR_SECTOR_PRIORITY, SECTOR_IDS, applySectorAssignments } from '../../src/sim/labor'; { allocateWorkers, type LaborSector } from '../../src/sim/population'; { SimRunner } from '../../src/sim/runner'; { SaveData } from '../../src/sim/types'; { validateSave, migrateSave } from '../../src/sim/saveCodec'. Reuse the labor.test.ts buildTown harness (18x16 town: farm/granary/market/well + N houses). Cases:
      (a) pure-sector ordering — allocateWorkers assigns scarce workers high-priority-first (copy population.test.ts:31-41 style with `priority: n as const`);
      (b) runner getLaborSectors() — after ticks, sector rows {id, priority, needed, assigned, pinned, paused} exist for the food sector and assignedWorkers ≤ totalWorkers;
      (c) scarcity allocation across sectors — with a shrunken pool the food sector (priority 1) stays fully staffed before commerce/culture;
      (d) pin = guaranteed minimum — pin the commerce sector under a shrunken/emptied pool and assert it keeps its prior assigned while unpinned sectors starve (Pitfall 2 reserve semantics — this is the NEW runner-side behavior, NOT the weak pure-function pinned branch);
      (e) pause ⇒ sector needed=0 — paused sector's buildings report 0 assigned and their workers spill to other sectors;
      (f) restore-auto — unpause reverts to default priority allocation;
      (g) surplus-pool totals — with a healthy pool every job is filled (labor.test.ts:93-122 style: assignedWorkers === jobDemand);
      (h) SaveCommand round-trip — issue setLaborSectorState('food', {pinned:true}) live, getSaveData(), then SimRunner.fromSaveData(migrateSave+validateSave'd data) and assert loaded.getLaborSectors() equals the original (deep-equal pinned/paused) — determinism.test.ts:48-49 style;
      (i) security — setLaborSectorState('bogus-sector', {}) returns {ok:false,error:/unknown/} and validateSave rejects a save whose command is {kind:'setLaborSectorState', sector: 42} or {sector:'food', pinned:'yes'} as 'malformed-command' (never a raw throw).
    2. NEW tests/determinism/population-determinism.test.ts (REQ POP-01/POP-02). Copy the determinism.test.ts:31-42 round-trip blueprint; build a town with roads + a house + setPolicy(0.1,0.2), tick 500 (spans %40 month boundaries so residency+migration hooks run). Cases:
      (a) byte-identity — original.getStateJson() === loaded.getStateJson() through migrateSave+validateSave (codec in the loop);
      (b) residency getters survive — loaded.getInspector(houseId,'building')?.internals?.house?.residents matches the original (class/age/employed arrays) and getDerived().residentCount matches;
      (c) migration case — force vacancy first (drive famine emigration via foodShortageEffects per housing.ts:388-412, or an intentionally under-occupied fixture), then tick past migration months; assert the refill set is byte-identical across a save/load round-trip;
      (d) chunked — same seed with chunk sizes 1/7/50 all byte-identical (civic-determinism.test.ts:46-56 style);
      (e) no-wall-clock audit — read src/sim/{population,labor,runner}.ts for Math.random(/Date.now(/new Date( (allow only getSaveData's savedAt metadata line) — assert the new paths are clean.
    3. EXTEND tests/unit/population.test.ts (REQ POP-01/POP-02/POP-04) — KEEP the 5 existing describes green at the top; append (target API surface):
      (a) residency consistency — residentsForHouse({...}, seeded) returns exactly effectivePopulation(b) residents; class share honors tierOfLevel (tier ≥ 3 introduces patricians); ages land in children(0-15)/workforce(16-60)/elderly(61+) bands; ageOnMonth advances ages deterministically;
      (b) migration wiring — netMigration against live inputs: attractiveness 0.9 / unemployment 0.1 / capacityAvailable 3 → 3; full city (capacityAvailable 0) → 0 (golden-neutral guarantee); famine (negative pull) drives residents out;
      (c) projection — feed getWalkerInternals().buildings house internals into residenceInspection and assert class/age/employment rows appear (advisors.test.ts:154-183 style);
      (d) wage band — wageBand({wageRate:0.2, imperialReference:0.3}) → {band:'below', relative:0.666...} and unemploymentBand thresholds (target API).
    4. EXTEND tests/unit/advisors.test.ts (REQ POP-04) — labor panel describe: assert advisorPanels(source)[labor-id].rows contains 'Wage vs Imperial' (value like 'below (67%)') and 'Unemployment Band' (value the band label), with values traced to source.getDerived() — never fabricated strings (composer rule game/advisors.ts:99-101); and the empty-city total-function case: advisorPanels composes with population === 0 and the wage rows are present-but-guarded (Pitfall 5), no throw.

    These scaffolds intentionally reference APIs delivered later (HouseInstance.residents / residentsForHouse / ageOnMonth → 19.1-01-01; DerivedSnapshot.residentCount+immigration/emigration/homeless → 19.1-01-01/19.1-02-01; labor.ts exports + getLaborSectors + setLaborSectorState + validateCommand case → 19.1-03-01/02; DerivedSnapshot.wageBand/unemploymentBand → 19.1-04-01). They are expected RED until the implementing tasks flip them green.
  </action>
  <verify>
    <human-check>Wave 0 is complete when the two files exist and the two existing files extend against the Phase-19.1 target APIs; the new/extended cases are expected RED (implementing modules land in Waves 1-4) and full `npm run typecheck` is red-but-expected for the same reason (see typecheck sequencing note above).</human-check>
    <automated>cd /Users/filipe.esch/projects/pessoal/opencaesar && test -f tests/unit/labor-sectors.test.ts && test -f tests/determinism/population-determinism.test.ts && grep -q "residentsForHouse" tests/unit/population.test.ts && grep -q "Wage vs Imperial" tests/unit/advisors.test.ts && grep -q "setLaborSectorState" tests/unit/labor-sectors.test.ts</automated>
  </verify>
  <acceptance_criteria>tests/unit/labor-sectors.test.ts and tests/determinism/population-determinism.test.ts exist and are discovered by the vitest include glob; tests/unit/population.test.ts carries residency-consistency + migration-wiring + wage-band target-surface cases while its 5 original describes stay green; tests/unit/advisors.test.ts carries the labor-panel wage/unemployment rows + empty-city total-function cases; every case pins the TARGET 19.1 API surface and is RED where its implementing wave has not landed.</acceptance_criteria>
  <done>The Wave-0 scaffolds exist/extend against the Phase-19.1 APIs (labor sectors, population determinism, residency/migration/wage units, advisor wage rows), are discovered by the runner, and are RED only where their implementing waves have not yet landed.</done>
</task>

<!-- ===================== WAVE 1 (POP-01) — per-residence population (TRACER) ===================== -->

<task type="tracer" tdd="true">
  <name>Task 19.1-01-01: Tracer — per-residence internal population on HouseInstance, derived deterministically and surfaced end-to-end (walkers.ts + population.ts + runner tickResidency + DerivedSnapshot + residenceInspection + HUDScene)</name>
  <files>src/sim/walkers.ts, src/sim/population.ts, src/sim/runner.ts, src/sim/advisors.ts, src/game/scenes/HUDScene.ts, tests/unit/population.test.ts, tests/determinism/population-determinism.test.ts</files>
  <read_first>
    - src/sim/population.ts (104 lines — the deliverable; Residence/Resident/isEligible/migration/labor/wage exports preserved verbatim; this task ADDS residentsForHouse/ageOnMonth in the same pure style)
    - src/sim/walkers.ts:87-122 (HouseInstance — the combinedPopulation/:99-102 and civic/:118-122 'never serialized' comment discipline to copy), :145-160 (BuildingInstance.house optional)
    - src/sim/housingLive.ts:82-109 (tierOfLevel, liveStats clamp, effectivePopulation — the residency source of truth), :57 (module-local constant discipline)
    - src/sim/rng.ts (mulberry32 + randInt — the ONLY legal randomness for resident ages; NEVER Math.random)
    - src/sim/runner.ts:291-401 (tick() order — residency hooks go right after tickHousingMerge at :312, before the walker loop at :315), :515-517 (tickHousingMerge), :136-163 (DerivedSnapshot interface — add residentCount/residentsByClass here), :1242-1349 (derivedSnapshot() + getDerived()), :3187-3228 (toBuildingState — MUST NOT copy residents), :2554-2556 (getStateJson = JSON.stringify(getState())), :2597-2625 (getInspector seam)
    - src/sim/advisors.ts:283-310 (residenceInspection additive-enrichment — append residents in the same guard style as civic at :298)
    - src/game/scenes/HUDScene.ts:897-937 (renderBuildingRows house branch — today fabricates (h.populationCapacity, h.populationCapacity, 'plebeian', [], {}) at :907-911; feed real internals values)
    - 19.1-PATTERNS.md walkers.ts + population.ts + advisors.ts + HUDScene.ts sections and 19.1-RESEARCH.md Pattern 1 + Pitfalls 1/4 + Code Examples §POP-01
  </read_first>
  <behavior>
    - Test 1: residentsForHouse returns exactly effectivePopulation(b) residents, deterministically (same seam/seed → same cohort), with class sourced from tierOfLevel (tier ≥ 3 introduces patricians) and ages in children/workforce/elderly bands; NEVER Math.random (population.test.ts residentsForHouse cases).
    - Test 2: after ticks spanning a %40 boundary, getInspector(houseId,'building').internals.house.residents matches getDerived().residentCount and effectivePopulation (consistency invariant) — and the same cohort survives getSaveData→fromSaveData byte-identity (population-determinism.test.ts).
    - Test 3: residenceInspection appends residents/class/age/employment ONLY when internals provide them (old minimal calls keep the old shape, UI-04), and HUDScene renders the real values instead of the fabricated 'plebeian' — getStateJson() unchanged.
  </behavior>
  <action>
    Build the POP-01 tracer slice — the internal per-residence population model wired from tick to HUD, proven end-to-end (decision POP-01). This is production code for keeps, not a prototype — the residents micro-state becomes the skeleton Waves 2/4 build on. All new state is internal-only and mutated ONLY on the %40 tick hook (Pitfall 4 / A6):
    1. MODIFY src/sim/walkers.ts (HouseInstance, :88-122): add `residents?: Resident[]` and `nextResidentId?: number` with the exact comment discipline of civic/:118-122 ('Internal only — never serialized to BuildingState, so goldens/SimState stay byte-identical'). Import `Resident` as a type-only import from './population' (no runtime cycle — population.ts has no imports; do NOT import from walkers.ts into population.ts). toBuildingState (runner.ts:3187-3228) must NOT copy these fields.
    2. EXTEND src/sim/population.ts (additive only — keep Resident/Residence/isEligible/netMigration/allocateWorkers/wageBand verbatim): add pure deterministic helpers
       - `residentsForHouse(level: number | undefined, capacity: number, salt: number, seeded: (seed:number)=>Rng): Resident[]` — deterministically builds a cohort of exactly `capacity` Resident objects: salt = a house-stable integer (id + level + tickCount-derived floor) so replay reproduces it; class share from tierOfLevel(level) (tier ≥ 3 introduces patricians — research A4); ages via mulberry32(seeded) — NEVER Math.random — bucketed into children(0-15)/workforce(16-60)/elderly(61+); employment started false; ids sequential from 1 per house (nextResidentId not required if ids are always 1..capacity). Reused by tickResidency to (re)create the cohort on level change/merge and to top up toward capacity.
       - `ageOnMonth(residents: Resident[], rngFactory): void` — deterministic month aging (age += 1 per %40 month), recomputing nothing else; id/class stable.
       Module-local constant discipline (housingLive.ts:57): any residency tuning (patrician share threshold, band boundaries) as consts in this file — never BALANCE/CONFIG keys.
    3. MODIFY src/sim/runner.ts:
       - DerivedSnapshot (:136-163): add `residentCount: number;` and `residentsByClass?: { plebeian: number; patrician: number };` (golden-safe — getStateJson serializes getState, not getDerived).
       - derivedSnapshot() (:1327-1343): populate residentCount = Σ effectivePopulation over house buildings (== population by construction) and residentsByClass from the live internals (0/0 row when no residency initialized yet — total function).
       - NEW `private tickResidency(): void` invoked from tick() at `tickCount % 40 === 0` right after tickHousingMerge (:312 — after merge sees final levels, before walkers move :315). Behavior: for each house building, (a) if b.house.residents is missing OR the house level changed since it was derived (store the deriving level on the cohort or compare effectivePopulation against residents.length), re-derive via residentsForHouse(level, capacity, salt) and assign b.house.residents; (b) else ageOnMonth(b.house.residents); (c) book new residents only up to capacity (residents.length stays === effectivePopulation). All reads are the internal b.house fields; never writes getState().
       - Keep getState():1914-1941 and toBuildingState:3187-3228 byte-identical.
    4. MODIFY src/sim/advisors.ts residenceInspection (:283-310): in the existing `if (h)` guard append `if (h.residents) out.residents = h.residents.map((r) => ({ ...r }))` (or an aggregated { count, classBreakdown, ageBands, employed } projection) — append-when-defined, original minimal calls keep the old shape (UI-04).
    5. MODIFY src/game/scenes/HUDScene.ts renderBuildingRows house branch (:903-937): replace the fabricated residenceInspection call at :907-911 with internals-derived values — feed real class/occupancy from insp/internals; add `if (typeof insp.residents === 'object')` guarded rows: appendRow(body,'Class', ...), appendRow(body,'Age Bands', ...), appendRow(body,'Employed', ...) (mirror the insp.foodInventory guard at :925-928). Never innerHTML for any sim-derived string.
    6. Flip green the Wave-0 residency unit cases (population.test.ts residency describe) and the residency part of population-determinism.test.ts (byte-identity + getInspector residents + residentCount). Run the tokens-listed existing suites to prove NO regression: goldens, economy, labor, advisors, and the full determinism folder.
    Security: all new simulation strings are deterministic data; no wall-clock (no new Date/Math.random/Date.now in src/sim/population.ts or runner.ts residency path — the determinism audit case in population-determinism.test.ts enforces it).
    Discretion resolved here (CONTEXT §the agent's Discretion): the age-band/children/elderly derivation formula (mulberry32 salt of house id + level + month tick) and the class-of-record (tierOfLevel, A4); the residents-by-class/total-function row shape; HUD row labels.
  </action>
  <verify>
    <automated>cd /Users/filipe.esch/projects/pessoal/opencaesar && npx vitest run tests/unit/population.test.ts tests/determinism/population-determinism.test.ts tests/golden tests/determinism -x</automated>
  </verify>
  <acceptance_criteria>HouseInstance carries residents?: Resident[] / nextResidentId? (internal-only, same comment discipline as civic — toBuildingState does NOT copy them); population.ts exposes residentsForHouse/ageOnMonth pure + deterministic (mulberry32, no Math.random); runner.ts ticks residency on %40 after tickHousingMerge so replay reproduces byte-identically, residents.length === effectivePopulation every tick, level change/merge re-derives; DerivedSnapshot has residentCount + residentsByClass? (total functions); residenceInspection appends residents when defined (old shape preserved); HUDScene renders real class/age/employed rows instead of fabricated 'plebeian' via guarded rows; getStateJson/getState/toBuildingState byte-identical; goldens untouched; the Wave-0 residency unit + determinism cases pass.</acceptance_criteria>
  <done>POP-01's tracer is live: per-residence internal population (class/age/employment) is derived deterministically from level/tick history, consistent with effectivePopulation, reproducible byte-identical after save/load replay, and surfaced through DerivedSnapshot + the residence inspector seam (residenceInspection append + HUDScene real rows) — with zero Baby-step golden delta.</done>
  <reversibility rating="reversible">The residency micro-state is internal-only (never serialized) and additive — reverting to no-residency costs nothing (toBuildingState is untouched) and no golden data is regenerated.</reversibility>
</task>

<!-- ===================== WAVE 2 (POP-02) — migration ===================== -->

<task type="auto" tdd="true">
  <name>Task 19.1-02-01: Wire netMigration on the month cadence — attractiveness from live derived state, vacancy-bounded internal occupancy deltas, famine emigration → refill, homeless count (runner.ts + DerivedSnapshot)</name>
  <files>src/sim/runner.ts, tests/unit/population.test.ts, tests/determinism/population-determinism.test.ts</files>
  <read_first>
    - src/sim/population.ts:56-68 (MigrationInput/netMigration — reused as-is; attractiveness 0..1, unemployment 0..1, capacityAvailable; bounded by Math.floor(capacityAvailable) so full cities are zero-delta)
    - src/sim/runner.ts:291-401 (tick() order — tickPopulationMigration goes right after tickHousingMerge at :312 alongside tickResidency, before the walker loop :315), :312 (the %40 hook), :469-480 (civicCoverage('health'|'literacy'|'entertainment') — attractiveness inputs), :1273-1275 (unemploymentRate in derivedSnapshot), :1242-1349 (derivedSnapshot + getDerived for added immigration/emigration/homeless fields), :136-163 (DerivedSnapshot interface)
    - src/sim/housing.ts:388-412 (foodShortageEffects().emigration — the ONLY existing vacancy source POP-02 consumes; per-house emigration when a famine persists)
    - src/sim/housing.ts:60 / houseHappinessInput area (desirabilityOf — the per-house desirability attractiveness input)
    - 19.1-PATTERNS.md runner.ts migration-wiring section (:149-154) and 19.1-RESEARCH.md Pitfall 3 (occupancy must NOT feed workerPool/tax/populationOf — capacity-based behavior preserved, A6) + Pitfall 4 (mutation ONLY inside tick()%40)
  </read_first>
  <behavior>
    - Test 1: with a full city (every house occupants === capacity) a migration month yields NO occupancy delta and getStateJson is unchanged (golden-neutral by construction — population.test.ts + determinism round-trip).
    - Test 2: when famine emigration (foodShortageEffects) has created vacancy, positive-attractiveness migration months refill houses deterministically up to capacity (population.test.ts migration-vacancy case + population-determinism refill byte-identity).
    - Test 3: DerivedSnapshot exposes immigration/emigration/homeless deltas (per-month counts); over a save/load round-trip the loaded city reports identical numbers (no state divergence).
  </behavior>
  <action>
    Wire POP-02's migration as internal-only derived deltas on the month cadence (decision POP-02; A6/A7 honored — occupancy is a derived layer, never a workerPool/tax/populationOf balance effect, and NO walker spawning):
    1. MODIFY src/sim/runner.ts — NEW `private tickPopulationMigration(): void` invoked from tick() at `tickCount % 40 === 0` right after tickHousingMerge (and after/alongside tickResidency — residency syncs the cohort first, migration then adjusts occupancy). Behavior:
       - Compute the per-month attractiveness input blend [ASSUMED A5] from live derived state ALREADY computed this tick — never a second recompute (sim/advisors.ts:133-134 rule): e.g. normalize desirabilityOf(b) for the house, the civicCoverage('health'|'literacy'|'entertainment') averages, and (1 − derivedSnapshot().unemploymentRate); blend weights module-local consts at the top of the method or in population.ts (balance-parity — never BALANCE/CONFIG).
       - capacityAvailable = Σ over houses of max(0, effectivePopulation(b) − (b.house.residents?.length ?? effectivePopulation(b))). Today houses are always-full so capacityAvailable is 0 ⇒ netMigration is 0 ⇒ zero golden delta (Pitfall 3). Famine emigration (housing.ts foodShortageEffects().emigration) or any under-occupancy makes it positive.
       - Apply netMigration(MigrationInput) as internal occupancy: new residents move into under-occupied houses (use the residentsForHouse/ageOnMonth ingest path from 19.1-01-01 — book up to capacity in deterministic house order); a negative pull (attractiveness &lt; unemployment) or active famine emigration drifts residents out of houses and accumulates a module-local `homeless` counter [ASSUMED A5].
       - DerivedSnapshot (:136-163) + derivedSnapshot() (:1327-1343): add optional `immigration?: number; emigration?: number; homeless?: number;` populated from the month's internal deltas (0 by default — total function, empty cities safe). NEVER write into getState()/toBuildingState/SimState; getStateJson stays byte-identical.
       - No immigration/emigration walker spawning (A7 anti-pattern — toWalkerState/:3230-3244 untouched); workerPool/tax/populationOf keep using capacity-based effectiveWorkers/effectiveTaxPerTick/effectivePopulation (A6).
    2. Demonstrate/verify the famine→vacancy→refill path deterministically (this is the only scenario where netMigration is observable): after tickResidency-synced residents, when a house's famine emigration has dropped residents below capacity, the next positive-attractiveness migration month fills it back to effectivePopulation; assert the final resident footprint is byte-identical across fromSaveData.
    3. Flip green the Wave-0 migration unit + determinism cases (population.test.ts migration-vacancy/famine cases + population-determinism refill case). Re-run the full determinism folder + tests/golden to prove zero golden/determinism delta.
    Security: migration math is pure over deterministic state; no wall-clock (audited by the population-determinism no-Math.random/Date.now/new-Date case).
    Discretion resolved here (CONTEXT §the agent's Discretion): the attractiveness formula inputs + weights (documented [ASSUMED A5] in a comment next to the blend consts), the homeless counter semantics (internal-only, reset each month, surfaced via DerivedSnapshot), the negative-pull drift strategy (deterministic house order).
  </action>
  <verify>
    <automated>cd /Users/filipe.esch/projects/pessoal/opencaesar && npx vitest run tests/unit/population.test.ts tests/determinism/population-determinism.test.ts tests/golden tests/determinism -x</automated>
  </verify>
  <acceptance_criteria>runner.ts has tickPopulationMigration() on tickCount % 40 after tickHousingMerge, computing attractiveness from already-derived inputs and applying netMigration as internal-only occupancy deltas bounded by capacityAvailable (0 when full ⇒ goldens unchanged); famine emigration creates vacancy → positive-attractiveness months refill deterministically; DerivedSnapshot gains immigration/emigration/homeless? (total functions); homelessness counted internally; getState/getStateJson/toBuildingState untouched; no walker spawning; workerPool/tax/populationOf stay capacity-based; the Wave-0 migration unit + determinism cases pass; full determinism + golden suites stay green.</acceptance_criteria>
  <done>POP-02 is complete: migration is modeled on the month cadence with a live attractiveness index, bounded by vacancy, refilling famine-emptied houses and counting homelessness — all as internal derived deltas with zero golden delta and byte-identical replay.</done>
  <reversibility rating="reversible">Migration changes only internal resident occupancy (never serialized state, never workerPool/tax), so reverting it costs nothing and goldens stay untouched.</reversibility>
</task>

<!-- ===================== WAVE 3 (POP-03) — labor sectors ===================== -->

<task type="auto" tdd="true">
  <name>Task 19.1-03-01: Replace greedy tickLabor with sector-priority allocateWorkers — labor.ts (priority map + grouping + distribution) + reserve-semantics pinning + getLaborSectors getter</name>
  <files>src/sim/labor.ts, src/sim/runner.ts, tests/unit/labor-sectors.test.ts, tests/unit/labor.test.ts</files>
  <read_first>
    - src/sim/economy.ts workerPool (runs the pool the allocateWorkers path must keep consistent with)
    - src/sim/population.ts:70-91 (SectorPriority/LaborSector/allocateWorkers — reused as-is; :84-89 the pinned branch does NOT reserve pool under an empty pool — see Pitfall 2)
    - src/sim/runner.ts:2729-2744 (the greedy tickLabor loop to REPLACE — pool, laborConnected guard, setActive(b, give >= want), per-building give = min(want, pool)), :3069-3075 (setActive — must keep emitting building-active/inactive), :197/:2561-2568 (marketConfigs private-map precedent for the sector pin/pause store used in 19.1-03-02)
    - tests/unit/labor.test.ts:58-69 (post-connection assignment via r['buildings']/r['tickLabor']()), :93-122 (surplus fill-all invariants to preserve), :125-140 (assigned ≤ pool / assigned ≤ required invariants)
    - 19.1-PATTERNS.md runner.ts tickLabor section (:94-131) + labor.ts section (:71-91) and 19.1-RESEARCH.md Code Examples §POP-03 + Pitfall 2 + Assumption A3; 19.1-VALIDATION.md 19.1-03-01
  </read_first>
  <behavior>
    - Test 1: priority ordering — with a scarce pool the food sector (priority 1) is fully staffed before commerce/culture; surplus pool fills every job (labor-sectors.test.ts scarcity/surplus cases).
    - Test 2: pinned reserve semantics — a pinned sector keeps its prior assigned under a shrunken pool while unpinned sectors starve (Pitfall 2 — runner-level reserve, not the weak pure branch).
    - Test 3: pausing a sector (needed=0) spills its workers to others; restore-auto reverts to default allocation; labor.test.ts invariants (assigned ≤ pool, assigned ≤ required, surplus fills all) all stay green.
  </behavior>
  <action>
    Rework worker assignment to sector-priority allocation (decision POP-03; A3 tuning is module-local, and the labor.test.ts invariant suite + full suite + winnability probe are the regression net):
    1. NEW src/sim/labor.ts (pure, no RNG/wall-clock — housingLive.ts analog). Exports:
       - `const LABOR_SECTOR_PRIORITY: Record<string, SectorPriority> = { food: 1, water: 2, utility: 3, commerce: 4, culture: 5 }` with a header comment marking the map [ASSUMED A3] and module-local (balance-parity — never BALANCE/CONFIG). `export const SECTOR_IDS = Object.keys(LABOR_SECTOR_PRIORITY)` (the known set the 19.1-03-02 command validates against).
       - `buildLaborSectors(buildings: BuildingInstance[]): LaborSector[]` — one LaborSector per known sector id; sector assignment: map each labor-connected building (b.workersRequired > 0, b.laborConnected) to its sector by BUILDINGS[b.type].category → LABOR_SECTOR_PRIORITY (unmapped categories fall into a default/lowest-urgency sector or are skipped — decide the mapping deterministically and document it; every known building category must map). needed = Σ workersRequired over the sector's labor-connected buildings; assigned = 0; pinned/paused read from a per-sector state argument (run IDs, actual booleans from the runner's store, default false/unset).
       - `applySectorAssignments(buildings: BuildingInstance[], sectors: LaborSector[]): void` — for each sector set its buildings' workersAssigned: distribute the sector's `assigned` across its labor-connected buildings greedily within each building's workersRequired cap (sum of per-sector assigned ≤ sector needed ≤ sector caps; total never exceeds pool by construction), calling nothing (activation handled by the runner loop as today).
    2. MODIFY src/sim/runner.ts:
       - Add a private per-sector state store (field, e.g. `private laborSectorCfg = new Map<string, { pinned: boolean; paused: boolean }>()`, default {pinned:false,paused:false} for any sector absent — marketConfigs precedent :197/:2561-2563). getLaborSectors() reads it; 19.1-03-02 writes it.
       - Replace the greedy body of tickLabor() (:2729-2744): `let pool = workerPool(this.buildings)` unchanged; if a sector is paused its needed is forced to 0 (buildLaborSectors already folds paused ⇒ needed=0); build sectors; **reserve semantics (Pitfall 2)**: for each PINNED sector subtract its previous `assigned` (or its needed, whichever is min under the current pool) from the pool upfront as a reservation, then run allocateWorkers(sectors, remainingPool) over the unpinned ordering (the pure function sorts by priority); distribute via applySectorAssignments; keep per-building workersAssigned within the building cap and `this.setActive(b, give >= want)` exactly as the current loop does (labor.test.ts:3069-3075 behavior preserved).
       - Preserve EVERY invariant labor.test.ts asserts: state.assignedWorkers ≤ state.totalWorkers (:130), per-building workersAssigned ≤ workersRequired (:137), surplus pool ⇒ assignedWorkers === jobDemand (:121), staffed-over-window non-oscillation (:93-122). Re-run `npx vitest run tests/unit/labor.test.ts tests/unit/labor-sectors.test.ts tests/golden` after the rewrite, plus the winnability probe (A3 — reordered allocation could shift tune-dependent staffing).
       - NEW `getLaborSectors(): Array<{ id: string; priority: SectorPriority; needed: number; assigned: number; pinned: boolean; paused: boolean }>` — read-only, derived from buildLaborSectors + the store (never fabricates; advisors/UI consume it via this getter, mirroring marketConfig :2564-2568).
    3. Flip green the Wave-0 labor-sectors.test.ts allocation cases (ordering, scarcity across sectors, pin reserve under a shrunken pool, pause ⇒ needed=0, restore-auto, surplus totals).
    Security: buildLaborSectors/applySectorAssignments are pure transformations over runner state; sector ids come from the module-local SECTOR_IDS (unknown ids never reach this layer — validated at the command boundary in 19.1-03-02).
    Discretion resolved here (CONTEXT §the agent's Discretion): the default sector→priority map (A3), the unmapped-category fallback, the pinned reserve amount rule (min(priorAssigned, needed, pool)), and the getLaborSectors() row vocabulary.
  </action>
  <verify>
    <automated>cd /Users/filipe.esch/projects/pessoal/opencaesar && npx vitest run tests/unit/labor-sectors.test.ts tests/unit/labor.test.ts tests/golden -x && npx vitest run tests/integration/winnability-probe.test.ts -x</automated>
  </verify>
  <acceptance_criteria>src/sim/labor.ts exists (LABOR_SECTOR_PRIORITY + SECTOR_IDS + buildLaborSectors + applySectorAssignments, module-local, deterministic); runner.tickLabor uses allocateWorkers over sector grouping with pinned reserve semantics (a pinned sector keeps its prior assigned under a shrunken pool) and paused ⇒ needed=0; per-building activation/setActive preserved; getLaborSectors() read-only getter exists; labor.test.ts invariants (assigned ≤ pool, assigned ≤ required, surplus fills all) remain green; labor-sectors.test.ts allocation cases pass; goldens untouched; winnability probe green.</acceptance_criteria>
  <done>POP-03's allocation half is live: scarce workers are assigned by sector priority 1-5 through allocateWorkers, pinned sectors reserve their staffing under scarcity, paused sectors release their workers, and every labor invariant + winnability probe stays green.</done>
  <reversibility rating="costly">The tickLabor reorder changes live worker-distribution behavior, which is assertion-covered by labor.test.ts + the winnability probe — reverting the code is trivial (no serialized change) but re-verifying the tune-dependent suites is required (A3).</reversibility>
</task>

<task type="auto">
  <name>Task 19.1-03-02: setLaborSectorState SaveCommand — pin/pause/restore-auto via types union + runner handler + exhaustive applyCommand branch + saveCodec validateCommand case</name>
  <files>src/sim/types.ts, src/sim/saveCodec.ts, src/sim/runner.ts, tests/unit/labor-sectors.test.ts</files>
  <read_first>
    - src/sim/types.ts:74-99 (SaveCommand union — append the new member after dismissTutorialStep at :98-99, copying the startMission comment convention for replay semantics), :108 (pendingCommands)
    - src/sim/runner.ts:1893-1906 (setPolicy — the paused-enqueue + push-on-accept + commandLog/saveCommands precedent), :946-973 (openTradeRoute — the unknown-city {ok:false,error} + push-on-accept guard precedent), :197/:2561-2568 (marketConfigs private-map store precedent), :2554-2556 (getStateJson), :3255-3292 (applyCommand exhaustive dispatch with the never-guard at :3288-3290)
    - src/sim/saveCodec.ts:90-147 (validateCommand switch — add the 'setLaborSectorState' case, mirroring :134-142), :156-224 (validateSave — protects pendingCommands too)
    - src/sim/labor.ts (the SECTOR_IDS export created in 19.1-03-01 — this task validates against it)
    - 19.1-PATTERNS.md runner.ts setLaborSectorState section (:182-239) + saveCodec.ts section (:243-259) + types.ts section, and 19.1-RESEARCH.md Pattern 3 + Security Domain (ASVS V5)
  </read_first>
  <behavior>
    - Test 1: a live setLaborSectorState('food', {pinned:true}) pins the sector; the command is recorded and reloads to an identical getLaborSectors() via getSaveData → migrateSave+validateSave → fromSaveData (labor-sectors.test.ts round-trip).
    - Test 2: setLaborSectorState('bogus', ...) and out-of-range payloads return {ok:false,error} (never a throw) AND validateSave rejects a save containing a malformed setLaborSectorState as 'malformed-command' before replay (security V5).
    - Test 3: while paused, setLaborSectorState enqueues and applies on the resume tick (paused-enqueue precedent), so the pin survives a paused save/reload.
  </behavior>
  <action>
    Complete POP-03 with the replayable player-action surface (decision POP-03; established SaveCommand governance — union + exhaustive applyCommand + validateSave codec + private-map store + push-on-accept/paused-enqueue):
    1. MODIFY src/sim/types.ts (SaveCommand union, :74-99): append
       `| { kind: 'setLaborSectorState'; sector: string; pinned?: boolean; paused?: boolean }`
       with a comment explaining replay semantics: pin/pause reconstruct from the replayed stream (setLaborSectorState commands mutate the runner's per-sector store during fromSaveData's command replay), so sector state survives save/load with NO SaveData schema field.
    2. MODIFY src/sim/runner.ts:
       - NEW public handler `setLaborSectorState(sector: string, opts: { pinned?: boolean; paused?: boolean }): { ok: boolean; error?: string }`:
         - Validate `sector` against labor.ts SECTOR_IDS — unknown ⇒ `{ ok: false, error: 'unknown-sector' }` (mirrors openTradeRoute's unknown-city guard, runner.ts:948). Validate `pinned`/`paused` are `undefined | boolean` — any other type ⇒ `{ ok: false, error: 'invalid-config' }`. These return-level guards AND the saveCodec case (step 3) are the security V5 boundary.
         - Paused path (openTradeRoute/setPolicy precedent, runner.ts:1893-1906): enqueue `{ kind: 'setLaborSectorState', sector, pinned, paused }` and return `{ ok: true }` — the queue drains with replaying=true and applies via applyCommand.
         - Live+replay path: compute the merged state from the current store entry (default {pinned:false,paused:false}) — a call with only pinned sets pinned and preserves existing paused (and vice versa) — store via this.laborSectorCfg.set(sector, merged); record via the establish push-on-accept pattern (commandLog + saveCommands.push, respecting suppressCommandRecording — openTradeRoute :968-973).
       - applyCommand (:3255-3292): add `else if (cmd.kind === 'setLaborSectorState') { runner.setLaborSectorState(cmd.sector, cmd); }` — the exhaustive never-guard (:3288-3290) forces this branch at typecheck.
       - Ensure tickLabor (19.1-03-01) reads the SAME laborSectorCfg store so a pin/pause set by this command immediately changes allocation on the next tick.
    3. MODIFY src/sim/saveCodec.ts validateCommand (:90-147): add case
       `case 'setLaborSectorState': if (!str(c.sector)) return 'malformed-command'; if (c.pinned !== undefined && typeof c.pinned !== 'boolean') return 'malformed-command'; if (c.paused !== undefined && typeof c.paused !== 'boolean') return 'malformed-command'; break;`
       Unknown kinds already fall to 'unknown-command-kind' (:143-144) and validateSave protects both commands and pendingCommands (:188-221) so a hostile payload is rejected BEFORE fromSaveData replays — never a raw 'unknown command kind' throw from applyCommand.
    4. Flip green the Wave-0 labor-sectors.test.ts SaveCommand cases: live pin → round-trip equality of getLaborSectors(); unknown sector → {ok:false,error}; validateSave rejecting {sector:42} and {pinned:'yes'} as 'malformed-command'; paused-enqueue ordering. Re-run tests/unit/labor-sectors.test.ts + tests/unit/saveCodec.test.ts + tests/unit/security.test.ts + tests/golden.
    Security (V5 + T-19.1-01): the handler AND the codec reject unknown sector ids and non-finite/non-boolean config; all strings are deterministic sim data; UI (future Phase 20) renders via textContent.
    Discretion resolved here (CONTEXT §the agent's Discretion): the merged-store update semantics (partial pin/pause set preserves the other flag), the 'invalid-config' vs 'unknown-sector' error vocabulary, and the Treasury-free/no-behavior-change guarantee (this command only affects sector allocation config).
  </action>
  <verify>
    <automated>cd /Users/filipe.esch/projects/pessoal/opencaesar && npx vitest run tests/unit/labor-sectors.test.ts tests/unit/saveCodec.test.ts tests/unit/security.test.ts tests/golden -x</automated>
  </verify>
  <acceptance_criteria>SaveCommand union gains setLaborSectorState {sector, pinned?, paused?}; runner.setLaborSectorState validates sector against SECTOR_IDS + boolean-only flags ({ok:false,error}, never a throw), enqueues while paused, applies live+replay into the laborSectorCfg store with partial-flag merge + push-on-accept (suppressCommandRecording); applyCommand has the exhaustive branch and typechecks; validateCommand has the setLaborSectorState case (str sector, boolean-only flags → 'malformed-command'); pin/pause survives save/load (getSaveData → fromSaveData → identical getLaborSectors()); labor-sectors.test.ts SaveCommand + security cases pass; saveCodec.test.ts + security.test.ts + goldens stay green.</acceptance_criteria>
  <done>POP-03 is complete: pin/pause/restore-auto is a validated, replayable setLaborSectorState SaveCommand wired through the union, the exhaustive applyCommand dispatch, the private per-sector store, and the save/load codec — a malformed payload is rejected before replay, and sector state survives a save/load round-trip.</done>
  <reversibility rating="reversible">The new command kind is additive and inert until issued (defaults are unpinned/unpaused), so removing it cannot corrupt saves — no schema change, no golden impact.</reversibility>
</task>

<!-- ===================== WAVE 4 (POP-04) — wage policy ===================== -->

<task type="auto" tdd="true">
  <name>Task 19.1-04-01: Urban wage policy — wageBand vs module-local imperial reference + unemploymentBand projected into DerivedSnapshot + labor advisor panel rows</name>
  <files>src/sim/population.ts, src/sim/runner.ts, src/game/advisors.ts, tests/unit/population.test.ts, tests/unit/advisors.test.ts</files>
  <read_first>
    - src/sim/population.ts:93-104 (WagePolicy/wageBand — reused verbatim: relative = wageRate/(imperialReference||1), band below/at/above — already a total function)
    - src/sim/runner.ts:136-163 (DerivedSnapshot interface — add wageBand + unemploymentBand), :181 (policy default {taxRate:0.1, wageRate:0.1}), :1273-1275 (unemploymentRate already computed in derivedSnapshot — the unemploymentBand input), :1327-1343 (the derivedSnapshot return object to extend), :1893-1904 (setPolicy clamps wageRate to 0..1 — the imperial reference must sit inside (0,1))
    - src/game/advisors.ts:96-105 (advisorPanels — derived = source.getDerived() already in scope), :188-202 (the labor panel where the two rows land), :71-85 (ADVISOR_TAB_ORDER — MUST stay unchanged), :99-101 (the composer never-fabricate rule)
    - tests/unit/advisors.test.ts:154-227 (inspectors-enriched/composer describes to extend) and the empty-city cases
    - 19.1-PATTERNS.md runner.ts DerivedSnapshot section (:158-178) + game/advisors.ts section (:288-313) and 19.1-RESEARCH.md Code Examples §POP-04 + Assumptions A1/A2 + Pitfall 5 + Open Question 4 (rows in the LABOR panel; finance panel unchanged to avoid advisor-test churn)
  </read_first>
  <behavior>
    - Test 1: derived.wageBand equals wageBand({wageRate: policy.wageRate, imperialReference}) — below/at/above + relative for a stock policy — and derived.unemploymentBand derives from the derivedSnapshot unemploymentRate (population.test.ts + advisors.test.ts).
    - Test 2: the labor advisor panel renders a 'Wage vs Imperial' row (e.g. 'below (67%)') and an 'Unemployment Band' row whose values trace to source.getDerived(), never fabricated (advisors.test.ts row-presence + value-tracing cases).
    - Test 3: empty city (population === 0) composes advisorPanels with wage rows guarded — no throw (Pitfall 5 total function).
  </behavior>
  <action>
    Deliver POP-04's pure-reporting wage band (decision POP-04; NO treasury/wage-rate behavior change, so goldens are unaffected):
    1. EXTEND src/sim/population.ts (additive): `const IMPERIAL_WAGE_REFERENCE = 0.3;` (module-local, inside (0,1) because setPolicy clamps wageRate to 0..1 — [ASSUMED A1]; a comment marks the value as agent-tuned reporting semantics) and `export function unemploymentBand(rate: number): { label: string; rate: number }` — module-local thresholds into labelled tiers (e.g. healthy/moderate/high — [ASSUMED A2]), a total function (guard NaN/out-of-range via Math.max(0,Math.min(1,rate))). wageBand itself is reused verbatim — do not modify it.
    2. MODIFY src/sim/runner.ts:
       - DerivedSnapshot (:136-163): add `wageBand: { band: 'below' | 'at' | 'above'; relative: number };` and `unemploymentBand: { label: string; rate: number };` (golden-safe — getStateJson serializes getState, not getDerived).
       - derivedSnapshot() (:1327-1343): populate `wageBand: wageBand({ wageRate: this.policy.wageRate, imperialReference: IMPERIAL_WAGE_REFERENCE })` and `unemploymentBand: unemploymentBand(unemploymentRate)` where unemploymentRate is the already-computed value at :1273-1275 (never a second recompute). Both are total functions — empty cities and imperialReference||1 guards already hold (population.ts:101).
    3. MODIFY src/game/advisors.ts labor panel (:188-202): append two rows reading derived (already in scope at :104), guarding for undefined (empty-city total function, Pitfall 5):
       - `{ label: 'Wage vs Imperial', value: `${derived.wageBand.band} (${Math.round(derived.wageBand.relative * 100)}%)` }` (tone: 'bad' when below, 'ok' when at/above).
       - `{ label: 'Unemployment Band', value: derived.unemploymentBand.label }`.
       ADVISOR_TAB_ORDER (:71-85) unchanged; finance panel untouched (Open Question 4). No fabricated strings — both values come from source.getDerived() (composer rule :99-101).
    4. Flip green the Wave-0 wage cases: population.test.ts wageBand/imperialReference + unemploymentBand cases (target API) and advisors.test.ts labor-panel row-presence + empty-city compose-without-throw cases. Re-run tests/unit/advisors.test.ts + tests/unit/population.test.ts + tests/golden.
    Security: pure projections only (no input beyond live policy + unemploymentRate); all advisor strings compose from getter data via string templates into the existing value type — no innerHTML anywhere.
    Discretion resolved here (CONTEXT §the agent's Discretion): the imperial reference value (A1) and unemployment band thresholds/labels (A2) — both module-local and report-only; the labor-panel row placement (Open Question 4).
  </action>
  <verify>
    <automated>cd /Users/filipe.esch/projects/pessoal/opencaesar && npx vitest run tests/unit/population.test.ts tests/unit/advisors.test.ts tests/golden -x</automated>
  </verify>
  <acceptance_criteria>population.ts gains IMPERIAL_WAGE_REFERENCE (module-local, inside (0,1)) + unemploymentBand(rate) total helper (wageBand reused verbatim); DerivedSnapshot gains wageBand + unemploymentBand populated in derivedSnapshot() from policy.wageRate + the already-computed unemploymentRate (total functions, no second recompute); game/advisors.ts labor panel renders 'Wage vs Imperial' + 'Unemployment Band' from source.getDerived() with undefined guards; ADVISOR_TAB_ORDER + finance panel unchanged; advisorPanels composes without throwing at population === 0; the Wave-0 wage unit + advisor cases pass; goldens untouched.</acceptance_criteria>
  <done>POP-04 is complete: the urban wage policy reports wageBand vs the imperial reference and an unemploymentBand, both as total pure projections in DerivedSnapshot surfaced in the labor advisor panel — with zero behavior/treasury change and no golden delta.</done>
  <reversibility rating="reversible">Pure reporting only — DerivedSnapshot fields and advisor rows; removing them changes nothing in the sim state or goldens.</reversibility>
</task>

<!-- ===================== WAVE 5 (close) — phase gate ===================== -->

<task type="auto">
  <name>Task 19.1-05-01: Close gate — full suite + typecheck + check:military green, goldens byte-unchanged, determinism replay for residents+migration+sectors proven</name>
  <files>tests/golden (read-only — assert unchanged), tests/determinism/population-determinism.test.ts, tests/unit/labor-sectors.test.ts, tests/unit/population.test.ts, tests/unit/advisors.test.ts</files>
  <read_first>
    - 19.1-RESEARCH.md Validation Architecture (full-suite command; Phase Requirements → Test Map for POP-01..04) and 19.1-VALIDATION.md (Per-Task Verification Map 19.1-05-01: `npm run typecheck && NODE_OPTIONS="--max-old-space-size=4096" npx vitest run --maxWorkers=4 && npm run check:military`)
    - The grep-proofs for the golden-byte contract (toBuildingState runner.ts:3187-3228, getStateJson :2554-2556, golden.test.ts:27,61) and the no-wall-clock rule (population-determinism.test.ts source audit)
  </read_first>
  <action>
    Run the authoritative phase gate AFTER Waves 0-4 are green, proving the deterministic wiring did not disturb any existing system (decision-basis: A6 'goldens untouched', milestone gate before /gsd-verify-work):
    1. `npm run typecheck` — full green (red earlier waves were expected-until-imported-modules existed; THIS is the authoritative gate).
    2. `NODE_OPTIONS="--max-old-space-size=4096" npx vitest run --maxWorkers=4` — the whole suite (unit + integration + determinism + golden + property) green together. Pay attention to the suites most likely to be affected by the wiring: tests/golden, tests/determinism/* (all 14 + the new population-determinism), tests/unit/labor*.test.ts, tests/unit/economy.test.ts, tests/unit/advisors.test.ts, tests/unit/population.test.ts, tests/integration/winnability-probe.test.ts (A3 labor rework regression net).
    3. `npm run check:military` — the military-absence CI gate green.
    4. Prove goldens byte-unchanged: `git status --porcelain tests/golden` must be empty (no fixture regeneration) — the hard golden-byte proof for the whole phase.
    5. Confirm the new Wave-1/2/3/4 suites are green and provide the exact per-requirement evidence: POP-01 (population.test.ts residency + population-determinism residents round-trip), POP-02 (population.test.ts migration + population-determinism refill byte-identity), POP-03 (labor-sectors.test.ts allocation/pin/pause/SaveCommand + security cases), POP-04 (advisors.test.ts wage rows + empty-city).
    If ANY gate is red, FIX within this task (do not ship a red phase): correct the implementation, NEVER regenerate golden fixtures unless the corpus genuinely changed (it must not — this phase is internal-only by construction). Surface a VERIFICATION-relevant note in the SUMMARY if a tuning assumption (A1/A2/A3/A5) was changed from the [ASSUMED] default to make a suite green.
  </action>
  <verify>
    <automated>cd /Users/filipe.esch/projects/pessoal/opencaesar && npm run typecheck && NODE_OPTIONS="--max-old-space-size=4096" npx vitest run --maxWorkers=4 && npm run check:military && test -z "$(git status --porcelain tests/golden)"</automated>
  </verify>
  <acceptance_criteria>npm run typecheck green; full vitest suite (all unit/integration/determinism/golden/property files) green including population-determinism + labor-sectors + the extended population/advisors tests; npm run check:military green; `git status --porcelain tests/golden` is EMPTY (goldens byte-unchanged); the per-requirement evidence suites for POP-01..04 all pass.</acceptance_criteria>
  <done>Phase 19.1 closes: the full suite + typecheck + check:military are green together, goldens are byte-identical, determinism replay proves residents + migration + labor sectors (including pinned/paused) reproduce exactly, and POP-01..04 each have passing evidence in the commit history.</done>
  <reversibility rating="reversible">The phase gate is a verification-only task over already-reversible wiring; no fixture regeneration is ever performed here.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player input → sim core (save/load + live actions) | Untrusted SaveCommand payloads cross here — including the NEW setLaborSectorState kind — and are replayed by fromSaveData on load (runner.ts:2662-2678). A malformed payload must be rejected by validateSave/validateCommand BEFORE replay, never reaching applyCommand's raw 'unknown command kind' throw. |
| sim core → UI (HUDScene renderBuildingRows + advisorPanels) | Deterministic sim strings (resident class/age/employment, wage/unemployment band labels) cross into the DOM/advisor rows; must be composed from getters via textContent (never innerHTML) and never fabricated. |
| tick mutation surface | New residency/migration/sector state mutates ONLY inside tick() %40 hooks and the setLaborSectorState handler; any mutation in a getter/read path breaks the save/load replay byte-identity (Pitfall 4). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-01 | Tampering | setLaborSectorState SaveCommand (types.ts union → applyCommand → validateCommand) | high | mitigate | Unknown sector id → {ok:false,error:'unknown-sector'} in the runner handler (openTradeRoute precedent); non-string sector / non-boolean pinned/paused rejected as 'malformed-command' by the new saveCodec validateCommand case BEFORE fromSaveData replay; unknown kind falls to 'unknown-command-kind' (:143-144). Covered by labor-sectors.test.ts security cases + saveCodec.test.ts + security.test.ts. Task 19.1-03-02, T-19.1-01. |
| T-19.1-02 | Tampering | Determinism integrity — save/load replay divergence from residency/migration/labor-sector state | high | mitigate | All new mutable state lives on HouseInstance internals / DerivedSnapshot (never BuildingState/SimState — toBuildingState/getStateJson untouched); residency+migration mutate only on tickCount % 40 inside tick(); sector config only in the setLaborSectorState handler; getters (getDerived/getInspector/getLaborSectors) read-only. Proven by population-determinism.test.ts byte-identity + chunked 1/7/50 + migration refill round-trip. Tasks 19.1-01-01/02-01/03-01/02, T-19.1-02. |
| T-19.1-03 | Tampering | HUDScene residence inspector + advisor labor panel strings | medium | mitigate | residenceInspection appends resident fields only when defined (old shape preserved, UI-04); HUDScene renders real internals values replacing fabricated 'plebeian' via guarded appendRow + textContent; advisor rows (Wage vs Imperial / Unemployment Band) compose from source.getDerived() — never fabricated strings (game/advisors.ts:99-101) — and are total functions over population === 0. Tasks 19.1-01-01/04-01, T-19.1-03. |
| T-19.1-04 | Tampering | Golden byte-identity — per-residence/sector state leaking into serialized state | critical | mitigate | residents/nextResidentId/sector config are internal-only (never copied by toBuildingState :3187-3228 / toWalkerState :3230-3244 / getState :1914-1941); DerivedSnapshot additions are golden-safe (getStateJson = JSON.stringify(getState()) :2554-2556); each wave gates `tests/golden` and the close task asserts `git status --porcelain tests/golden` empty. Tasks 19.1-01-01..05-01, T-19.1-04. |
| T-19.1-05 | Tampering | no-wall-clock rule in the new sim paths | high | mitigate | residentsForHouse/ageOnMonth/netMigration wiring use only deterministic inputs + mulberry32 seeded RNG (rng.ts) — never Math.random/Date.now/new Date; ONLY getSaveData's non-state savedAt metadata may use Date.now; population-determinism.test.ts enforces the no-wall-clock source audit over src/sim/{population,labor,runner}.ts. Tasks 19.1-01-01/02-01, T-19.1-05. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | low | accept | No external packages are installed this phase — RESEARCH Package Legitimacy Audit is N/A (all work is in-repo TypeScript wiring an existing module); no new dependency surface, so no postinstall-exposure risk to gate. |
</threat_model>

<verification>
Overall phase checks (per-wave gates; also see 19.1-VALIDATION.md Per-Task Verification Map — the authoritative sampling rate):
- **Wave 0 (scaffolds):** the four test artifacts exist/extend pinned to the Phase-19.1 TARGET API (residentsForHouse, HouseInstance.residents, DerivedSnapshot.residentCount/immigration/homeless/wageBand/unemploymentBand, labor.ts exports, getLaborSectors, setLaborSectorState); they are RED where the implementing wave has not landed; targeted vitest runs parse on compiled-on-the-fly.
- **Wave 1 (POP-01):** `npx vitest run tests/unit/population.test.ts tests/determinism/population-determinism.test.ts tests/golden tests/determinism -x` green on the residency paths; getStateJson byte-identity + goldens untouched.
- **Wave 2 (POP-02):** migration unit + determinism (refill byte-identity, zero-delta-full-city) green; full determinism + golden folders green.
- **Wave 3 (POP-03):** labor-sectors.test.ts (ordering/scarcity/pin-reserve/pause/restore/surplus/SaveCommand-roundtrip/security) green; labor.test.ts invariants + saveCodec.test.ts + security.test.ts + tests/golden green; winnability probe green (A3).
- **Wave 4 (POP-04):** population.test.ts wage + advisors.test.ts labor-panel rows + empty-city compose green; goldens untouched.
- **Wave 5 (close):** `npm run typecheck && NODE_OPTIONS="--max-old-space-size=4096" npx vitest run --maxWorkers=4 && npm run check:military && test -z "$(git status --porcelain tests/golden)"` — the authoritative phase gate.
</verification>

<success_criteria>
- Population is modeled per residence: internal per-house residents (class/age/employment/children/elderly + tax/service/sentiment/crime-health context), consistent with effectivePopulation, deterministic + replay-byte-identical, surfaced in DerivedSnapshot + the residence inspector (real rows, no fabricated 'plebeian'), zero serialized-state growth, zero golden delta (POP-01).
- Migration is modeled: netMigration on the month cadence with a live attractiveness index (desirability/services/unemployment/vacancy), internal-only occupancy deltas, famine emigration → vacancy → refill, homelessness counted, no walker spawning, goldens unchanged (POP-02).
- Labor sectors: priority 1-5 allocateWorkers over the worker pool replacing greedy tickLabor, pinned reserve under scarcity, pause/restore-auto, all labor.test.ts invariants preserved, winnability probe green, and pin/pause survives save/load via the validated replayable setLaborSectorState SaveCommand (POP-03).
- Urban wage policy: wageBand vs a module-local imperial reference + unemploymentBand reported in DerivedSnapshot and the labor advisor panel, pure projections, empty-city-safety, no behavior change, no golden delta (POP-04).
- Deterministic byte-identity + security held end-to-end: no wall-clock in new paths, all mutation in tick()/%40/command handlers, toBuildingState/getStateJson untouched, malformed setLaborSectorState rejected before replay; full suite + typecheck + check:military green together and `tests/golden` byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-SUMMARY.md` when done, mapping the per-requirement evidence (POP-01..04 suites) and noting any [ASSUMED] tuning deviation (A1/A2/A3/A5) made to keep a suite green. This is the last phase closing the v1.0 milestone audit gap (POP-01..04 unwired).
</output>
