---
phase: 19.1-population-labor
plan: 19.1-PLAN.md
verifier: gsd-verifier (adversarial goal-backward verification)
date: 2026-08-06
status: passed
score: 4/4 goals verified
---

# Phase 19.1: Population & Labor — Verification Report

## Summary

Phase 19.1's goal — wire the previously orphaned population module per-residence (per-residence population, month-cadence migration, labor sectors with priority allocation, urban wage policy) — is **achieved**. All 4 goals (POP-01..04) verified against the actual codebase, not the summary. All milestone-gap constraints hold: goldens byte-unchanged (`git status --porcelain tests/golden` empty), getStateJson byte-identical replay proven by tests, HouseInstance internals never serialized (confirmed in `toBuildingState`), no Math.random/Date.now in the new sim paths (source-audit test), and full suite + typecheck + check:military green.

Independent runs of the gates:
- `npm run typecheck` — green (tsc --noEmit, exit 0)
- `NODE_OPTIONS="--max-old-space-size=4096" npx vitest run --maxWorkers=4` — **121 files / 969 tests passed** (2 "onTaskUpdate" worker RPC timeouts are the documented pre-existing vitest infra noise; test count 969 > summary's 966, consistent with the 3 post-summary CR-01/WR regression tests)
- `npm run check:military` — clean
- `git status --porcelain tests/golden` — EMPTY (goldens byte-unchanged)
- All 7 phase commits (4a411af, 74743b0, 960e3ec, 51c4720, 5c4af6e, f22cff6, a04446e) plus the 5 code-review fix commits (4a69cd9 CR-01, 1709d30 WR-01, 1decdf0 WR-02, 9b95791 IN-03, 842c758 IN-04) present in `git log`.

## Goals vs Evidence

### POP-01: Per-residence population model — PASS

**Goal:** Per-residence population (residents with class/age/employment; derived deterministically; surfaced end-to-end incl. inspectors/HUD; no SaveData growth).

| Evidence | Verdict |
| --- | --- |
| `src/sim/population.ts:161` `residentsForHouse` builds exactly-`effectivePopulation` cohorts; class from `patricianFraction` (tier ≥ 3, `population.ts:134`); ages from mulberry32 seeded by house-stable salt (`runner.ts:671`) — never Math.random | ✓ |
| `src/sim/population.ts:186` `ageOnMonth` ages every resident +1 per %40 month | ✓ |
| `src/sim/runner.ts:660` `tickResidency()` runs on `tickCount % 40` (runner.ts:364-368) right after `tickHousingMerge`; re-derives on level/merge change | ✓ |
| `src/sim/walkers.ts:128-146` `HouseInstance.residents/nextResidentId/residentsDerivedLevel/residentsDerivedCapacity/starvedMonths` — internal-only; `toBuildingState` (runner.ts:3536-3577) serializes NONE of them (only tier/level/cooldowns/services/happiness/etc.) ⇒ no SaveData growth | ✓ |
| `src/sim/advisors.ts:302-323` `residenceInspection` appends `residents {count, classBreakdown, ageBands, employed}` when live cohort present (old shape preserved for minimal calls) | ✓ |
| `src/game/scenes/HUDScene.ts:903-947` renders real Class / Age Bands / Employed rows from live cohort — fabricated `('plebeian', full-capacity)` call removed | ✓ |
| `DerivedSnapshot.residentCount/residentsByClass` (runner.ts:1550-1551) sum the SAME live residents (WR-02 fix, runner.ts:1572-1590) | ✓ |
| Tests: `tests/unit/population.test.ts:63-113` (cohort count/class/age bands/aging/wiring), `tests/determinism/population-determinism.test.ts:37-95` (getStateJson byte-identity, getInspector round-trip, residentCount consistency, chunked 1/7/50) | ✓ |

### POP-02: Migration wired on the month cadence — PASS

**Goal:** vacancy-bounded migration, famine emigration → refill, homeless count; golden-neutral (0-delta when full); no walker spawning.

| Evidence | Verdict |
| --- | --- |
| `runner.ts:694` `tickPopulationMigration()` on `%40` hook (runner.ts:367); attractiveness blend from ALREADY-derived inputs (desirability/civic coverage/unemployment — no second recompute) | ✓ |
| `population.ts:65` `netMigration` vacancy-bounded by `capacityAvailable`; `runner.ts:744-750` computes capacityAvailable from internal occupancy; full city ⇒ 0 ⇒ golden-neutral | ✓ |
| `runner.ts:724-739` famine emigration: `starvedMonths >= FAMINE_EMIGRATION_MONTHS` + `foodShortageEffects(...).emigration` drains residents, increments `emigration` + `homeless`; fed month resets counter | ✓ |
| `runner.ts:751-763` positive-attractiveness months refill vacancy deterministically via `bookResidents` (own seeded RNG, sequential nextResidentId, workforce-age immigrants) | ✓ |
| `DerivedSnapshot.immigration/emigration/homeless` per-month deltas (runner.ts:1554-1556, `lastMigration`) | ✓ |
| No walker spawning: migration mutates only internal residents; no walker-creation code in the path (A7 respected) | ✓ |
| Tests: `tests/unit/population.test.ts:115-152` (vacancy-bound, full-city zero-delta, famine drain); `tests/determinism/population-determinism.test.ts:97-176` (full-city 0-delta + byte-identical round-trip, famine determinism via two identical runs, per-month deltas) | ✓ |

### POP-03: Labor sectors — PASS

**Goal:** replace greedy tickLabor with sector-priority allocateWorkers; pin/pause/restore-auto as replayable SaveCommand; reserve-guard pinning; getLaborSectors getter; preserve labor.test.ts invariants.

| Evidence | Verdict |
| --- | --- |
| `src/sim/labor.ts` new pure module: `LABOR_SECTOR_PRIORITY` food 1…culture 5 (line 24), `SECTOR_IDS` (line 33), type-based `TYPE_SECTOR` incl. `palatine: 'commerce'` (line 80, CR-01 fix), `buildLaborSectors` (paused ⇒ needed=0, line 137), `applySectorAssignments` per-building caps (line 150) | ✓ |
| `runner.ts:2984` `tickLabor` rewritten: pinned sectors reserve prior `laborSectorAssigned` first (reserve-guard, line 3002-3010), then priority-order fill incl. pinned sectors (WR-01, line 3016-3022), `applySectorAssignments` + `setActive` activation, per-sector tracking (line 3032) | ✓ |
| `runner.ts:3040` `getLaborSectors()` reads `assigned` from the runner's per-sector store (never fabricates — the in-flight `assigned: 0` bug fixed in 51c4720) | ✓ |
| `runner.ts:3062` `setLaborSectorState(sector, {pinned?, paused?})`: unknown sector → `{ok:false,error:'unknown-sector'}`, non-boolean flag → `invalid-config`; paused ⇒ enqueue-and-apply-on-resume; partial-flag merge into private `laborSectorCfg`; push-on-accept recording | ✓ |
| Replayable SaveCommand: `src/sim/types.ts:104` union member `{kind:'setLaborSectorState'; sector; pinned?; paused?}`; `src/sim/saveCodec.ts:143-147` `validateCommand` rejects non-string sector/non-boolean flags as `malformed-command`; `runner.ts:3637-3638` exhaustive `applyCommand` branch | ✓ |
| labor.test.ts invariants preserved: `assigned ≤ pool`, per-building `assigned ≤ required`, surplus fills all — `tests/unit/labor.test.ts` (untouched, dated pre-phase) + surplus fill-all test (`labor-sectors.test.ts:243-252`) all green in full suite | ✓ |
| Tests: `tests/unit/labor-sectors.test.ts` — priority ordering (110-124), getLaborSectors rows (127-160), pin reserve under shrunken pool (163-186), WR-01 sticky-zero fix (188-204), pause/restore-auto (207-241), SaveCommand round-trip through getSaveData→migrate→validate→fromSaveData incl. pinned/paused (254-295), security rejections (297-319), CR-01 palatine regression (353-377) | ✓ |

### POP-04: Urban wage policy — PASS

**Goal:** wageBand vs module-local imperial reference + unemploymentBand as pure DerivedSnapshot reporting + labor advisor panel rows; NO treasury/wage behavior change.

| Evidence | Verdict |
| --- | --- |
| `population.ts:193` `IMPERIAL_WAGE_REFERENCE = 0.3` (module-local, inside (0,1)); `wageBand` (line 100) total function with guarded reference (IN-03 fix: ref 0 → band comparison uses the guarded ref) | ✓ |
| `population.ts:198` `unemploymentBand` clamped total function (healthy/moderate/high at 0.15/0.35) — empty-city safe | ✓ |
| `runner.ts:1560-1564` DerivedSnapshot `wageBand` (vs policy.wageRate) + `unemploymentBand` (from already-computed unemploymentRate) — derived-only, `getStateJson` serializes `getState` not this snapshot (runner.ts:1547 comment) ⇒ no golden impact | ✓ |
| `src/game/advisors.ts:197-214` labor panel gains 'Wage vs Imperial' (`below/at/above (NN%)`, bad/ok tone) + 'Unemployment Band' rows composed from `getDerived()`; optional-chaining guards for empty city (Pitfall 5) | ✓ |
| NO treasury/wage behavior change: wageBand/unemploymentBand are read-only projections; finance panel and `ADVISOR_TAB_ORDER` untouched (confirmed by advisors diff scope) | ✓ |
| Tests: `tests/unit/advisors.test.ts:494-521` (Wage vs Imperial + Unemployment Band rows traced to getDerived; empty-city compose-without-throw); `tests/unit/population.test.ts:180-200` (wageBand/unemploymentBand unit + live DerivedSnapshot cases) | ✓ |

## Truth Tests

| Statement | Evidence | Verdict |
| --- | --- | --- |
| Goldens byte-unchanged | `git status --porcelain tests/golden` → empty; full suite green | ✓ PASS |
| getStateJson() byte-identical replay (residents + migration) | `population-determinism.test.ts:38-51, 148-164` (save/load byte-identity); chunked 1/7/50 (76-94) | ✓ PASS |
| Labor-sectors round-trip covers pinned/paused | `labor-sectors.test.ts:255-295` (pin food + pause commerce issued pre-window, full getLaborSectors equality after fromSaveData) | ✓ PASS |
| Internal-only HouseInstance state never serialized | `runner.ts:3536-3577` `toBuildingState` excludes residents/nextResidentId/residentsDerived*/starvedMonths; determinism tests confirm byte-identity | ✓ PASS |
| No Math.random / Date.now in new sim paths | `population-determinism.test.ts:178-200` source audit (population.ts/labor.ts clean; runner.ts ≤1 Date.now, the savedAt metadata line); verified by grep over the new code | ✓ PASS |
| No walker spawning for migration | `tickPopulationMigration`/`bookResidents` mutate only `h.residents`; no walker spawn call in the path (A7) | ✓ PASS |
| Full suite + typecheck + check:military green | All three run independently: 121 files/969 tests, tsc clean, military clean | ✓ PASS |
| No debt markers in phase files | grep TBD/FIXME/XXX/PLACEHOLDER over phase files → 0 matches | ✓ PASS |

## Verification Result

**Overall: PASS** — 4/4 goals verified with codebase evidence. All phase goals (POP-01..04) and all milestone-gap constraints (goldens, byte-identity, internal-only state, no wall-clock/RNG, green gates) hold. No FAIL or PARTIAL items. The 2 vitest "onTaskUpdate" worker RPC timeouts are pre-existing infra noise (the run reports 121/121 files, 969/969 tests passed); the summary's "966 passed" is superseded by 969 (3 post-summary regression tests from the CR-01/WR fix commits).

---

_Verified: 2026-08-06_
_Verifier: gsd-verifier (adversarial, goal-backward; read-only + independent gate runs)_
