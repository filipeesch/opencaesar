You are RESUME-executing Plan 19.1 of Phase 19.1 (population-labor) in OpenCaesar at /Users/filipe.esch/projects/pessoal/opencaesar.

A prior executor run was interrupted after completing Waves 0-2. Current state:
- Committed: 4a411af (Wave 0 scaffolds), 74743b0 (Wave 1 tracer POP-01 per-residence), 960e3ec (Wave 2 POP-02 migration).
- In-flight uncommitted: src/sim/labor.ts (NEW) + src/sim/runner.ts (modified) — partial work on task 19.1-03-01 (labor sectors allocateWorkers).
- `npx tsc --noEmit` currently FAILS (expected mid-waves): wageBand/unemploymentBand not yet on DerivedSnapshot (Wave 4), setLaborSectorState not yet on SimRunner (Wave 3), scaffolds reference future APIs.

YOUR JOB: assess the in-flight 19.1-03-01 labor work (git diff, src/sim/labor.ts), complete it to the plan spec, then execute the remaining tasks IN ORDER per the plan XML, commit each atomically, and finish:
1. Complete 19.1-03-01: src/sim/labor.ts (sector priority map + grouping + distribution overriding the greedy tickLabor while PRESERVING labor.test.ts invariants: assigned ≤ pool, assigned ≤ required, surplus fills all) + reserve-semantics pinning + getLaborSectors(). Commit feat(19.1): ...
2. 19.1-03-02: setLaborSectorState SaveCommand (pin/pause/restore-auto) — union branch types.ts + runner handler + exhaustive applyCommand + saveCodec validateCommand case (typed {ok:false} reject). Commit feat(19.1): ...
3. 19.1-04-01: urban wage policy — wageBand vs module-local imperial reference + unemploymentBand in DerivedSnapshot + labor advisor panel rows (temperature of the advisors composer). Commit feat(19.1): ...
4. 19.1-05-01: close gate — full suite + typecheck + check:military green, goldens byte-unchanged (git status --porcelain tests/golden empty), determinism replay for residents+migration+sectors proven. Commit docs(19.1): complete.
5. Update STATE.md + ROADMAP.md, write 19.1-SUMMARY.md, commit it.

CRITICAL constraints:
- Work DIRECTLY in this folder (workflow.use_worktrees=false — no worktrees/temp branches/recovery sentinels).
- GOLDEN-SAFE: new per-residence/migration/labor/wage state on HouseInstance INTERNALS (never serialized; toBuildingState must NOT copy them) + DerivedSnapshot report fields (not serialized). getStateJson() byte-identical; goldens UNCHANGED. All mutations in tick() %40 hooks + SaveCommand handlers.
- Preserve labor.test.ts invariants exactly.
- Pin semantics = reserve-guard (pinned sectors filled before general pool split).
- residentsForHouse uses mulberry32 (never Math.random).
- No Math.random/Date.now/new Date in sim (runner savedAt: Date.now() is pre-existing, not a violation).
- Wage reporting only — no treasury/wage behavior change.
- Balance-parity: module-local constants; no new data/balance.ts keys.
- Test commands per the plan's <verify><automated> blocks. Full suite: NODE_OPTIONS="--max-old-space-size=4096" npx vitest run --maxWorkers=4. Military: npm run check:military. Typecheck: npm run typecheck.
- Follow the plan file's task XML precisely: .planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-PLAN.md (tasks at lines ~299-460).

Do NOT return the summary content — confirm completion with the commit list and full-suite count. Conclude with ## PLAN 19.1 COMPLETE or ## EXECUTE BLOCKED + reason.
