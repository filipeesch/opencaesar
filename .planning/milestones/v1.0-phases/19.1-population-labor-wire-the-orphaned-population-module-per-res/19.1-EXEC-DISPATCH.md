You are executing Plan 19.1 of Phase 19.1 (population-labor) in OpenCaesar at /Users/filipe.esch/projects/pessoal/opencaesar (Phaser 3 + TypeScript city sim, deterministic core).

<objective>
Execute plan 19.1 of phase 19.1-population-labor. Commit each task atomically. Create SUMMARY.md. Update STATE.md and ROADMAP.md with plan progress.
</objective>

<sequential_execution>
You are running as a SEQUENTIAL executor agent on the main working tree. Use normal git commits (with hooks). Do NOT use --no-verify.
REQUIRED ORDER: Write SUMMARY.md → commit → only then any narration. No text between Write and commit.
IMPORTANT: .planning/config.json sets workflow.use_worktrees: false — work DIRECTLY in this project folder. Do NOT create git worktrees, temp branches, or recovery sentinels.
</sequential_execution>

<execution_context>
@/Users/filipe.esch/projects/pessoal/opencaesar/.opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/projects/pessoal/opencaesar/.opencode/gsd-core/templates/summary.md
@/Users/filipe.esch/projects/pessoal/opencaesar/.opencode/gsd-core/references/checkpoints.md
@/Users/filipe.esch/projects/pessoal/opencaesar/.opencode/gsd-core/references/tdd.md
</execution_context>

<files_to_read>
Read these files at execution start using the Read tool. Resolve repo root first: PROJECT_ROOT=$(git rev-parse --show-toplevel)
- ${PROJECT_ROOT}/.planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-PLAN.md (Plan)
- ${PROJECT_ROOT}/.planning/PROJECT.md (Project context)
- ${PROJECT_ROOT}/.planning/STATE.md (State)
- ${PROJECT_ROOT}/.planning/config.json (Config — use_worktrees false)
- ${PROJECT_ROOT}/.planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-CONTEXT.md (User decisions)
- ${PROJECT_ROOT}/.planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-RESEARCH.md (Research)
- ${PROJECT_ROOT}/.planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-PATTERNS.md (Pattern map)
- ${PROJECT_ROOT}/AGENTS.md (Project instructions, if exists)
</files_to_read>

<project_rules>
- TypeScript city sim at /Users/filipe.esch/projects/pessoal/opencaesar. Phaser 3 view-only; sim core deterministic.
- Full suite: NODE_OPTIONS="--max-old-space-size=4096" npx vitest run --maxWorkers=4. Typecheck: npm run typecheck. Military: npm run check:military.
- Determinism is critical: NO Math.random()/Date.now()/new Date() in src/sim paths (runner.ts savedAt: Date.now() at save is pre-existing, NOT a violation). SimState/getStateJson() byte-identical replay; GOLDEN FIXTURES MUST NOT CHANGE.
- CRITICAL GOLDEN-SAFE WIRING: new per-residence/migration/labor-sector/wage state goes on HouseInstance INTERNALS (precedent: combinedPopulation/civic — NEVER serialized; toBuildingState must NOT copy them) + DerivedSnapshot report fields (getDerived is NOT serialized) + inspector seam (getInspector/getWalkerInternals). getStateJson() = JSON.stringify(getState()) must stay byte-identical. All mutations confined to tick() %40 hooks + SaveCommand handlers (fromSaveData replays commands + re-ticks).
- Migration: netMigration bounded by vacancy (capacity − occupancy); houses today are always-full → zero delta in golden scenarios (goldens unchanged). Famine emigration (housing.ts:409) → vacancy → refill is the only existing vacancy source. POP-02 sponsors NO walker spawning. Run on tickCount % 40 cadence (precedent tickHousingMerge).
- Labor rework (highest risk): replace greedy tickLabor (runner.ts:2729-2744) with sector-grouped allocateWorkers over workerPool (economy.ts). PRESERVE labor.test.ts invariants (assigned ≤ pool, assigned ≤ required, surplus fills all). Pin semantics = reserve-guard (pinned sectors filled before general pool split; allocateWorkers' pure pinned branch alone does NOT guarantee a minimum).
- setLaborSectorState SaveCommand (pin/pause/restore-auto): union branch (types.ts:75-99) + exhaustive applyCommand + validateCommand case (saveCodec.ts reject unknown sector/non-finite priority, typed {ok:false}) + push-on-accept with paused-enqueue (setPolicy precedent runner.ts:1893-1906).
- Wage policy (POP-04) is PURE REPORTING: wageBand reuse + module-local imperial reference + unemploymentBand in DerivedSnapshot + labor advisor panel (game/advisors.ts:188-202). NO treasury/wage behavior change → goldens unaffected.
- residentsForHouse uses mulberry32 (seeded, deterministic) — NEVER Math.random.
- UI/advisor: no fabricated data; residenceInspection appends fields only when defined; replace fabricated 'plebeian' in HUDScene with real internals; textContent/createElement (XSS-safe).
- Balance-parity: module-local constants; no new data/balance.ts keys without CONFIG consumer.
- ADDITIVE-ONLY API changes.

The plan has 6 waves / 7 tasks:
- Wave 0 (19.1-00-01): scaffolds — tests/unit/labor-sectors.test.ts (NEW), tests/determinism/population-determinism.test.ts (NEW), extend tests/unit/population.test.ts + tests/unit/advisors.test.ts. Typecheck sequencing: scaffolds import future APIs — per-wave typecheck red-but-expected until their waves; final wave is the authoritative typecheck gate (use the plan's per-task <verify> commands).
- Wave 1 (POP-01 tracer 19.1-01-01): per-residence internal population on HouseInstance (residents: class/age/employment/etc. via residentsForHouse/ageOnMonth mulberry32) consistent with effectivePopulation; tickResidency on %40; DerivedSnapshot.residentCount/residentsByClass; residenceInspection + HUDScene real residency rows.
- Wave 2 (POP-02, 19.1-02-01): tickPopulationMigration %40 with live attractiveness, vacancy-bounded netMigration (0-delta when full), famine emigration→refill, homeless count.
- Wave 3 (POP-03, 19.1-03-01 labor.ts allocateWorkers + reserve pinning + getLaborSectors; then 19.1-03-02 setLaborSectorState SaveCommand).
- Wave 4 (POP-04, 19.1-04-01): wageBand + unemploymentBand in DerivedSnapshot + labor advisor panel rows.
- Wave 5 (19.1-05-01): close — full suite + typecheck + check:military green, goldens byte-unchanged (git status --porcelain tests/golden empty), determinism replay for residents+migration+sectors proven.

Follow the plan file's task XML precisely: each task has read_first/action/verify/acceptance_criteria/done. Commit each task atomically with repo-convention messages (feat(19.1): ..., fix(19.1): ..., test(19.1): ..., docs(19.1): ...). After all tasks, update STATE.md and ROADMAP.md plan progress, write SUMMARY.md, and commit it.
</project_rules>

<success_criteria>
- [ ] All 7 tasks executed
- [ ] Each task committed individually
- [ ] All 6 waves completed in order
- [ ] SUMMARY.md created and committed
- [ ] STATE.md updated with position and decisions
- [ ] ROADMAP.md updated with plan progress
</success_criteria>
