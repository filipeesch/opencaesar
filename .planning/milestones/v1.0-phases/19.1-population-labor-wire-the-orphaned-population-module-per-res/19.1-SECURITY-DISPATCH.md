You are running the security audit for Phase 19.1 (population-labor) of OpenCaesar at /Users/filipe.esch/projects/pessoal/opencaesar.

## Task
- Read the phase plan .planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-PLAN.md and its threat-model section (STRIDE register).
- Verify each threat mitigation exists in the implemented code (read src/sim/labor.ts, src/sim/runner.ts setLaborSectorState handler + applyCommand, src/sim/saveCodec.ts validateCommand, src/game/advisors.ts + HUDScene.ts UI composition).
- Key expectations: setLaborSectorState validated in handler (unknown sector -> {ok:false,error:'unknown-sector'}, boolean-only flags -> invalid-config); saveCodec validateCommand rejects malformed (non-string sector / non-boolean flags) as 'malformed-command' BEFORE fromSaveData replay (never a raw throw on corrupt save); UI uses textContent/createElement (no innerHTML); no wall-clock/random in sim paths (source-audit test enforces); mutation confined to %40 tick hooks + command handlers (toBuildingState/getStateJson byte-identical).
- If a mitigation is missing, note it (do NOT fix code — report only).
- Write SECURITY.md at .planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-SECURITY.md with frontmatter (phase, status: SECURED or OPEN_THREATS or ESCALATE, threats_verified: N) and sections: ## Summary, ## Threat Verification (per threat: threat, mitigation, status, evidence file:line), ## Open Items.
- Commit it (docs(19.1): ...).

## Report
Return ONLY: verdict (SECURED / OPEN_THREATS / ESCALATE), threats verified count, and any open items.
