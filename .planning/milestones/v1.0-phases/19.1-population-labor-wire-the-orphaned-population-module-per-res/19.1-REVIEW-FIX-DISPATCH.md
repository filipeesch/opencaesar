You are applying fixes for the code review of Phase 19.1 (population-labor) in OpenCaesar at /Users/filipe.esch/projects/pessoal/opencaesar.

## Context
- .planning/config.json sets workflow.use_worktrees: false — work DIRECTLY in this project folder. Do NOT create git worktrees, temp branches, or recovery sentinels.
- Review file: /Users/filipe.esch/projects/pessoal/opencaesar/.planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-REVIEW.md
- Fix ALL Critical and Warning findings; fix Info findings only if trivial/safe. Write the fix log to .planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-REVIEW-FIX.md.
- Commit each fix atomically (or coherently grouped), with repo-convention messages (fix(19.1): ...).

## Findings to fix
1. CR-01 (src/sim/labor.ts:46-88,149-172; src/sim/runner.ts:3000-3004) — `palatine` (workers: 40) is the only job-bearing building missing from `TYPE_SECTOR`; new tickLabor/applySectorAssignments zeroes its workersAssigned every tick, no sector re-assigns it → permanently deactivated, 40 jobs vanish from sector reporting — regression vs legacy tickLabor. Fix: map palatine to the appropriate sector + add a regression test pinning that palatine keeps staffing.
2. WR-01 (src/sim/runner.ts:2982-2996) — pinned reserve uses prior laborSectorAssigned only; pinning an unstaffed sector is sticky-zero (never regains workers under surplus pool) violating "surplus fills all" for that sector. Fix reserve semantics so a pinned sector with surplus available CAN gain workers up to required while still honoring the reserve guard for other pinned sectors.
3. WR-02 (src/sim/runner.ts:1546-1547,1566-1577) — DerivedSnapshot.residentCount is capacity-based while residentsByClass sums live internal residents → disagree after famine emigration (class rows sum < residentCount). Fix so residentCount reflects live residents (or document+align the semantics; prefer making residentCount = live residents sum consistent with residentsByClass).
4. Info findings: apply only if trivial and safe.

## Constraints (unchanged)
- Determinism sacred: no Math.random/Date.now/new Date in src/sim; residentsForHouse mulberry32 only.
- getStateJson() byte-identical; toBuildingState must NOT copy new internals; goldens byte-unchanged (git status --porcelain tests/golden must stay empty).
- New mutations only in %40 tick hooks and SaveCommand handlers.
- Labor invariants (labor.test.ts): assigned <= pool, assigned <= required, surplus fills all.
- setLaborSectorState validateCommand rejects malformed before replay; unknown sector -> {ok:false}.
- Wage policy pure reporting; no treasury/wage behavior change.
- UI: textContent/createElement, no fabricated data.
- After fixes: npm run typecheck + NODE_OPTIONS="--max-old-space-size=4096" npx vitest run --maxWorkers=4 (full suite) + npm run check:military must be green.

## Report
Return ONLY: the list of fixes applied with commit hashes and per-finding status, plus the full-suite test count. Conclude with ## FIXER COMPLETE or ## FIXER BLOCKED + reason.
