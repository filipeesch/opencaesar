You are verifying Phase 19.1 (population-labor) of OpenCaesar at /Users/filipe.esch/projects/pessoal/opencaesar.

## Task
Verify that Phase 19.1 actually delivers what the phase promised — goal-backward analysis, not just task completion. Write VERIFICATION.md at .planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-VERIFICATION.md with frontmatter (phase, plan, verifier, date) and sections: ## Summary, ## Goals vs Evidence (per goal: goal, evidence, verdict), ## Truth Tests (tables of statements/evidence/verdict), ## Verification Result (overall pass/fail).

## Phase goals (from ROADMAP.md "### Phase 19.1: Population & Labor" — read it for exact wording)
Wire the previously orphaned population module per-residence, per the milestone audit gap:
- POP-01: Per-residence population model (residents with class/age/employment; derived deterministically; surfaced end-to-end incl. inspectors/HUD; no SaveData growth).
- POP-02: Migration wired on the month cadence — vacancy-bounded, famine emigration → refill, homeless count; golden-neutral (0-delta when full); no walker spawning.
- POP-03: Labor sectors — replace greedy tickLabor with sector-priority allocateWorkers; pin/pause/restore-auto as replayable SaveCommand; reserve-guard pinning; getLaborSectors getter; preserve labor.test.ts invariants (assigned ≤ pool, assigned ≤ required, surplus fills all).
- POP-04: Urban wage policy — wageBand vs module-local imperial reference + unemploymentBand as pure DerivedSnapshot reporting + labor advisor panel rows; NO treasury/wage behavior change.
- Milestone-gap constraints: goldens byte-unchanged; getStateJson() byte-identical replay; internal-only HouseInstance state (never serialized); no Math.random/Date.now in sim; full suite + typecheck + check:military green.

## Evidence to check (read the code, do not trust the summary)
- src/sim/population.ts (residentsForHouse/ageOnMonth/wage helpers), src/sim/labor.ts (TYPE_SECTOR incl. palatine after CR-01 fix, buildLaborSectors, applySectorAssignments), src/sim/runner.ts (tickResidency/tickPopulationMigration/tickLabor/setLaborSectorState/getLaborSectors stores), src/sim/types.ts (command union member), src/sim/saveCodec.ts (validateCommand case), src/sim/advisors.ts (residenceInspection), src/game/advisors.ts (Wage vs Imperial + Unemployment Band rows), src/game/scenes/HUDScene.ts (real residency rows).
- Tests: tests/unit/population.test.ts, tests/unit/labor-sectors.test.ts (incl. palatine regression test from fix commit 4a69cd9), tests/determinism/population-determinism.test.ts, tests/unit/labor.test.ts, tests/unit/advisors.test.ts, tests/unit/saveCodec.test.ts.
- Run: npm run typecheck; NODE_OPTIONS="--max-old-space-size=4096" npx vitest run --maxWorkers=4 (note: 2 vitest "onTaskUpdate" worker RPC timeouts are pre-existing infra noise — verify the summary count ~969 passed); npm run check:military; git status --porcelain tests/golden must be EMPTY.
- Determinism: confirm tests/determinism/population-determinism.test.ts proves byte-identity replay for residents + migration; labor-sectors round-trip covers pinned/paused.

## Output rules
- Every verdict: PASS / FAIL / PARTIAL with evidence (file:line).
- If something is unverifiable, mark it and explain why.
- Do NOT modify any code. Read-only + test runs.
- Return in your reply ONLY: overall verdict (PASS/FAIL/PARTIAL), per-goal verdict list, and any FAIL/PARTIAL items with one-line reasons.
