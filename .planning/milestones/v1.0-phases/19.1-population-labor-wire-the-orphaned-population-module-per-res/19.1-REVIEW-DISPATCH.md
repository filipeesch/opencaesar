You are performing a code review for Phase 19.1 (population-labor) in OpenCaesar at /Users/filipe.esch/projects/pessoal/opencaesar.

## Scope
Review the source files changed during phase 19.1 (git diff 2add081..HEAD). The relevant source files are:
- src/sim/labor.ts (NEW — sector priority map, TYPE_SECTOR, buildLaborSectors, applySectorAssignments, allocateWorkers integration)
- src/sim/runner.ts (tickResidency, tickPopulationMigration, tickLabor rewrite, setLaborSectorState handler, getLaborSectors, laborSectorCfg/laborSectorAssigned stores)
- src/sim/population.ts (residentsForHouse, ageOnMonth, IMPERIAL_WAGE_REFERENCE, unemploymentBand, wageBand helpers)
- src/sim/types.ts (setLaborSectorState command union member + sector type)
- src/sim/saveCodec.ts (validateCommand case for setLaborSectorState)
- src/sim/advisors.ts (residenceInspection enrichment)
- src/game/advisors.ts (labor panel Wage vs Imperial + Unemployment Band rows)
- src/game/scenes/HUDScene.ts (residency rows replacing fabricated 'plebeian')

## Context / constraints to check against
- Determinism is sacred: NO Math.random()/Date.now()/new Date() in src/sim paths. residentsForHouse must use mulberry32 (seeded). getStateJson() must stay byte-identical; toBuildingState must NOT copy new internals (residents, residentsDerivedLevel, residentsDerivedCapacity, starvedMonths, laborSectorCfg, laborSectorAssigned, homeless).
- Goldens must remain byte-unchanged (no fixture regeneration).
- New mutations only in tick() %40 hooks (tickResidency, tickPopulationMigration after tickHousingMerge) and SaveCommand handlers (push-on-accept + paused-enqueue, setPolicy precedent).
- Labor invariants (labor.test.ts): assigned <= pool, assigned <= required, surplus fills all. Pin semantics = reserve-guard at runner level; the pure allocateWorkers pinned branch is NOT to be relied on for guarantees.
- setLaborSectorState: union branch + exhaustive applyCommand + saveCodec validateCommand rejecting malformed before replay; unknown sector -> {ok:false}; paused => enqueue-and-apply-on-resume; live+replay merge partial flags.
- DerivedSnapshot additions (residentCount, residentsByClass, immigration/emigration/homeless, wageBand, unemploymentBand) are derived-only reports.
- Wage policy is pure reporting — NO treasury/wage behavior change.
- UI: textContent/createElement (XSS-safe), no fabricated data.
- Module-local constants only; no new data/balance.ts keys.

## Task
- Read each changed source file and its tests (tests/unit/population.test.ts, tests/unit/labor-sectors.test.ts, tests/determinism/population-determinism.test.ts, tests/unit/advisors.test.ts, tests/unit/labor.test.ts, tests/unit/saveCodec.test.ts).
- Find bugs, security issues, and code-quality problems. Classify each finding CRITICAL / WARNING / INFO with file:line references and a clear description of the defect and its consequence.
- Write the findings report to /Users/filipe.esch/projects/pessoal/opencaesar/.planning/phases/19.1-population-labor-wire-the-orphaned-population-module-per-res/19.1-REVIEW.md following this structure: # Code Review: Phase 19.1 ... (frontmatter: phase, status: needs-fix, findings: N) ... then ## Summary, ## Findings (severity table), then per-finding sections (ID, severity, location, description, impact, suggested fix).
- Return in your reply ONLY: the findings count by severity (Critical/Warning/Info) and the top 3 most severe findings in 1-2 lines each. Do not paste the whole report.
