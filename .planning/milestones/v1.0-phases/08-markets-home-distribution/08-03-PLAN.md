---
phase: 08-markets-home-distribution
plan: 08-03
type: feature
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - src/sim/logistics.ts
  - tests/integration/market-distribution-priority.test.ts
  - tests/determinism/market-chain-determinism.test.ts
autonomous: true
requirements: [MARK-01, MARK-02, MARK-03]
must_haves:
  truths:
    - "Distribution priority is composable and honored through an additive surface: marketLoadComposition(cfg, marketStock, perFoodCap, capacity, opts) derives the seller's load order from the market config — basic/essential food first, then the evolution-blocking good, then remaining stocked products — skipping refused products, bounded by per-food caps and the 100-unit capacity (MARK-03, decision 5)."
    - "The distribution-priority composition is deterministic and matches the §18.4/§12.11 order: essential food precedes the evolution-blocking good in the composed load, a product refused by the market config never appears in the load, and the load never exceeds capacity (MARK-03, decision 5)."
    - "The market chain is deterministic under chunking: same-seed runner ticks at chunk sizes 1/7/50 yield identical getStateJson() with a per-market config set (setMarketConfig), and a fixed-seed buyer/seller walker sequence yields identical market/granary stock and house marketCoverage across runs — no wall clock, no Math.random anywhere in the market chain (MARK-01/02/03, decision 5 determinism)."
  artifacts:
    - path: src/sim/logistics.ts
      provides: "Additive marketLoadComposition(cfg, marketStock, perFoodCap, capacity, opts) — config-driven seller load ordering (basic food, evolution-blocking good, then rest, skipping refused products) delegating to sellerLoadComposition"
      min_lines: 15
    - path: tests/integration/market-distribution-priority.test.ts
      provides: "Composed distribution-priority tests: essential-food-before-evolution-blocking load order via marketLoadComposition with a market config, refused-product exclusion, capacity/cap bounds, and policyOrder over a realistic market-serving house set"
      min_lines: 30
    - path: tests/determinism/market-chain-determinism.test.ts
      provides: "Market-chain chunked determinism: runner ticks at chunk sizes 1/7/50 with a configured per-market config yield identical getStateJson(); a fixed-seed buyer/seller walker micro-sequence yields identical stock/coverage"
      min_lines: 30
  key_links:
    - from: src/sim/logistics.ts
      to: src/sim/logistics.ts
      via: "marketLoadComposition builds on the existing sellerLoadComposition (logistics.ts:715-734), marketAccepts (214-218), and the MarketConfig default (209-211)"
    - from: tests/determinism/market-chain-determinism.test.ts
      to: src/sim/runner.ts
      via: "uses SimRunner ticks + setMarketConfig (08-02 registry) and the walkers' decideBuyer/decideSeller determinism (walkers.ts:289-340) — mirroring warehouse-logistics-determinism.test.ts chunk pattern"
    - from: tests/integration/market-distribution-priority.test.ts
      to: src/sim/logistics.ts
      via: "composes marketLoadComposition + policyOrder + the 08-01 policy matrix contract over a realistic market state"
---

<objective>
Close the remaining MARK-03 and determinism gaps. RESEARCH found that the
distribution-priority order (essential food first, then the evolution-blocking
good, §18.4/§12.11) is **unproven as composed behavior** — `sellerLoadComposition`
is asserted once with all-priorities (logistics.test.ts:366-376), `policyOrder`
is driven by only 2 of 5 policies (378-387), and **`policyOrder` is never
consumed by production code** (decision 5). Add an additive
`marketLoadComposition(cfg, marketStock, perFoodCap, capacity, opts)` in
`src/sim/logistics.ts` that derives the seller's load order from the market
config (basic food → evolution-blocking good → rest, skipping refused products)
and delegates fill to `sellerLoadComposition`; prove the composed order with a
new integration test (`tests/integration/market-distribution-priority.test.ts`)
and compose `policyOrder` over a realistic market-serving house set. Also,
RESEARCH found **no chunked-tick determinism test for the market chain**
(only food / production / warehouses) — add `tests/determinism/
market-chain-determinism.test.ts` proving same-seed runner ticks at chunk sizes
1/7/50 (with a configured per-market config from 08-02) and a fixed-seed
buyer/seller walker micro-sequence yield identical state.

Builds on 08-01's policy matrix (assumed green) and 08-02's per-market config
registry + SimInternals wiring (assumed green). No existing export renamed or
resemantized; `marketFoodState`/`decideSeller` legacy paths untouched.
Output: `.planning/phases/08-markets-home-distribution/08-03-PLAN.md` plus the
tasks below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-markets-home-distribution/08-CONTEXT.md
@.planning/phases/08-markets-home-distribution/08-RESEARCH.md
@.planning/phases/08-markets-home-distribution/08-VALIDATION.md
@.planning/phases/08-markets-home-distribution/08-01-PLAN.md
@.planning/phases/08-markets-home-distribution/08-02-PLAN.md

Existing: `src/sim/logistics.ts` — MarketConfig (200-207), defaultMarketConfig
(209-211), marketAccepts (214-218), sellerLoadComposition (715-734),
MarketServicePolicy (737), policyOrder (761-775), nextPickPriority (187-197),
SELLER_CAPACITY (576), MARKET_FOOD_CAPS (572-574). `src/sim/runner.ts` —
SimRunner with the 08-02 additive registry setMarketConfig/marketConfig; use
`new SimRunner(seed, map)` + `tick()`/`getStateJson()` (see
tests/determinism/warehouse-logistics-determinism.test.ts for the chunk
pattern). `src/sim/walkers.ts` — decideBuyer (289-318), decideSeller (326-340),
recordMarketVisit (via logistics). Tests: `tests/unit/market-distribution.test.ts`
(08-01), `tests/unit/market-config.test.ts` (08-01),
`tests/unit/market-config-surface.test.ts` (08-02),
`tests/determinism/warehouse-logistics-determinism.test.ts` (chunk pattern).
Baseline: 506 + 08-01/08-02 deltas, typecheck clean. Per-task sampling follows
08-VALIDATION.md.
</context>

<tasks>

<task type="auto">
  <name>Composed distribution priority: marketLoadComposition + policy ordering (MARK-03, decision 5)</name>
  <files>src/sim/logistics.ts, tests/integration/market-distribution-priority.test.ts</files>
  <read_first>src/sim/logistics.ts (MarketConfig 200-207, defaultMarketConfig 209-211, marketAccepts 214-218, sellerLoadComposition 715-734, SELLER_CAPACITY 576, MarketServicePolicy 737, policyOrder 761-775, MarketFoodState 591-604), tests/unit/market-distribution.test.ts (08-01 policy + load matrix), tests/integration/food-slice.test.ts (seller 164-191), 08-CONTEXT.md decision 5</read_first>
  <action>Read src/sim/logistics.ts and the 08-01 distribution matrix before editing. Add one additive export to src/sim/logistics.ts — do not change any existing export (sellerLoadComposition/policyOrder/marketAccepts stay as they are):
  `marketLoadComposition(cfg: MarketConfig, marketStock: Record<string, number>, perFoodCap: Record<string, number>, capacity = SELLER_CAPACITY, opts: { basicFood?: string; evolutionBlocking?: string | null; priorities?: string[] } = {}): Record<string, number>` — build the ordered priority list as [opts.basicFood, opts.evolutionBlocking, ...(opts.priorities ?? [])] plus the remaining stocked products, filter out foods whose marketAccepts(cfg, food, 'plebeian') is false (refused products) and foods with zero stock or zero cap, then delegate to the existing sellerLoadComposition(marketStock, perFoodCap, capacity, ordered). Pure and deterministic.
  Create tests/integration/market-distribution-priority.test.ts importing marketLoadComposition, defaultMarketConfig, policyOrder, MARKET_FOOD_CAPS, SELLER_CAPACITY from '../../src/sim/logistics' plus the HouseServingInfo type: (1) essential-first — cfg default, opts { basicFood: 'wheat', evolutionBlocking: 'vegetables', priorities: [] }, marketStock { wheat: 60, vegetables: 50, fruit: 30 }, perFoodCap MARKET_FOOD_CAPS → the composed load is ordered wheat before vegetables and the total never exceeds SELLER_CAPACITY; (2) evolution-blocking after essential — with wheat stocked and vegetables (the evolution-blocking good) the next fill, an evolutionBlocking entry appears in the load after the basic food; (3) refused exclusion — cfg.productRules.fruit = 'refuse' → fruit never appears in the composed load even when stocked, while wheat/vegetables still load; (4) caps — each food in the load is at most its per-food cap and the total ≤ capacity across all cases; (5) policy composition — build a realistic house set and assert policyOrder('promote-evolution', houses) orders a higher-missingVariety house before a lower one, and 'patrician-reserve' orders a higher-tier house first, matching the 08-01 matrix contract when composed with a market serving them. Keep sellerLoadComposition's own tests (08-01) green unchanged.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/integration/market-distribution-priority.test.ts && npx vitest run tests/unit/market-distribution.test.ts</automated>
  </verify>
  <done>market-distribution-priority.test.ts passes proving the composed distribution priority: marketLoadComposition fills essential food first then the evolution-blocking good, excludes refused products, and respects per-food caps and the 100-unit capacity; policyOrder composes with a realistic house set for promote-evolution and patrician-reserve; the 08-01 matrix (market-distribution.test.ts) stays green; typecheck clean.</done>
</task>

<task type="auto">
  <name>Market-chain chunked determinism: config + buyer/seller sequence (MARK-01/02/03, decision 5)</name>
  <files>tests/determinism/market-chain-determinism.test.ts</files>
  <read_first>tests/determinism/warehouse-logistics-determinism.test.ts (chunk-pattern), tests/helpers.ts (runScenario 12, foodChainMap 26, buildFoodCity 37), src/sim/runner.ts (SimRunner, setMarketConfig 08-02 registry, getStateJson), src/sim/walkers.ts (decideBuyer 289-318, decideSeller 326-340, recordMarketVisit), tests/integration/market-chain.test.ts (08-02)</read_first>
  <action>Read the warehouse-logistics chunk determinism test and the runner API before writing. Add the missing market-chain determinism coverage (only food/production/warehouse chains had chunked tests). Create tests/determinism/market-chain-determinism.test.ts:
  (1) Runner-level chunk identity with a configured per-market config: for seeds {1, 7, 1337}, build a city with a market, a granary, and houses (reuse tests/helpers buildFoodCity or place on foodChainMap), call setMarketConfig(marketId, config) on the configured market (productRules with one refused product + a non-default targetStock), then run the same total ticks (e.g., 200) in three chunked loops — chunk sizes 1, 7, 50 (repeating the last partial chunk to reach the same total, as the warehouse determinism test does) — and assert runner.getStateJson() is identical across all three chunkings for each seed.
  (2) Fixed-seed buyer/seller micro-sequence determinism: step a createWalker('buyer') then a createWalker('seller') via updateWalker for a fixed number of ticks over the same seeded stub (mirroring food-slice.test.ts:77-118) twice from identical state, and assert the resulting market stock, granary stock, and house marketCoverage are identical across the two runs — the decide paths (nextFoodToFetch/pickGranary/sellerLoadComposition) are pure or seeded and must never vary. Assert no Math.random or Date is introduced in the market chain (the identity holds by construction; the register tracks the grep).
  Keep the existing determinism tests (determinism.test.ts, production-chain-determinism.test.ts, warehouse-logistics-determinism.test.ts) green unchanged.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/determinism/market-chain-determinism.test.ts && npm run test</automated>
  </verify>
  <done>market-chain-determinism.test.ts passes proving the market chain is deterministic under chunking: same-seed runner ticks at chunk sizes 1/7/50 with a configured per-market config (refused product + target stock) yield identical getStateJson(), and identical fixed-seed buyer/seller updateWalker sequences yield identical market/granary stock and house marketCoverage; all existing determinism tests stay green; typecheck clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Market config → seller load composition | The composed load must honor essential-then-evolution-blocking priority, exclude refused products, and never exceed capacity — no product silently dropped or mis-ordered |
| Service policy → house ordering | policyOrder must order houses by the configured policy even when composed with a realistic market state |
| Market chain → determinism | The buyer/seller market chain (incl. per-market config) must be byte-identical under chunked ticking and repeatable runs — no wall-clock or RNG influence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Spoofing | distribution-priority misordering / refused leak | medium | mitigate | composed marketLoadComposition tests lock essential→evolution-blocking order, refused-product exclusion, and cap/capacity bounds (market-distribution-priority.test.ts) |
| T-08-04 | Tampering | non-determinism in the market chain | high | mitigate | chunked 1/7/50 + repeat-run identity asserted (market-chain-determinism.test.ts), including the configured per-market config path |
| T-08-05 | Repudiation | policyOrder ignored for house targeting | medium | mitigate | policyOrder composed with a realistic house set for promote-evolution/patrician-reserve (market-distribution-priority.test.ts) |
</threat_model>

<verification>
Run `npm run typecheck` and `npm run test` (full suite, golden + determinism +
property included). Spot-check the two new files:
`npx vitest run tests/integration/market-distribution-priority.test.ts` and
`npx vitest run tests/determinism/market-chain-determinism.test.ts`; confirm the
08-01/08-02 test files and all pre-existing determinism tests stay green.
Per-task sampling follows
`.planning/phases/08-markets-home-distribution/08-VALIDATION.md`.
</verification>

<success_criteria>
- Composed distribution priority proven: essential food → evolution-blocking
  good via marketLoadComposition, refused products excluded, caps + capacity
  respected, policy ordering composed with a realistic house set (MARK-03,
  decision 5).
- Market-chain determinism proven: chunked 1/7/50 identity for a configured
  per-market config runner, and repeat-run identity for the fixed-seed
  buyer/seller sequence (MARK-01/02/03, decision 5 determinism).
- Full suite (baseline + 08-01/08-02/08-03 deltas) green; no existing export
  renamed or resemantized; legacy walker paths untouched.
</success_criteria>

<output>
Create `.planning/phases/08-markets-home-distribution/08-03-SUMMARY.md` when done.
</output>
