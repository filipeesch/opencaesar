---
phase: 06-production-manufacturing
plan: 06-03
type: feature
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - tests/determinism/production-chain-determinism.test.ts
  - tests/integration/production-runner.test.ts
autonomous: true
requirements: [PROD-01, PROD-02]
must_haves:
  truths:
    - "The production chain is deterministic: the same seed, map, and command sequence produce the identical snapshot regardless of how ticks are batched (chunk sizes 1/7/50 → byte-identical getStateJson) (PROD-02, decision 5)."
    - "The production step and advisors use only the seeded RNG, never Math.random or the clock; determinism is enforced by a chunked-tick test, not by inspection (decision 5)."
    - "End-to-end through the runner: extraction requires its deposit, workshops produce and dispatch porters, destination validity holds (needy workshop > warehouse > blocked), and every blocked state preserves goods — verified as acceptance in the live sim (PROD-01/02)."
    - "The full existing suite stays green after Phase 6; the new production tests are additive to the 424-test baseline."
  artifacts:
    - path: tests/determinism/production-chain-determinism.test.ts
      provides: "Chunked-tick determinism test for the production chain — same seed ticked in chunks of 1/7/50 yields identical state; different seeds diverge"
      min_lines: 20
    - path: tests/integration/production-runner.test.ts
      provides: "End-to-end runner acceptance: deposit enforcement, extraction→workshop→porter→warehouse pipeline, bottleneck no-loss, and the full-suite green check"
      min_lines: 20
  key_links:
    - from: tests/determinism/production-chain-determinism.test.ts
      to: tests/helpers.ts
      via: "buildProductionCity / productionChainMap (06-02) script the determinism scenario"
    - from: tests/integration/production-runner.test.ts
      to: src/sim/runner.ts
      via: "acceptance asserts tickProduction + deposit gate + porter conservation end-to-end"
    - from: tests/integration/production-runner.test.ts
      to: tests/determinism/production-chain-determinism.test.ts
      via: "both share the productionChainMap/buildProductionCity scenario surface"
---

<objective>
Final verification wave. (1) Close decision 5: add a chunked-tick determinism test
for the production chain — RESEARCH confirms src/sim/production.ts is RNG/clock-free,
and the runner step (06-02) iterates in stable building order, so the same
seed/map/commands ticked in chunk sizes 1/7/50 must produce byte-identical
`getStateJson()` (mirroring the existing food-city chunked test,
determinism.test.ts:55-76). (2) Add an end-to-end runner acceptance test that ties
every Phase-6 contract together at the live-sim level — deposit enforcement, the
extraction → workshop → porter → warehouse pipeline, destination validity with
warehouse fallback, and blocked-state no-loss — and run the full suite to prove the
whole project stays green.

Output: `.planning/phases/06-production-manufacturing/06-03-PLAN.md` plus the tasks
below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-production-manufacturing/06-CONTEXT.md
@.planning/phases/06-production-manufacturing/06-RESEARCH.md
@.planning/phases/06-production-manufacturing/06-VALIDATION.md
@.planning/phases/06-production-manufacturing/06-01-PLAN.md
@.planning/phases/06-production-manufacturing/06-02-PLAN.md

Existing implementation: runner tick + production stepping from 06-02
(src/sim/runner.ts tickProduction wired into tick() at 173-216); deposit gate and
porter destination/transfer surface from 06-01 (src/sim/production.ts:
satisfiesDeposit/canExtract, porterDestination/porterDeliversTo, workshopBottleneck);
helpers from 06-02 (tests/helpers.ts: productionChainMap + buildProductionCity).
Determinism pattern: tests/determinism/determinism.test.ts chunked test (55-76).
Baseline after 06-02: >424 tests green, typecheck clean (re-confirm on task 2).
Per-task sampling follows 06-VALIDATION.md.
</context>

<tasks>

<task type="auto">
  <name>Chunked-tick determinism for the production chain (decision 5)</name>
  <files>tests/determinism/production-chain-determinism.test.ts</files>
  <read_first>tests/determinism/determinism.test.ts (chunked idempotency test 55-76, same-seed test 15-22), tests/helpers.ts (productionChainMap + buildProductionCity from 06-02), src/sim/production.ts (model purity — no Math.random/Date), 06-CONTEXT.md decision 5, 06-RESEARCH.md section 3 Q5/Q10</read_first>
  <action>Read the existing determinism suite (tests/determinism/determinism.test.ts) and the production helpers before writing. Create tests/determinism/production-chain-determinism.test.ts with a 'production chain determinism (decision 5)' describe block importing SimRunner, productionChainMap, and buildProductionCity from tests/helpers. Script a production scenario: `const scripted = () => { const r = new SimRunner(seed, productionChainMap()); buildProductionCity(r); r.setPolicy(0, 0.5); for a fixed total of ticks r.tick(); return r.getStateJson(); }`. Assert: (1) same seed and command sequence produce byte-identical snapshots across two full runs (mirror determinism.test.ts:15-22); (2) tick batching is order-independent — run the scenario 600 ticks in chunk sizes 1, 7 and 50 (loop `for i in chunk` calling tick) and assert all three `getStateJson()` strings are identical (mirror the food-chain chunked test determinism.test.ts:55-76, now over the production city so the extraction/workshop/porter step is covered); (3) different seeds (e.g. 1 vs 2) diverge once randomness involved, or at minimum do not need to be identical — assert the seeds produce runnable, non-crashing state while documenting that the production chain itself is RNG-free (any divergence comes from shared sim systems, not production); (4) save/load round-trips a production city to byte-identical state (SimRunner.fromSaveData over the scripted city, mirror determinism.test.ts:29-42). Do not modify determinism.test.ts or any golden fixture — additions only.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/determinism/production-chain-determinism.test.ts && npx vitest run tests/determinism/determinism.test.ts</automated>
  </verify>
  <done>production-chain-determinism.test.ts passes: same-seed identity, chunk-size (1/7/50) idempotency over the production city, seed divergence note, and save/load round-trip are all green; the existing determinism suite is untouched and green.</done>
</task>

<task type="auto">
  <name>End-to-end runner acceptance: deposit enforcement, pipeline, no-loss, full suite (PROD-01/02)</name>
  <files>tests/integration/production-runner.test.ts</files>
  <read_first>tests/integration/production-chain.test.ts (06-02 chain test), tests/unit/workshop-blocked.test.ts (06-01 no-loss tests), src/sim/runner.ts (tickProduction, getProductionAdvisor from 06-02), src/sim/production.ts (satisfiesDeposit, porterDestination, porterDeliversTo, workshopBottleneck), tests/helpers.ts (productionChainMap/buildProductionCity), 06-RESEARCH.md section 2</read_first>
  <action>Read the production-chain integration test created in 06-02 and the no-loss unit tests from 06-01 to avoid duplication while composing the acceptance net. Create tests/integration/production-runner.test.ts importing SimRunner, the production helpers, and the production model, with a 'production & manufacturing acceptance (PROD-01/02)' describe block asserting through the RUNNER:
  (1) deposit requirement end-to-end — a staffed clay pit built on the clay_deposit footprint produces clay; a staffed iron mine built on a plain-earth footprint (resourceType null) produces zero iron for 300+ ticks and its advisor row reads 'blocked' with output 0 (while a matching-deposit mine or the timber yard on a 'trees' patch does produce); (2) full pipeline — the built production city reaches a state where the pottery workshop holds real inputs (clay consumed), produced pottery output, and the warehouse `b.stock.pottery` rose above 0 after enough ticks (input consumed → output produced → porter dispatched → destination stock rises); (3) destination fallback + validity — when the workshop's output is full (capacity reached) the porter still finds the warehouse and the warehouse stock keeps rising; when the warehouse is also full, the workshop keeps its load, reads 'no_destination'/blocked, and no pottery is destroyed (conservation: total clay + pottery across pit stock, workshop inputs/output, and warehouse stock is invariant across the whole window); (4) blocked-state no-loss through the runner — a workshop starved of clay (no feedstock) ticks repeatedly with zero output and byte-identical stocks, status 'missing_input'; (5) advisor integration — `r.getProductionAdvisor().rows` reflects the observed stocks/statuses (blocked off-deposit mine, missing_input starved workshop, working pipeline rows) with no fabricated values.
  Run the FULL suite at the end of the task: `npm run test` must be green (baseline 424 + Phase-6 additions) and `npm run typecheck` clean.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/integration/production-runner.test.ts && npm run test</automated>
  </verify>
  <done>production-runner.test.ts passes covering deposit enforcement end-to-end (off-deposit mine blocked with zero output while on-deposit/timber sites produce), the full extraction→workshop→porter→warehouse pipeline with destination fallback, blocked/no-destination no-loss with conservation, and advisor rows without fabricated values; the full `npm run test` (incl. golden + determinism + property) and typecheck are green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Same seed + same commands → snapshot | The production chain must never introduce batching-dependent state; chunked 1/7/50 ticks must be byte-identical |
| Seed → map + production step | All randomness flows through the seeded RNG; production.ts and tickProduction must stay RNG/clock-free |
| Runner acceptance → Phase contracts | Deposit gate, destination validity, and no-loss must hold at the live-sim level, not only in unit tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Repudiation | tickProduction nondeterminism | high | mitigate | chunked-tick determinism test (task 1) enforces byte-identical snapshots over the production city |
| T-06-09 | Tampering | deposit bypass reintroduced at runner level | high | mitigate | acceptance test: off-deposit site zero output + 'blocked' row after 300 ticks (task 2) |
| T-06-10 | Tampering | silent goods loss in live pipeline | high | mitigate | conservation assertion across pit/workshop/warehouse stock over the run window (task 2) |
</threat_model>

<verification>
Run `npm run test` (full suite, golden + determinism + property included) and
`npm run typecheck`. Spot-check: `npx vitest run tests/determinism/production-chain-determinism.test.ts`
and `npx vitest run tests/integration/production-runner.test.ts` are green.
Per-task sampling follows `.planning/phases/06-production-manufacturing/06-VALIDATION.md`.
</verification>

<success_criteria>
- Production chain is provably deterministic: chunked 1/7/50 ticks produce identical state; save/load round-trips (decision 5).
- End-to-end runner acceptance passes: deposit enforcement, extraction→workshop→porter→warehouse pipeline with destination fallback, blocked/no-destination no-loss (conservation), and advisor rows without fabricated values (PROD-01/02, decisions 1-4).
- Full project suite (baseline 424 + Phase-6 additions) and typecheck green.
</success_criteria>

<output>
Create `.planning/phases/06-production-manufacturing/06-03-SUMMARY.md` when done.
</output>
