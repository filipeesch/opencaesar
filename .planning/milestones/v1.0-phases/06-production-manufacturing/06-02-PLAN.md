---
phase: 06-production-manufacturing
plan: 06-02
type: feature
wave: 2
depends_on: [06-01]
files_modified:
  - src/sim/types.ts
  - src/sim/buildings.ts
  - src/sim/walkers.ts
  - src/sim/production.ts
  - src/sim/runner.ts
  - src/sim/advisors.ts
  - tests/helpers.ts
  - tests/unit/advisors.test.ts
  - tests/integration/production-chain.test.ts
autonomous: true
requirements: [PROD-01, PROD-02]
must_haves:
  truths:
    - "Extraction sites and workshops are real placeable buildings in the live sim: they appear in the runtime BUILDINGS catalog, have runtime BuildingType identities, and are staffed through the standard labor system (PROD-01/02)."
    - "SimRunner.tick() steps extraction and workshops every tick: extraction output is gated by the deposit requirement (off-deposit → blocked, no output); workshops consume inputs and produce output; porters move loads to valid destinations (workshop then warehouse) so destination stock rises and nothing is destroyed (PROD-01/02, decision 4)."
    - "Production advisor data is derived from live sim state (per-building stock, production state, workshop status, destination) — never fabricated (§33-23), exposed via a runner accessor without changing the SimState shape (PROD-02)."
    - "The added production buildings and tick step are additive: all 424 existing tests stay green, goldens unchanged, determinism preserved (seeded RNG only)."
  artifacts:
    - path: src/sim/runner.ts
      provides: "SimRunner.tickProduction() wiring — extraction (deposit-gated) + workshop stepping + porter dispatch to warehouse/workshop destinations, called from tick()"
      min_lines: 30
    - path: src/sim/advisors.ts
      provides: "productionAdvisorRows/productionAdvisorSummary deriving per-building production advisor data from SimState (stock, output, status, bottleneck, destination)"
      min_lines: 20
    - path: tests/helpers.ts
      provides: "productionChainMap() + buildProductionCity() — scripted deposit map and city (extraction on deposit, workshop, warehouse) reused by the chain and determinism suites"
      min_lines: 20
    - path: tests/integration/production-chain.test.ts
      provides: "Runner-level chain tests: extraction → workshop → porter → warehouse stock rise; off-deposit site blocked with no output; no goods destroyed"
      min_lines: 20
  key_links:
    - from: src/sim/runner.ts
      to: src/sim/production.ts
      via: "tickProduction uses satisfiesDeposit/canExtract, tickWorkshop, porterDestination, porterDeliversTo, EXTRACTION/WORKSHOP building-type maps (06-01)"
    - from: src/sim/runner.ts
      to: src/sim/advisors.ts
      via: "getProductionAdvisor() projects advisor rows from the runnable production state"
    - from: src/sim/runner.ts
      to: src/sim/logistics.ts
      via: "warehouse destinations respect warehouseAccepts/slot capacity (logistics.ts:25-29)"
---

<objective>
Gap-fill the decision-4 gap RESEARCH confirmed: `src/sim/production.ts` is not
referenced by any `src/sim/*` module and `SimRunner.tick()` has no extraction or
workshop stepping. Make PROD-01/PROD-02 runnable in the live sim: (1) add the
runtime building types (raw sites / workshops / warehouse) to `types.ts` and the
runtime `src/sim/buildings.ts` catalog and an optional production-state field on
`BuildingInstance`; (2) add `SimRunner.tickProduction()` — deposit-gated extraction,
workshop input/output stepping, and porter dispatch into valid workshop/warehouse
destinations (using the `porterDestination`/`porterDeliversTo` surface from 06-01) —
wired into `tick()`; add a production-chain map/city builder to `tests/helpers.ts`
and a runner-level chain integration test; (3) add SimState-derived production
advisor rows in `src/sim/advisors.ts` plus a `getProductionAdvisor()` runner
accessor and cover them in `tests/unit/advisors.test.ts`.

Output: `.planning/phases/06-production-manufacturing/06-02-PLAN.md` plus the tasks
below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-production-manufacturing/06-CONTEXT.md
@.planning/phases/06-production-manufacturing/06-RESEARCH.md
@.planning/phases/06-production-manufacturing/06-VALIDATION.md
@.planning/phases/06-production-manufacturing/06-01-PLAN.md

Existing implementation: runner tick order at `src/sim/runner.ts:173-216`
(tickFood 689-728 is the pattern to mirror; placeBuilding 341-398; toBuildingState
830-862; simInternals 803-814). Runtime catalog `src/sim/buildings.ts` (BUILDINGS
30-164, BuildingDef 4-28) needs the raw/workshop/warehouse defs that already exist
in the data catalog (data/buildings.ts:121-170). Types: `BuildingType`
(types.ts:17-20) and `BuildingCategory` (types.ts:22-25) need additive members;
`Good` (types.ts:15) already lists every production commodity. `BuildingInstance`
(walkers.ts:82-98) needs an optional production-state field. Model gates from 06-01:
EXTRACTION/WORKSHOP type maps in src/sim/production.ts, satisfiesDeposit/canExtract,
tickWorkshop, porterDestination, porterDeliversTo, workshopBottleneck. Tests:
tests/helpers.ts (foodChainMap 26-31, buildFoodCity 37-55), tests/unit/advisors.test.ts
(productionInspection test 139-147), tests/data-catalog.test.ts and
tests/integration/buildings-catalog.test.ts (closed ALL_TYPES arrays — unaffected by
additive types). Baseline: 424 tests / 57 files green, typecheck clean. Per-task
sampling follows 06-VALIDATION.md.
</context>

<tasks>

<task type="auto">
  <name>Runtime production buildings + SimRunner.tickProduction() chain (PROD-01/02, decision 4)</name>
  <files>src/sim/types.ts, src/sim/buildings.ts, src/sim/walkers.ts, src/sim/production.ts, src/sim/runner.ts, tests/helpers.ts, tests/integration/production-chain.test.ts</files>
  <read_first>src/sim/types.ts (BuildingType 17-20, BuildingCategory 22-25, Good 15), src/sim/buildings.ts (BUILDINGS 30-164, BuildingDef 4-28), src/sim/walkers.ts (BuildingInstance 82-98), src/sim/runner.ts (tick 173-216, tickFood 689-728, placeBuilding 341-398), src/sim/production.ts (EXTRACTION_SITES 38-43, WORKSHOPS 45-51, emptyProduction 62-64, tickWorkshop 78-90, satisfiesDeposit/canExtract/porterDestination/porterDeliversTo from 06-01), tests/helpers.ts (foodChainMap 26-31), tests/integration/supply-chains.test.ts, 06-01-PLAN.md, data/buildings.ts (raw 121-144, workshops 146-165, warehouse 167-170)</read_first>
  <action>Read the listed files before editing — in particular the tickFood (runner.ts:689-728) pattern to mirror and the BuildingInstance shape (walkers.ts:82-98).
  (1) Add the runtime building types. In src/sim/types.ts extend `BuildingType` additively with 'clay_pit' | 'timber_yard' | 'iron_mine' | 'quarry' | 'olive_farm' | 'grape_farm' | 'pottery_workshop' | 'furniture_workshop' | 'oil_press' | 'winery' | 'tool_workshop' | 'warehouse' and extend `BuildingCategory` with 'raw' | 'workshop' | 'storage'. In src/sim/buildings.ts add BUILDINGS entries for those twelve types mirroring the data catalog (data/buildings.ts:121-170): footprint 2 (quarry 3, warehouse 2), cost and workers matching the data catalog (clay_pit 120/8, timber_yard 130/8, iron_mine 220/12, quarry 400/16, olive_farm 150/8, grape_farm 150/8, pottery_workshop 200/8, furniture_workshop 210/8, oil_press 190/8, winery 190/8, tool_workshop 230/8, warehouse 150/3), all `requiresRoad: true` and `allowedTerrains: ['earth', 'fertile', 'trees', 'rock']` (iron_mine and quarry may additionally list 'rock' first — matches checkPlacement; see placement.ts:17-61). Do not set `spawnEveryTicks` so tickSpawns (runner.ts:632-663) never spawns walkers from them.
  (2) In src/sim/walkers.ts add an optional `production?: ProductionState` field to `BuildingInstance` (additive; import the type from production). It stays internal — toBuildingState (runner.ts:830-862) does not serialize it, so getStateJson and goldens are unaffected.
  (3) In src/sim/production.ts export `EXTRACTION_BUILDING_TYPES: Record<string, ExtractionKind>` mapping the runtime building ids — 'clay_pit'→'clay_pit', 'timber_yard'→'timber_yard', 'iron_mine'→'iron_mine', 'quarry'→'marble_quarry', 'olive_farm' and 'grape_farm' excluded (they are treated as raw farms whose produce feeds oil_press/winery inputs via the raw-producer set below) — and `WORKSHOP_BUILDING_TYPES: Record<string, WorkshopKind>` mapping 'pottery_workshop'→'pottery', 'furniture_workshop'→'carpentry', 'oil_press'→'oil_press', 'winery'→'winery', 'tool_workshop'→'metallurgy'. Also export a module const `EXTRACTION_OUTPUT_CAPACITY = 8` and a raw-producer set `RAW_OLIVE_GRAPE: Record<string, { produces: 'olives' | 'grapes'; perTick: number }>` for 'olive_farm'/'grape_farm' (perTick 0.3) so the oil_press/winery workshops are reachable. All additive.
  (4) In src/sim/runner.ts add `import` of the production model, add `private tickProduction(): void`, and call it from `tick()` right after `this.tickFood()` (runner.ts:178). Implementation contract: (a) extraction — for each building whose type is in EXTRACTION_BUILDING_TYPES: compute terrain = this.map.get(x,y), resourceType = this.map.tileState(x,y).resourceType; if `b.active` and `satisfiesDeposit(site, terrain, resourceType)` then add `site.outputPerTick` to `b.stock[site.produces]` capped at EXTRACTION_OUTPUT_CAPACITY else mark the site blocked (store the blocked flag on `b.production. blocked`, no output); (b) oil/grape farms produce olives/grapes the same way (no deposit gate — they are farms) into `b.stock`; (c) workshops — for each workshop building: ensure `b.production ??= emptyProduction(def)` on first sight, set `b.production.active = b.active` (labor gate), then run `tickWorkshop` (consumes one unit per input from `b.production.inputs`, produces to `b.production.output`); (d) feedstock porters — for each raw/extraction building with `b.stock[commodity] > 0`, build LoadDestinations for workshops that `missing_input` for that commodity (has productionState with the input at or below 0 and capacity in inputs, distance = Manhattan from the producer) and warehouses that can accept the commodity (capacity > 0 in a per-commodity slot), then use `porterDestination` — if a workshop is chosen, move via `porterDeliversTo` from producer `b.stock` into the workshop's `production.inputs`; if only a warehouse is chosen, move from `b.stock` into the warehouse `b.stock`; (e) output porters — for each workshop with `production.output[produces] > 0`, choose `porterDestination(produces, [], warehouseCandidates)` and `porterDeliversTo` from `production.output` into the warehouse `b.stock`; if no destination, keep the load and leave `s.blocked` true (workshopBottleneck reports 'no_destination'). Every move preserves units exactly (source falls by moved, destination rises by moved); nothing is created or destroyed; all iteration is in stable building order (this.buildings) so the step is deterministic.
  (5) In tests/helpers.ts add, additively, `productionChainMap()` — a Map.fromLayout builder whose layout creates an earth floor, a 'trees' patch at (0..2, 0..2) for the timber yard, and an 'earth' deposit area at (8..9, 8..9); after building the map call `map.mutateTileState` (map.ts:135) to stamp `resourceType: 'clay_deposit'` on the (8..9, 8..9) footprint so a clay pit placed there satisfies the deposit gate — and `buildProductionCity(r)` placing roads plus a clay pit on the deposit, a pottery_workshop, and a warehouse (mirroring buildFoodCity's road scaffolding, helpers.ts:37-55). Keep the existing helpers unchanged so all food-city tests stay green.
  (6) Create tests/integration/production-chain.test.ts importing SimRunner, the new helpers, and the production model. Assert: (a) placeability — each raw/workshop/warehouse type places on productionChainMap (repr the buildings-catalog pattern, tests/integration/buildings-catalog.test.ts:33-41); (b) full chain — run buildProductionCity + enough houses for labor, tick 300+, then assert the pottery workshop's productionState has consumed clay, produced pottery, and the warehouse `b.stock` pottery rose above 0 (input consumed → output produced → porter dispatched → destination stock rises at the runner level); (c) deposit enforcement — a second iron_mine placed OFF the deposit (on plain earth, resourceType null) produces no iron after many ticks and reads blocked, while staffed; (d) no-loss — sum of clay in {pit stock + workshop inputs} plus pottery in {workshop output + warehouse stock} is conserved across the run relative to its starting total (no goods destroyed) and no `b.stock` goes negative.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/integration/production-chain.test.ts && npx vitest run tests/unit/production.test.ts tests/unit/logistics.test.ts</automated>
  </verify>
  <done>production-chain.test.ts passes: raw/workshop/warehouse types place; the deposit-gated extraction → workshop → porter → warehouse chain moves goods with destination stock rising; an off-deposit site is blocked with zero output; conservation holds (no goods destroyed) and no stock is negative; the existing production/logistics unit suites + typecheck stay green.</done>
</task>

<task type="auto">
  <name>Production advisor data from live sim state (PROD-02, decision 4)</name>
  <files>src/sim/advisors.ts, src/sim/runner.ts, tests/unit/advisors.test.ts</files>
  <read_first>src/sim/advisors.ts (productionInspection 163-167, foodAdvisorFromState 379-427 as the pattern, overlaysFrom 67-83), src/sim/runner.ts (getState 474-499, getDerived 300-302, toBuildingState 830-862), src/sim/production.ts (workshopBottleneck, workshopStatus from 06-01), tests/unit/advisors.test.ts (inspectors block 139-147, mkFoodState helper 18-25)</read_first>
  <action>Read src/sim/advisors.ts, src/sim/runner.ts and the existing advisors tests before editing. Add SimState-derived production advisor data to src/sim/advisors.ts following the foodAdvisorFromState pattern (never fabricated values — every field must trace to state):
  (1) export `interface ProductionAdvisorRow { id: number; kind: 'extraction' | 'workshop'; buildingType: string; commodity: string; inputs: Record<string, number>; output: number; status: string; bottleneck: string | null; destination: string | null; producedLastTick: number }`; (2) export `function productionAdvisorRows(state: SimState): ProductionAdvisorRow[]` — for every building whose type is in EXTRACTION_BUILDING_TYPES / WORKSHOP_BUILDING_TYPES (or the RAW_OLIVE_GRAPE raw producers) build a row from `b.stock`, the optional `b.production` state (read via the existing BuildingState.stock only for what is serialized — production state is internal, so derive status: when `b.production` is absent on a building state, fall back to 'idle' with zero stocks rather than inventing values; for workshop rows, status = workshopStatus-sourced label and bottleneck = workshopBottleneck when the workshop is not 'working', destination = the chosen warehouse id when a porter move occurred this tick — the runner records it, see (3)); extraction rows report `output: b.stock[produces] ?? 0` and status 'blocked' when the site is off-deposit/closed; (3) in src/sim/runner.ts, during tickProduction record onto each production building a small internal note (e.g. `b.lastProductionTick`, `b.lastDestinationId`, `b.lastProduced`) that productionAdvisorRows can consume through a new overload or via the state — simplest is to expose `getProductionAdvisorRows()` on the runner that reads the internal BuildingInstance fields directly and calls productionAdvisorRows over a projection; also provide `getProductionAdvisor()` returning `{ rows, summary }` where `summary` is an aggregate Record (total workshops, active workshops, blocked, outputFull, missingInput, and per-commodity output stock). Keep the SimState shape unchanged (no golden churn).
  (4) Extend tests/unit/advisors.test.ts in place with a 'production advisor (PROD-02)' describe block that builds a live production city via the helpers added by task 1 of this plan (productionChainMap + buildProductionCity), ticks the runner, and asserts: rows exist for the clay pit, the pottery workshop and the warehouse; the workshop row reports real input/output values that changed after ticking; an off-deposit mine row reads status 'blocked' with zero output; the summary counts active vs blocked workshops and total pottery output stock; and every row value derives from state (e.g. workshop inputs equals what the chain actually moved — no hardcoded zero or 1). Also keep the existing inspectors test (advisors.test.ts:139-147) green — productionInspection stays as-is.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/advisors.test.ts</automated>
  </verify>
  <done>advisors.test.ts passes with the production advisor describe block: rows derived from live sim state for extraction/workshop/warehouse, status/bottleneck/destination reported, summary aggregates correct, off-deposit site blocked with zero output, and no SimState/golden changes; typecheck clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime catalog → placement | New raw/workshop/warehouse BuildingTypes must place through checkPlacement with valid defs and never spawn walkers (no spawnEveryTicks) |
| Deposit substrate → extraction tick | Extraction output is gated by the deposit requirement at tick time; a mis-placed site yields zero output and reports blocked |
| tickProduction moves → destination | Every porter move conserves units (source falls = destination rises); no load is destroyed, teleported, or double-counted |
| SimState → advisor rows | Advisor rows are projections of live state/buildings; they never mutate sim state and never invent values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | off-deposit extraction produces anyway | high | mitigate | satisfiesDeposit gate in tickProduction (task 1); chain test asserts zero output + blocked for a mine off the deposit |
| T-06-06 | Repudiation | advisor invents production values | high | mitigate | productionAdvisorRows projects only from building stock/production state; test asserts real moved quantities, no hardcoded zeros |
| T-06-07 | Repudiation | non-deterministic tickProduction breaks determinism | high | mitigate | stable building-order iteration, no Math.random/Date in the step; asserted end-to-end by the plan 06-03 determinism test |
| T-06-08 | Integrity | new types break goldens/serialization | medium | mitigate | production state is internal (walkers.ts field) and skipped by toBuildingState; SimState shape unchanged |
</threat_model>

<verification>
Run `npm run test` (full suite, golden + determinism + property included) and
`npm run typecheck`. Spot-check: `npx vitest run tests/integration/production-chain.test.ts`
and `npx vitest run tests/unit/advisors.test.ts` are green; the existing
tests/data-catalog.test.ts, tests/integration/buildings-catalog.test.ts and property
invariants stay green after the additive type/catalog changes.
Per-task sampling follows `.planning/phases/06-production-manufacturing/06-VALIDATION.md`.
</verification>

<success_criteria>
- Extraction sites and workshops are real, placeable, staffable live-sim buildings with runtime defs and types (PROD-01/02).
- SimRunner.tick() runs extraction (deposit-gated) and workshops every tick, with porters moving loads into valid workshop/warehouse destinations; destination stock rises and nothing is destroyed (decision 4).
- Production advisor data is derived from live sim state and exposed via a runner accessor; advisor tests cover real values (PROD-02).
- Full suite (424 tests) and typecheck remain green; goldens and SimState shape unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-production-manufacturing/06-02-SUMMARY.md` when done.
</output>
