---
phase: 01-time-deterministic-core
plan: 01-02
type: feature
wave: 2
depends_on: [01-01]
files_modified:
  - src/sim/runner.ts
  - src/sim/types.ts
  - tests/unit/paused-queue.test.ts
  - tests/determinism/determinism.test.ts
autonomous: true
requirements: [CORE-02]
must_haves:
  truths:
    - "Build, demolish, and policy orders issued while paused are consumed on the next fixed step (CORE-02)."
    - "A demolish command exists on the public sim interface and removes a building's footprint and any road tiles it occupied."
    - "Commands issued while paused are deferred (not applied, not persisted) and drained in FIFO order on the first tick after resume."
    - "Same seed + same paused-command script produces an identical final state, and save/load round-trips the paused pipeline."
---

<objective>
Gap-fill CORE-02 to full coverage: the paused-command queue exists for build and policy but a
demolish command is entirely absent, and only the policy path is tested. This plan adds
`SimRunner.demolish`, threads it through `PendingCommand` and `SaveCommand` (with replay in
`fromSaveData`), and locks the whole pipeline — build/demolish/policy issued while paused are
consumed on the next step — with unit + determinism + save/load tests.

Output: `.planning/phases/01-time-deterministic-core/01-02-PLAN.md` plus the tasks below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-time-deterministic-core/01-CONTEXT.md
@.planning/phases/01-time-deterministic-core/01-RESEARCH.md
@.planning/phases/01-time-deterministic-core/01-VALIDATION.md

Existing behavior: `SimRunner.setPaused` (src/sim/runner.ts:135-137); `pendingCommands` queue
(runner.ts:107); `drainPendingCommands` at the top of `tick()` (runner.ts:147-155, 162);
`placeBuilding`/`setPolicy` enqueue when paused (runner.ts:330-333, 402-405). `PendingCommand`
(runner.ts:56-58) and `SaveCommand` (src/sim/types.ts:63-65) currently cover only
place/setPolicy; replay lives in `fromSaveData` (runner.ts:543-556). Test helpers in
tests/helpers.ts (foodChainMap, buildFoodCity, place). Baseline: 253 tests green, typecheck clean.
Per-task sampling follows 01-VALIDATION.md.
</context>

<tasks>

<task type="auto">
  <name>Add the demolish command (functional CORE-02 gap)</name>
  <files>src/sim/runner.ts, src/sim/types.ts, tests/unit/paused-queue.test.ts</files>
  <action>Read src/sim/runner.ts and src/sim/types.ts before editing. Add `demolish(x: number,
  y: number): boolean` to SimRunner (src/sim/runner.ts, near placeBuilding at line 329) that,
  when not paused: (1) looks up the building whose footprint covers (x, y) via `occupiedTiles`
  (runner.ts:96) + `buildingById` (runner.ts:95); (2) if none, returns false; (3) otherwise
  removes it from `buildings`/`buildingById`, clears every footprint tile in `occupiedTiles`,
  and when the type is 'road' resets the footprint to 'earth' via `map.setRect`; (4) pushes
  `{ kind: 'demolish', x, y }` to `saveCommands` (runner.ts:104) and a `commandLog` entry
  `{ tick, command: 'demolish x,y', result: 'ok' }`; (5) returns true. When paused, mirror the
  existing enqueue pattern (runner.ts:330-333): push `{ kind: 'demolish', x, y }` to
  `pendingCommands` and return true. Extend the `PendingCommand` union (runner.ts:56-58) with
  `| { kind: 'demolish'; x: number; y: number }` and handle it in `drainPendingCommands`
  (runner.ts:147-155). Extend the `SaveCommand` union (src/sim/types.ts:63-65) with
  `| { kind: 'demolish'; x: number; y: number }` and add the demolish branch to the replay loop
  in `fromSaveData` (src/sim/runner.ts:543-556). Do not modify the `PlacementError`/`PlacementResult`
  types (types.ts:31-39) — demolish returns a boolean, not a PlacementResult.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>demolish exists on SimRunner, enqueues while paused, drains on tick, replays from
  save data; typecheck clean; paused-queue tests pass.</done>
</task>

<task type="auto">
  <name>Paused-command pipeline unit + determinism + save/load tests</name>
  <files>tests/unit/paused-queue.test.ts, tests/determinism/determinism.test.ts</files>
  <action>Read tests/helpers.ts (foodChainMap/buildFoodCity/place) and the existing CORE-02
  tests in tests/runner-accessors.test.ts:75-94 before writing. Create tests/unit/paused-queue.test.ts
  covering: (1) place-while-paused defers — `placeBuilding('road', 2, 2)` while paused leaves the
  building absent from getState() and `getPendingCommandCount()` === 1; after `setPaused(false)`
  + one `tick()`, the road is placed and the queue is empty; (2) demolish-while-paused defers —
  place then pause, `demolish(x,y)`, assert the building is still present while paused and
  removed after resume tick; (3) FIFO drain — enqueue place, policy, demolish while paused and
  assert `drainPendingCommands` applies them in order on the first resume tick at the same
  tickCount (tickCount before and after drain differs by exactly 1); (4) un-paused commands
  apply immediately with an empty queue. Add a determinism test to tests/determinism/determinism.test.ts:
  script (same seed + foodChainMap + buildFoodCity) that pauses, enqueues place/demolish/policy,
  resumes, and ticks 500 times — run it twice and assert identical getStateJson(); plus a
  save/load round-trip: pause → enqueue → resume → tick → `getSaveData()` → `fromSaveData` →
  assert identical getStateJson() (exercises the new demolish SaveCommand branch).</action>
  <verify>
    <automated>npx vitest run tests/unit/paused-queue.test.ts && npx vitest run tests/determinism/determinism.test.ts && npm run typecheck</automated>
  </verify>
  <done>paused-queue and determinism suites pass, including demolish replay through
  save/load; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI/commands → SimRunner | place/demolish/policy issued while paused must queue, never mutate state until the next tick |
| Save/load → runner | The demolish SaveCommand must replay identically in fromSaveData |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | paused-command queue | high | mitigate | FIFO + next-tick-only drain asserted by pipeline unit test; save/load round-trip determinism test covers the demolish branch |
| T-01-04 | Spoofing | out-of-order replay | medium | mitigate | determinism test replays the same paused script twice and asserts identical state |
</threat_model>

<verification>
Run `npm run test` (full suite, determinism + golden included) and `npm run typecheck`.
Spot-check: `npx vitest run tests/unit/paused-queue.test.ts` and `npx vitest run tests/determinism/determinism.test.ts`.
Per-task sampling follows `.planning/phases/01-time-deterministic-core/01-VALIDATION.md`.
</verification>

<success_criteria>
- `SimRunner.demolish(x, y)` exists and works both immediately and through the paused queue (CORE-02).
- Build/demolish/policy issued while paused are consumed on the next fixed step, in FIFO order.
- Demolish commands replay deterministically from save data.
- Full suite and typecheck remain green.
</success_criteria>

<output>
Create `.planning/phases/01-time-deterministic-core/01-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `SimRunner.demolish(x, y): boolean` (new method, `src/sim/runner.ts`)
- `PendingCommand` variant `{ kind: 'demolish'; x: number; y: number }` (`src/sim/runner.ts:56-58`)
- `SaveCommand` variant `{ kind: 'demolish'; x: number; y: number }` (`src/sim/types.ts:63-65`)
- `fromSaveData` demolish replay branch (`src/sim/runner.ts:543-556`)
- New test file: `tests/unit/paused-queue.test.ts`; new determinism cases in `tests/determinism/determinism.test.ts`
