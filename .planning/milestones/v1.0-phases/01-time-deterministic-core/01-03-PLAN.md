---
phase: 01-time-deterministic-core
plan: 01-03
type: feature
wave: 3
depends_on: [01-02]
files_modified:
  - src/sim/runner.ts
  - src/sim/tile.ts
  - src/sim/map.ts
  - tests/unit/tile.test.ts
  - tests/runner-accessors.test.ts
  - tests/golden/golden.test.ts
  - tests/golden/fixtures/paused-commands-golden.json
autonomous: true
requirements: [CORE-03]
must_haves:
  truths:
    - "Per-tile state exposes elevation, fertility, resourceType/Amount, waterDepth, aqueduct, road, desirability, fireRisk, collapseRisk, pollution, traffic, serviceCoverage, ownership, and blocked (CORE-03)."
    - "Tile state stays read-only outside src/sim/: the public accessor returns a copy, never a live reference."
    - "Commands issued while paused are applied identically at the same seed, producing the same final state in a golden snapshot."
    - "The existing determinism suite and food-chain golden fixture remain green."
---

<objective>
Close CORE-03: the TileState interface already carries all 15 fields, but the default-assertion
test omits `resourceAmount`, and per-tile state is unreachable through the public sim interface.
This plan adds the read-only `SimRunner.getTileState(x, y)` accessor (copy, not live reference),
fills the test gaps, and adds the paused-command pipeline golden — commands issued while paused
applied identically at the same seed → identical final state — per the CONTEXT decision, while
keeping the existing determinism and golden suites green.

Output: `.planning/phases/01-time-deterministic-core/01-03-PLAN.md` plus the tasks below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-time-deterministic-core/01-CONTEXT.md
@.planning/phases/01-time-deterministic-core/01-RESEARCH.md
@.planning/phases/01-time-deterministic-core/01-VALIDATION.md

Existing implementation: `TileState` interface + `defaultTileState` (src/sim/tile.ts:9-45);
`Map.tileState`/`Map.mutateTileState` (src/sim/map.ts:128-137); terrain grid exposure in
`SimRunner.getState` (src/sim/runner.ts:435). Tests: tests/unit/tile.test.ts (3 cases),
tests/runner-accessors.test.ts, tests/golden/golden.test.ts (food-chain fixture).
Golden regen: `npm run test:golden:update` (package.json:16). Baseline: 253 tests green,
typecheck clean. Per-task sampling follows 01-VALIDATION.md.
</context>

<tasks>

<task type="auto">
  <name>Expose read-only per-tile state through the public sim interface</name>
  <files>src/sim/runner.ts, src/sim/tile.ts, src/sim/map.ts, tests/unit/tile.test.ts, tests/runner-accessors.test.ts</files>
  <action>Read src/sim/runner.ts, src/sim/tile.ts, and src/sim/map.ts before editing. Audit the
  `TileState` interface (src/sim/tile.ts:9-25) against the CORE-03 field list — elevation,
  fertility, resourceType, resourceAmount, waterDepth, aqueduct, road, desirability, fireRisk,
  collapseRisk, pollution, traffic, serviceCoverage, ownership, blocked — and add any missing
  field plus its neutral default in `defaultTileState` (none are expected to be missing). Add
  `getTileState(x: number, y: number): TileState` to SimRunner (src/sim/runner.ts, near
  getState at line 422) returning `{ ...this.map.tileState(x, y) }` — a shallow copy so no live
  reference escapes `src/sim/` (all TileState fields are primitives, so the copy is complete).
  Do not alter `Map.tileState`'s internal live-reference semantics (sim systems still mutate via
  `mutateTileState`). Extend the defaults test in tests/unit/tile.test.ts to assert
  `resourceAmount === 0` and `desirability === 0` in the neutral-defaults case (currently
  omitted). Add a case to tests/runner-accessors.test.ts asserting `getTileState` returns all 15
  fields for an in-bounds tile on a seeded runner and that mutating the returned object does not
  affect subsequent reads (read-only contract).</action>
  <verify>
    <automated>npx vitest run tests/unit/tile.test.ts && npx vitest run tests/runner-accessors.test.ts && npm run typecheck</automated>
  </verify>
  <done>getTileState accessor exists and returns a copy; tile defaults test asserts all 15
  fields including resourceAmount and desirability; accessor test passes; typecheck clean.</done>
</task>

<task type="auto">
  <name>Add the paused-command pipeline golden + keep determinism suites green</name>
  <files>tests/golden/golden.test.ts, tests/golden/fixtures/paused-commands-golden.json</files>
  <action>Read tests/golden/golden.test.ts and tests/determinism/determinism.test.ts before
  editing. Add a second `describe` block to tests/golden/golden.test.ts for the paused-command
  pipeline: build a runner at a fixed seed (use `runScenario`/`foodChainMap`/`buildFoodCity`
  from tests/helpers.ts), `setPaused(true)`, enqueue a mix of place/demolish/policy commands
  (including at least one `demolish`), `setPaused(false)`, then tick 1200 times. Follow the
  existing golden pattern (golden.test.ts:16-36): under `process.env.GOLDEN_UPDATE` write
  `JSON.stringify(state, null, 2)` to `tests/golden/fixtures/paused-commands-golden.json`;
  otherwise compare `state` to the recorded fixture with `expect(state).toEqual(recorded)`.
  Seed the new fixture by running `npm run test:golden:update`. The existing food-chain golden
  and the determinism suite must remain untouched and green — the new demolish SaveCommand kind
  does not alter the recorded state shape (golden fixtures snapshot state, not the save payload).</action>
  <verify>
    <automated>npm run test:golden:update && npx vitest run tests/golden && npm run test && npm run typecheck</automated>
  </verify>
  <done>paused-commands-golden.json fixture recorded and matched; full `npm run test` (golden +
  determinism included) green; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/game (UI) → TileState | must only read; never mutate tile state (determinism D1) |
| Golden fixture ↔ runner | fixture must replay deterministically from seed + command script |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | read-only tile state | medium | mitigate | getTileState returns a copy; accessor test asserts mutating it cannot corrupt sim state |
| T-01-06 | Repudiation | golden determinism | high | mitigate | paused-command golden runs at fixed seed and compares against recorded fixture; regen only via npm run test:golden:update |
</threat_model>

<verification>
Run `npm run test` (full suite, golden + determinism included) and `npm run typecheck`.
Spot-check: `npx vitest run tests/golden`, `npx vitest run tests/unit/tile.test.ts`, `npx vitest run tests/runner-accessors.test.ts`.
Per-task sampling follows `.planning/phases/01-time-deterministic-core/01-VALIDATION.md`.
</verification>

<success_criteria>
- All 15 CORE-03 fields are exposed through `SimRunner.getTileState(x, y)` and asserted by tests.
- Tile state remains read-only outside `src/sim/` (copy accessor, no live reference).
- Paused-command pipeline has a golden snapshot; same seed → identical final state.
- Existing determinism suite and food-chain golden remain green; full suite + typecheck green.
</success_criteria>

<output>
Create `.planning/phases/01-time-deterministic-core/01-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `SimRunner.getTileState(x: number, y: number): TileState` (new method, `src/sim/runner.ts`)
- New golden fixture: `tests/golden/fixtures/paused-commands-golden.json`
- New golden case: paused-command pipeline (`tests/golden/golden.test.ts`)
- Test cases: `tests/unit/tile.test.ts` (resourceAmount/desirability defaults), `tests/runner-accessors.test.ts` (read-only accessor contract)
