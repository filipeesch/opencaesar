---
phase: 03-road-graph-walker-categories
plan: 03-03
type: auto
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/sim/walkerProfiles.ts
  - src/sim/walkers.ts
  - src/sim/pathfind.ts
  - tests/unit/walker-profile-contract.test.ts
  - tests/unit/walker-category-behavior.test.ts
  - tests/unit/walker-roadblock-permissions.test.ts
  - tests/golden/fixtures/food-chain-golden.json
  - tests/golden/fixtures/paused-commands-golden.json
autonomous: true
requirements: [ROAD-03]
must_haves:
  truths:
    - "WalkerProfile schema covers all ROAD-03 per-type fields (maxRoadSteps, serviceTTL, spawnInterval, speed, allowedRoadTypes, roadblockPolicy, serviceRadius, preferredDirection, returnPolicy) for every catalog walker (ROAD-03)."
    - "Wandering walkers with returnPolicy true turn back at maxRoadSteps and never stray farther from their origin than maxRoadSteps tiles (ROAD-03)."
    - "Destination walkers travel by graph path only: a Manhattan-near but road-unreachable target is never served (no Euclidean fallback) (ROAD-03)."
    - "One walker of each category (wandering/destination/recruiter) is profiled; per-type assets (movementSpeed, serviceTTL) are consumed by walker behavior (ROAD-03)."
    - "Per-category roadblock permissions are configurable and enforced: a 'stop' walker never enters a service_roadblock tile; a 'pass' walker traverses it (ROAD-03)."
---

<objective>
Make the ROAD-03 walker categories behave per their data. Research found the
`WalkerProfile` schema is complete (all 9 fields, walkerProfiles.ts:14-35) but the module
has zero src/ consumers: `walkers.ts` never reads a profile, wandering walkers never
return at `maxRoadSteps`, service freshness uses the flat CONFIG constant, and pathfinding
ignores road types / roadblockPolicy. Deliver (1) a profile-contract test pinning the
schema, (2) wiring of movementSpeed / serviceTTL / wandering return-at-maxRoadSteps, and
(3) configurable per-category roadblock permissions enforced in both pathfinding and
wandering movement, plus a graph-path (no-Euclidean-fallback) confirmation test.
Because wandering return-at-maxRoadSteps changes well-walker trajectories in the
food-city goldens, they are regenerated intentionally in this plan (second documented
ROAD-03 mechanic change; speed/serviceTTL defaults match CONFIG so only trajectories shift).

Output: `.planning/phases/03-road-graph-walker-categories/03-03-PLAN.md` plus the tasks below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-road-graph-walker-categories/03-CONTEXT.md
@.planning/phases/03-road-graph-walker-categories/03-RESEARCH.md
@.planning/phases/03-road-graph-walker-categories/03-VALIDATION.md

Parents: 03-01 (roadNet) and 03-02 (per-tile roadType via Map.roadTypeAt/setRoadType,
walker speed already multiplied by roadSpeedMultiplier at walkers.ts:move()). ROAD-03
facts from RESEARCH section 1: profiles exist but unimported; destination pathfind is
as-built (walkers.ts:190-201); recruiter spawn deferred (WalkerType, types.ts:27-29) —
recruiter labor-link proven via the existing `labor` walker contract. Baseline: 289 tests
/ 46 files green. Goldens regenerated in 03-02 (desirability) will be regenerated AGAIN in
task 2 below (wandering trajectories).
</context>

<tasks>

<task type="auto">
  <name>WalkerProfile schema contract test across the catalog</name>
  <files>src/sim/walkerProfiles.ts, data/walkers.ts, tests/unit/walker-profile-contract.test.ts</files>
  <read_first>src/sim/walkerProfiles.ts (WalkerProfile 14-35, DEFAULT_PROFILE 52-63, walkerProfile 69-78, allWalkerProfiles 80-82), data/walkers.ts (WALKERS 14-27), tests/unit/walker-profiles.test.ts, 03-CONTEXT.md decision 4</read_first>
  <action>Read src/sim/walkerProfiles.ts and data/walkers.ts before editing. Verify the `WalkerProfile` interface (lines 14-35) covers every per-type field ROAD-03 requires: maxRoadSteps, serviceTTL, spawnInterval, movementSpeed (the requirement's "speed"), allowedRoadTypes, roadblockPolicy, serviceRadiusFromCurrentTile (the requirement's "serviceRadius"), preferredDirection, returnPolicy. If any of these nine keys is absent from the interface OR from the object returned by `walkerProfile(id)` via `DEFAULT_PROFILE`/the merge at lines 69-78, add it with a typed default matching the existing values in DEFAULT_PROFILE (research indicates all nine are present — make a final check before concluding; do not rename existing keys).
  Create `tests/unit/walker-profile-contract.test.ts` with a 'WalkerProfile schema contract (ROAD-03)' describe block asserting, for EVERY profile returned by `allWalkerProfiles()`: the nine required field names are all defined (own keys of the object); `maxRoadSteps` is an integer > 0; `serviceTTL` is an integer > 0; `spawnInterval` is an integer > 0; `movementSpeed` is a finite number > 0; `allowedRoadTypes` is a non-empty array whose entries are all keys of `ROAD_TYPES` (import from '../../src/sim/roadTypes'); `roadblockPolicy` is exactly 'stop' or 'pass'; `serviceRadiusFromCurrentTile` is a finite number >= 0; `preferredDirection` is exactly 'left', 'right', or 'straight'; `returnPolicy` is a boolean. Also assert: `allWalkerProfiles().length === Object.keys(WALKERS).length` (every catalog walker has a profile, import WALKERS from '../../data/walkers'); at least one profile of each category (wandering/destination/recruiter) exists; every recruiter profile has `spawnInterval === 60`; and `walkerCategory('market') === 'destination'` while `walkerCategory('official') === 'recruiter'`.</action>
  <acceptance_criteria>
    - The `WalkerProfile` schema object returned by `walkerProfile(id)` exposes all nine ROAD-03 fields with valid typed values.
    - `tests/unit/walker-profile-contract.test.ts` passes and asserts schema completeness + range validity for every catalog walker.
    - `npx vitest run tests/unit/walker-profiles.test.ts` (existing suite) still passes untouched.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/walker-profile-contract.test.ts && npx vitest run tests/unit/walker-profiles.test.ts</automated>
  </verify>
  <done>Profile contract test created and green; the schema is confirmed complete for all nine ROAD-03 per-type fields; existing walker-profiles suite unchanged and passing.</done>
</task>

<task type="auto">
  <name>Wire profile data into walker behavior: movementSpeed, serviceTTL, wandering return-at-maxRoadSteps</name>
  <files>src/sim/walkers.ts, src/sim/walkerProfiles.ts, tests/unit/walker-category-behavior.test.ts, tests/golden/fixtures/food-chain-golden.json, tests/golden/fixtures/paused-commands-golden.json</files>
  <read_first>src/sim/walkers.ts (WalkerInstance 18-38, createWalker 101-117, updateWalker 120-141, move 249-271, serviceHousesAround 154-166, handleArrival 207-241, DIRS 94-99), src/sim/walkerProfiles.ts (walkerProfile 69-78, DEFAULT_PROFILE 52-63), tests/unit/walkers.test.ts (makeStub 43-58, roadLoopMap 10-21), 03-CONTEXT.md decision 3 + deferred 'per-walker overrides'</read_first>
  <action>Read src/sim/walkers.ts and src/sim/walkerProfiles.ts before editing. Wire `walkerProfile(w.type)` into behavior, preserving current behavior for default profiles (movementSpeed 0.5 == CONFIG.walkerSpeedPerTick; serviceTTL 120 == CONFIG.serviceCooldownTicks):
  - src/sim/walkers.ts: import `walkerProfile` from './walkerProfiles'. Extend `WalkerInstance` (lines 18-38) with `origin: { x: number; y: number } | null` and `stepsTaken: number`, initialized in `createWalker` (101-117) as `origin: { x, y }` and `stepsTaken: 0`.
  - In `updateWalker` (120-141), resolve `const profile = walkerProfile(w.type)` once and pass it down to `move` and the coverage/arrival code paths that need it.
  - Movement speed: in `move()` (line ~263), replace the base constant `CONFIG.walkerSpeedPerTick` with the profile value, keeping the road multiplier from 03-02: `const rt = sim.map.roadTypeAt(w.x, w.y) ?? 'dirt'; w.progress += profile.movementSpeed * roadSpeedMultiplier(rt);`. Default 0.5 * 1 == previous 0.5, so the suite stays equivalent.
  - Service TTL: in `serviceHousesAround` (154-166) and the market delivery branch of `handleArrival` (line 214), set the served cooldown to `profile.serviceTTL` instead of `CONFIG.serviceCooldownTicks` (default equal, so coverage tests asserting 120 stay green).
  - Wandering return-at-maxRoadSteps: for profiles with `returnPolicy === true` and category 'wandering' (well, etc.), after each tile advance in `move()` (the block at 264-270) increment `w.stepsTaken`; when `stepsTaken >= profile.maxRoadSteps` and state is 'wandering', choose the next wander step from the neighbors that reduce Manhattan distance to `w.origin`, tie-broken by the seeded `randInt(sim.rng, ...)` (no Math.random); when the walker is back at its origin tile, reset `stepsTaken` to 0 and resume free wandering. Destination (market/labor, 'seeking' state) and recruiter paths are untouched.
  Create `tests/unit/walker-category-behavior.test.ts` using the roadLoopMap + makeStub pattern from tests/unit/walkers.test.ts (import createWalker/updateWalker, walkerProfile, and a SimInternals stub with a seeded rng). Assert: (1) speed pipeline — a `createWalker('well', ...)` on a dirt tile advances `progress` by exactly `walkerProfile('well').movementSpeed` (0.5) after one `updateWalker`, and on a 'paved' tile (map.setRoadType) by `walkerProfile('well').movementSpeed * roadSpeedMultiplier('paved')`; (2) serviceTTL — a `createWalker('clinic', ...)` adjacent to a house sets `house.services['health']` to `walkerProfile('clinic').serviceTTL`; (3) wandering return — track the Manhattan distance from a wandering walker (e.g. `well`) to its origin each tick for several hundred ticks on the loop map: the distance never exceeds `walkerProfile('well').maxRoadSteps` (8) and at some point drops back toward 0 (it returns). No assertion may depend on a specific RNG draw beyond using the same seed for setup.</action>
  <acceptance_criteria>
    - `WalkerInstance` gains `origin` and `stepsTaken`; `createWalker` initializes both.
    - `move()` advances by `profile.movementSpeed * roadSpeedMultiplier(tileType)` (defaults equal the old CONFIG constant → no speed regression).
    - `serviceHousesAround`/market delivery set cooldowns from `profile.serviceTTL`.
    - A wandering walker with returnPolicy true never exceeds `maxRoadSteps` from its origin and returns after hitting it.
    - `tests/unit/walker-category-behavior.test.ts` passes; `tests/unit/walkers.test.ts` and `tests/property/invariants.test.ts` stay green.
    - Since well-walker trajectories now change, goldens are regenerated intentionally: `GOLDEN_UPDATE=1 npm run test:golden:update` and the full suite passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/walker-category-behavior.test.ts && npx vitest run tests/unit/walkers.test.ts && npx vitest run tests/property/invariants.test.ts && GOLDEN_UPDATE=1 npm run test:golden:update && npm run test</automated>
  </verify>
  <done>walkerProfiles drive movementSpeed/serviceTTL/return-at-maxRoadSteps; category-behavior tests green; existing walker + property suites unaffected; goldens regenerated and full suite green.</done>
</task>

<task type="auto">
  <name>Per-category roadblock permissions + graph-path (no Euclidean fallback) confirmation</name>
  <files>src/sim/walkerProfiles.ts, src/sim/pathfind.ts, src/sim/walkers.ts, tests/unit/walker-roadblock-permissions.test.ts</files>
  <read_first>src/sim/walkerProfiles.ts (RoadBlockPolicy 12, WalkerProfile 14-35, walkerProfile 69-78), src/sim/pathfind.ts (findRoadPath 33-101, hardcoded terrain checks 36 and 77, DIRS 4-9), src/sim/walkers.ts (startSeeking 190-201, move 257-260, walkerProfile import from task 2), src/sim/map.ts (roadTypeAt/setRoadType from 03-02), tests/unit/walkers.test.ts (makeStub, roadLoopMap), 03-CONTEXT.md decision 5</read_first>
  <action>Read src/sim/walkerProfiles.ts, src/sim/pathfind.ts, and src/sim/walkers.ts before editing. Add configurable per-category roadblock permissions and enforce them in pathfinding and wandering movement:
  - src/sim/walkerProfiles.ts: add `export const ROADBLOCK_POLICY_BY_CATEGORY: Record&lt;WalkerCategory, RoadBlockPolicy&gt; = { wandering: 'stop', destination: 'pass', recruiter: 'stop' };` and have `walkerProfile` resolve the returned `roadblockPolicy` from this table (falling back to 'stop'). Import `isRoadPassable` from './roadTypes'. Add `export function mayTraverse(profile: WalkerProfile, type: string): boolean` where a `service_roadblock` tile is granted solely by `profile.roadblockPolicy === 'pass'` (independent of `isRoadPassable`, since `isRoadPassable('service_roadblock') === false`), and every other type requires `isRoadPassable(type) && profile.allowedRoadTypes.includes(type)`:
  `return type === 'service_roadblock' ? profile.roadblockPolicy === 'pass' : isRoadPassable(type) && profile.allowedRoadTypes.includes(type);`
  - src/sim/pathfind.ts: add an optional 4th parameter `isTraversable?: (x: number, y: number) => boolean` to `findRoadPath` (line 33) defaulting to `(x, y) => map.get(x, y) === 'road'`; replace the hardcoded terrain check in the neighbor expansion (line 77) with `if (!isTraversable(nx, ny)) continue;`. Keep the start/goal `map.get(...) === 'road'` guards at 34-36 unchanged (walkers stand/arrive on road tiles).
  - src/sim/walkers.ts: in the code paths that use `walkerProfile(w.type)`, build a per-walker predicate `const trav = (x: number, y: number): boolean => map.get(x, y) === 'road' && mayTraverse(profile, map.roadTypeAt(x, y) ?? 'dirt');`. Pass `trav` as the `isTraversable` argument to `findRoadPath` in `startSeeking` (line 193) so destination pathfinding routes around blocked roadblock tiles; in `move()` (line ~257) replace the bare `roadNeighbors(sim.map, w.x, w.y)` filter with the same predicate so wandering walkers never enter a service_roadblock tile they are not permitted to cross.
  Create `tests/unit/walker-roadblock-permissions.test.ts` asserting: (1) configuration — `ROADBLOCK_POLICY_BY_CATEGORY.wandering === 'stop'`, `.destination === 'pass'`, `.recruiter === 'stop'`; `mayTraverse(walkerProfile('well'), 'service_roadblock') === false` (wandering is 'stop'); `mayTraverse(walkerProfile('market'), 'service_roadblock') === true` (destination is 'pass' — proves the roadblock exception fires even though `isRoadPassable('service_roadblock') === false`); and a profile with `allowedRoadTypes` including a passable type returns true; (2) pathing around a block — on a map where the direct road route to a granary passes through one 'service_roadblock' tile (set via `map.setRoadType`) but an alternate longer road route exists, a market (destination, 'pass') still reaches the granary and its `path` never contains the roadblock tile; (3) stop enforcement — a `well` (wandering, 'stop') on a map containing a 'service_roadblock' tile never occupies it over many `updateWalker` calls; (4) graph-path / no Euclidean fallback — a granary separated from a market by a one-tile road gap (Manhattan-near, road-unreachable) is never served: `startSeeking` returns false, the market walker's `path` stays empty, and `granary.stock.wheat` is unchanged after many ticks. Use the makeStub pattern from tests/unit/walkers.test.ts (import findRoadPath where needed, and construct a map with road + setRoadType tiles).</action>
  <acceptance_criteria>
    - `ROADBLOCK_POLICY_BY_CATEGORY` exists with the three defaults; `mayTraverse` enforces allowedRoadTypes + roadblockPolicy against `isRoadPassable`.
    - `findRoadPath` accepts an optional `isTraversable` predicate whose default equals the previous terrain-only behavior (no existing caller changes).
    - Destination pathfinding routes around a blocked roadblock tile; a 'stop' walker never steps onto service_roadblock.
    - A Manhattan-near but road-unreachable granary is never served (graph-path distance, no Euclidean fallback).
    - `tests/unit/walker-roadblock-permissions.test.ts` passes; `tests/unit/walkers.test.ts` and `tests/property/invariants.test.ts` stay green; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/walker-roadblock-permissions.test.ts && npx vitest run tests/unit/walkers.test.ts && npx vitest run tests/property/invariants.test.ts</automated>
  </verify>
  <done>ROADBLOCK_POLICY_BY_CATEGORY + mayTraverse exist; findRoadPath is traversability-aware; wandering/destination movement enforces per-walker permissions; graph-path/no-Euclidean fallback test green; walkers + property suites unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| walkerProfiles data → walkers.ts | Profile fields must keep defaults behavior-identical to the old CONFIG constants (speed 0.5, TTL 120) to avoid silent gameplay churn |
| profile roadblockPolicy → movement/path | A 'stop' policy must never put a walker on a service_roadblock; 'pass' only when the profile says so |
| pathfind traversability → reachability | An over-restrictive predicate can make buildings unreachable (labor links, deliveries) — default must be unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Denial of Service | profile schema regression | medium | mitigate | profile-contract test pins all nine fields + ranges (task 1) |
| T-03-06 | Tampering | unconsumed per-type data | high | mitigate | movementSpeed/serviceTTL/return wiring with behavior tests (task 2) |
| T-03-07 | Elevation of Privilege | roadblock bypass | medium | mitigate | mayTraverse + per-category policy + path/movement enforcement + tests (task 3) |
</threat_model>

<verification>
Run `npm run test` (full suite, golden + determinism + property included) and `npm run typecheck`.
Spot-check: `npx vitest run tests/unit/walker-profile-contract.test.ts`, `npx vitest run tests/unit/walker-category-behavior.test.ts`, and `npx vitest run tests/unit/walker-roadblock-permissions.test.ts` are green.
Per-task sampling follows `.planning/phases/03-road-graph-walker-categories/03-VALIDATION.md`.
</verification>

<success_criteria>
- The nine-field WalkerProfile schema is pinned by a contract test for every catalog walker.
- Wandering walkers return at maxRoadSteps (never farther than 8 from origin); destination walkers still pathfind; recruiter category is profiled and its labor-link (the `labor` walker) remains contract-covered.
- Per-category roadblock permissions are configurable and enforced in both pathfinding and wandering movement.
- Travel distance is graph-path only — a Manhattan-near but road-unreachable target is never served.
- Goldens regenerated for the wandering-trajectory change; full suite + typecheck green.
</success_criteria>

<output>
Create `.planning/phases/03-road-graph-walker-categories/03-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/sim/walkerProfiles.ts`: `ROADBLOCK_POLICY_BY_CATEGORY` table; `mayTraverse(profile, type)` helper; per-category `roadblockPolicy` resolution in `walkerProfile()`.
- `src/sim/walkers.ts`: `WalkerInstance.origin` + `WalkerInstance.stepsTaken`; `walkerProfile(w.type)` consumption in `updateWalker`/`move`/`serviceHousesAround`/`handleArrival`; speed formula `profile.movementSpeed * roadSpeedMultiplier(tile)`; service cooldowns from `profile.serviceTTL`; wandering return-at-maxRoadSteps; per-walker `isTraversable` predicate in `startSeeking` + `move`.
- `src/sim/pathfind.ts`: optional `isTraversable` parameter on `findRoadPath` (default = terrain-only).
- New test files: `tests/unit/walker-profile-contract.test.ts`, `tests/unit/walker-category-behavior.test.ts`, `tests/unit/walker-roadblock-permissions.test.ts`.
- Regenerated golden fixtures: `tests/golden/fixtures/food-chain-golden.json`, `tests/golden/fixtures/paused-commands-golden.json`.
