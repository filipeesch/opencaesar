---
phase: 02-data-catalogs-military-absence-gate
plan: 02-02
type: auto
wave: 2
depends_on: [02-01]
files_modified:
  - tests/balance-parity.test.ts
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "CONFIG (src/sim/config.ts) is an exact value-identical re-export of data/balance.ts (key-set and value equality hold)."
    - "Every key in BALANCE is consumed in src/ via CONFIG.<key>, mapping each externalized balance constant to its consuming code."
    - "No balance key is re-declared or hard-coded as a numeric literal in src/ outside data/ and the src/sim/config.ts re-export."
    - "Golden determinism tests stay green (behavioral equivalence of externalized constants remains observable)."
---

<objective>
Deliver the DATA-02 catalog→behavior parity test (decision 2). The audit is already done in
research: all 29 BALANCE keys are consumed only via `CONFIG.<key>` in `src/` (e.g.
src/sim/housing.ts:42, src/sim/economy.ts:56, src/sim/walkers.ts:111, src/game/scenes/MainScene.ts:42
for ticksPerSecond) and the identity test (tests/data-catalog.test.ts:63-73) passes. What is
missing is a deterministic parity test that maps each externalized constant to its consuming
code and a regression guard preventing a balance key from being re-created as a raw literal in
`src/`. New file `tests/balance-parity.test.ts`. Depends on plan 02-01 (validation baseline).

Output: `.planning/phases/02-data-catalogs-military-absence-gate/02-02-PLAN.md` plus the tasks below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-catalogs-military-absence-gate/02-CONTEXT.md
@.planning/phases/02-data-catalogs-military-absence-gate/02-RESEARCH.md
@.planning/phases/02-data-catalogs-military-absence-gate/02-VALIDATION.md

Existing implementation: `BALANCE` in data/balance.ts:7-80 (29 numeric keys); `CONFIG` re-export
in src/sim/config.ts:10-12 (`{ ...BALANCE }`); existing identity test at tests/data-catalog.test.ts:63-73.
Golden suite at tests/golden/golden.test.ts. Baseline: `npm run typecheck` clean, `npm run test`
green (273 tests). Per-task sampling follows 02-VALIDATION.md.
</context>

<tasks>

<task type="auto">
  <name>Catalog-to-consumer parity test: every BALANCE key is consumed via CONFIG in src/</name>
  <files>tests/balance-parity.test.ts, data/balance.ts, src/sim/config.ts</files>
  <action>Read data/balance.ts, src/sim/config.ts, and tests/data-catalog.test.ts (the existing
  identity block) before editing. Create tests/balance-parity.test.ts with a 'balance catalog -
  behavior parity (DATA-02)' describe block. Import BALANCE from ../data/balance and CONFIG from
  ../src/sim/config. Block 1 - key identity: assert Object.keys(CONFIG).sort() deep-equals
  Object.keys(BALANCE).sort(), and assert ({ ...CONFIG }) equals ({ ...BALANCE }) (value
  identity, mirroring the existing test). Block 2 - constant-to-consumer mapping: use Node fs
  (readdirSync/readFileSync, as tests/military-absence.test.ts does) to collect every source file
  under src/ (recursive, .ts only), and for EACH key of BALANCE assert that at least one src/ file
  other than src/sim/config.ts contains the literal `CONFIG.` + key (proving each externalized
  constant is actually consumed by code outside its re-export). This mapping intentionally scans
  src/ (sim + game) so ticksPerSecond (consumed at src/game/scenes/MainScene.ts:42) resolves.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/balance-parity.test.ts</automated>
  </verify>
  <done>tests/balance-parity.test.ts passes: key sets and values match, and every BALANCE key is
  referenced as CONFIG.&lt;key&gt; in a src/ file other than the re-export; typecheck clean.</done>
</task>

<task type="auto">
  <name>No-redeclaration regression + golden equivalence confirmation</name>
  <files>tests/balance-parity.test.ts, src/sim/config.ts, tests/golden/golden.test.ts</files>
  <action>Read tests/balance-parity.test.ts (the file created in the previous task) before
  editing. Extend the same describe block. Block 3 - no-redeclaration regression: for EACH key
  of BALANCE, assert that no source file under src/ other than src/sim/config.ts matches the
  regex word-boundary + key + optional whitespace + '=' (i.e. `new RegExp('\\b' + key +
  '\\s*=')`) — proving the externalized constant is never re-declared or re-assigned as a raw
  literal in code (CONFIG.&lt;key&gt; usages do not match because '=' never follows immediately
  after the key name). Assert src/sim/config.ts contains the spread `CONFIG = { ...BALANCE }`.
  Then confirm behavioral equivalence stays observable: run the existing golden suite
  (tests/golden/golden.test.ts) unchanged and assert it passes — a balance value drift would
  break a golden, so green goldens are the end-to-end equivalence proof for the re-export.
  Do not run `npm run test:golden:update`; no fixture regeneration is expected.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/balance-parity.test.ts && npx vitest run tests/golden</automated>
  </verify>
  <done>No-redeclaration regression passes (no src/ file outside config.ts declares a BALANCE
  key); src/sim/config.ts contains the { ...BALANCE } spread; golden suite stays green; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/balance.ts → src/sim/config.ts | CONFIG must remain a pure re-export; any drift must surface as a test failure |
| externalized value → consuming sim code | A constant that stops being consumed (dead config) or is re-created in code (false externalization) must fail the parity scan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | balance re-externalization drift | medium | mitigate | per-key CONFIG-mapping test + value identity + no-redeclaration regex in tests/balance-parity.test.ts |
</threat_model>

<verification>
Run `npm run test` (full suite, golden + determinism included) and `npm run typecheck`.
Spot-check: `npx vitest run tests/balance-parity.test.ts` and `npx vitest run tests/golden` are green.
Per-task sampling follows `.planning/phases/02-data-catalogs-military-absence-gate/02-VALIDATION.md`.
</verification>

<success_criteria>
- Key identity and value identity between CONFIG and BALANCE hold.
- Every BALANCE key maps to a CONFIG.&lt;key&gt; consumer under src/ outside the re-export.
- No src/ file outside src/sim/config.ts re-declares/re-assigns a balance key.
- Golden suite stays green; typecheck clean; full suite (273 tests) green.
</success_criteria>

<output>
Create `.planning/phases/02-data-catalogs-military-absence-gate/02-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- New test file: `tests/balance-parity.test.ts` (key identity, value identity, per-key consumer mapping, no-redeclaration regression).
