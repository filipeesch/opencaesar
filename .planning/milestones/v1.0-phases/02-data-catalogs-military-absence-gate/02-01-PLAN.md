---
phase: 02-data-catalogs-military-absence-gate
plan: 02-01
type: tracer
wave: 1
depends_on: []
files_modified:
  - data/validate.ts
  - src/sim/runner.ts
  - tests/catalog-load-guard.test.ts
autonomous: true
requirements: [DATA-01]
must_haves:
  truths:
    - "Every building/commodity/housing/walker/trade/event/mission/localization/balance definition lives in a validated external data catalog under data/ (DATA-01)."
    - "validateCatalogs() covers every catalog including BALANCE and returns an empty issue list on the current data."
    - "SimRunner refuses to construct when validateCatalogs() reports issues (load-time hard-fail guard, DATA-01 'refuses to run on corrupt data')."
---

<objective>
Close the two genuine DATA-01 gaps found in research: (1) `validateCatalogs()` does not
validate the BALANCE catalog (`data/validate.ts:6-13` imports omit balance.ts), and (2)
nothing calls the validator at sim load time — `SimRunner` constructs (`src/sim/runner.ts:113-131`)
without ever validating, so the sim would happily run on corrupt data. This tracer plan
establishes the Phase-2 baseline (typecheck + 273 tests) and delivers the full DATA-01
load-time validation: balance-aware `validateCatalogs()` plus a one-time hard-fail guard in
the SimRunner constructor.

Output: `.planning/phases/02-data-catalogs-military-absence-gate/02-01-PLAN.md` plus the tasks below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-catalogs-military-absence-gate/02-CONTEXT.md
@.planning/phases/02-data-catalogs-military-absence-gate/02-RESEARCH.md
@.planning/phases/02-data-catalogs-military-absence-gate/02-VALIDATION.md

Existing implementation: `validateCatalogs()` in data/validate.ts:20-82 (validates 8
catalogs; CatalogIssue = { catalog, message }; returns [] when all pass). `BALANCE` in
data/balance.ts:7-80 (29 numeric keys). `CONFIG = { ...BALANCE }` in src/sim/config.ts:10-12.
`SimRunner` constructor at src/sim/runner.ts:113-131. Baseline: `npm run typecheck` clean,
`npm run test` green (273 tests). Per-task sampling follows 02-VALIDATION.md.
</context>

<tasks>

<task type="tracer">
  <name>Baseline + extend validateCatalogs to cover the BALANCE catalog</name>
  <files>data/validate.ts, data/balance.ts, tests/catalog-load-guard.test.ts, tests/data-catalog.test.ts</files>
  <action>Read data/validate.ts, data/balance.ts, and tests/data-catalog.test.ts before editing.
  Establish the baseline: run `npm run typecheck` and `npm run test`, record 273 tests passing
  (already verified in RESEARCH, re-confirm the count). Add to data/validate.ts a new exported
  helper `validateBalance(balance: Record&lt;string, unknown&gt;): CatalogIssue[]` that walks
  Object.entries(balance) and, for each key, pushes `{ catalog: 'balance', message: KEY + ':' +
  <reason> }` when the value is not a finite non-negative number (treat undefined, NaN,
  Infinity, and negative numbers as invalid). Import BALANCE from './balance' and call
  validateBalance(BALANCE) inside validateCatalogs() so its issues are included in the returned
  array; keep the validateCatalogs() signature unchanged (existing callers in
  tests/data-catalog.test.ts:49 depend on it). Create tests/catalog-load-guard.test.ts with a
  'balance catalog validation (DATA-01)' describe block asserting: validateBalance({}) returns [];
  validateBalance({ goodKey: 5 }) returns []; validateBalance({ startingTreasury: -1,
  walkerSpeedPerTick: NaN, farmProductionPerTick: Infinity }) returns exactly 3 issues, each with
  catalog 'balance', and the message for the negative case contains 'startingTreasury'; and
  validateCatalogs() still returns [] on the real catalogs (guards against regressions from the
  new balance loop).</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/data-catalog.test.ts && npx vitest run tests/catalog-load-guard.test.ts</automated>
  </verify>
  <done>Baseline recorded; validateBalance() flags undefined/NaN/Infinity/negative values as
  catalog 'balance' issues; validateCatalogs() now includes balance issues but returns [] on the
  real (valid) catalogs; new tests pass; typecheck clean.</done>
</task>

<task type="auto">
  <name>Load-time hard-fail guard: SimRunner throws on invalid catalogs</name>
  <files>data/validate.ts, src/sim/runner.ts, tests/catalog-load-guard.test.ts</files>
  <action>Read data/validate.ts, src/sim/runner.ts (constructor at lines 113-131, imports at
  lines 12-55), and tests/data-catalog.test.ts before editing. Add to data/validate.ts an
  exported helper `throwCatalogIssues(issues: CatalogIssue[]): void` that, when issues.length is
  greater than 0, throws `new Error('Data catalog validation failed: ' + issues.map(i => '[' +
  i.catalog + '] ' + i.message).join('; '))` and otherwise returns without throwing. In
  src/sim/runner.ts add a module-level `let catalogsValidated = false;` and import
  validateCatalogs + throwCatalogIssues from '../../data/validate'. At the very top of the
  SimRunner constructor (src/sim/runner.ts:113, before this.seed assignment) add the guard:
  when catalogsValidated is false, call throwCatalogIssues(validateCatalogs()) then set
  catalogsValidated = true (one-time validation per process so the 273-test suite pays no
  per-construction cost). Because all real catalogs are valid, every existing `new SimRunner(...)`
  call site keeps working. Extend tests/catalog-load-guard.test.ts with a 'load-time guard
  (DATA-01)' describe block: throwCatalogIssues([]) does not throw; throwCatalogIssues([{catalog:
  'balance', message: 'startingTreasury: invalid'}]) throws an Error whose message contains 'Data
  catalog validation failed' and '[balance]'; and constructing `new SimRunner(1)` and
  `new SimRunner(2, undefined, 10)` succeeds while catalogs are valid (import SimRunner from
  ../src/sim/runner in the test).</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/catalog-load-guard.test.ts && npx vitest run tests/determinism/determinism.test.ts && npx vitest run tests/runner-accessors.test.ts</automated>
  </verify>
  <done>throwCatalogIssues exists in data/validate.ts; SimRunner constructor runs the one-time
  guard and throws on non-empty issues; existing constructions still succeed; determinism +
  accessor suites stay green; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/*.ts → SimRunner | Corrupt catalog values must never reach sim state; a throw is the only acceptable mode of failure |
| data/*.ts → CONFIG consumers | Balance values flow to all sim systems via src/sim/config.ts; validation must cover the whole surface incl. balance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | corrupt catalog data | high | mitigate | balance now covered by validateCatalogs via validateBalance() |
| T-02-02 | Tampering | sim runs on unvalidated data | high | mitigate | one-time guard in SimRunner constructor throws via throwCatalogIssues() before any state is set |
</threat_model>

<verification>
Run `npm run test` (full suite, golden + determinism included) and `npm run typecheck`.
Spot-check: `npx vitest run tests/catalog-load-guard.test.ts` and `npx vitest run tests/data-catalog.test.ts` are green.
Per-task sampling follows `.planning/phases/02-data-catalogs-military-absence-gate/02-VALIDATION.md`.
</verification>

<success_criteria>
- validateCatalogs() validates every catalog including BALANCE and returns [] on the current data.
- validateBalance() flags undefined/NaN/Infinity/negative values with catalog 'balance'.
- SimRunner throws 'Data catalog validation failed: ...' on invalid catalogs; constructs cleanly on valid ones.
- Full suite (273 tests) and typecheck remain green.
</success_criteria>

<output>
Create `.planning/phases/02-data-catalogs-military-absence-gate/02-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `data/validate.ts`: new `validateBalance(balance)` helper; `validateCatalogs()` now includes balance issues.
- `data/validate.ts`: new `throwCatalogIssues(issues)` helper (throws on non-empty issues).
- `src/sim/runner.ts`: module-level `catalogsValidated` memo + one-time guard call in the constructor.
- New test file: `tests/catalog-load-guard.test.ts`.
