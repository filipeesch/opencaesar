---
phase: 07-warehouses-logistics
plan: 07-01
type: tracer
wave: 1
depends_on: []
files_modified:
  - src/sim/logistics.ts
  - tests/unit/warehouse-orders.test.ts
  - tests/unit/warehouse-reservation.test.ts
autonomous: true
requirements: [WARE-01]
must_haves:
  truths:
    - "Warehouse per-commodity orders resolve by all six modes: accept accepts deliveries, refuse and empty refuse new deliveries, request seeks receipt, maintain seeks a target stock, reserve holds stock and blocks export/distribution — and the default (no order set) behaves as accept (WARE-01)."
    - "Warehouse slot gating holds: a warehouse accepts a commodity load only while used slots are below its slot capacity, and at capacity it refuses every mode (WARE-01, one load per slot)."
    - "A request/maintain warehouse that is below its target is a priority destination (need score > 0) and a reserve order blocks non-priority claim/export for that commodity (§17.3)."
    - "ReservationPool expiry is deterministic: reservations released by an expiry deadline are tick-based (now + window), never wall-clock, and expired units return to availability (decision 5)."
    - "All logistics model math is deterministic: no Math.random or clock anywhere in src/sim/logistics.ts."
  artifacts:
    - path: src/sim/logistics.ts
      provides: "Additive warehouse order-semantics surface: optional WarehousePolicy.maintainTargets/reserveAmounts, warehouseOrder, warehouseReserves, warehouseNeedsStock, warehousePriority, and ReservationPool.reserveWithExpiry/expireReservations"
      min_lines: 30
    - path: tests/unit/warehouse-orders.test.ts
      provides: "Per-mode order matrix for warehouseAccepts (accept/refuse/request/maintain/empty/reserve + default + slot-capacity boundaries) and the order-semantics tests (maintain target, reserve gate, need score)"
      min_lines: 30
    - path: tests/unit/warehouse-reservation.test.ts
      provides: "ReservationPool reservation-pool expiry determinism: tick-based expiry, available restoration, identical outcomes for identical (now, window)"
      min_lines: 20
  key_links:
    - from: src/sim/logistics.ts
      to: src/sim/logistics.ts
      via: "warehouseOrder/warehouseReserves/warehouseNeedsStock build on the existing warehouseAccepts (logistics.ts:25-29) and WarehouseReorder union (logistics.ts:13)"
    - from: src/sim/logistics.ts
      to: src/sim/logistics.ts
      via: "ReservationPool.expireReservations mirrors the tick-based GranaryModel expiry (logistics.ts:377-388)"
    - from: src/sim/logistics.ts
      to: src/sim/runner.ts
      via: "warehousePriority/warehouseNeedsStock feed the runner's warehouse destination need field (runner.ts:1036 need:0 today) once the runner honors per-warehouse policies (Phase 8)"
---

<objective>
Tracer plan that establishes the Phase-7 baseline (typecheck + 467 tests) and
closes the model-level gaps RESEARCH found in `src/sim/logistics.ts` for
WARE-01 and decision 5: (1) only accept/capacity/refuse/empty/request are
asserted today (logistics.test.ts:16-32) — add the full **per-mode matrix test**
covering all six modes (accept/refuse/request/maintain/empty/reserve) plus slot
gating for `warehouseAccepts` (decision 1), creating
`tests/unit/warehouse-orders.test.ts`; (2) `maintain` and `reserve` have no
behavioral surface (`WarehousePolicy`, logistics.ts:15-18, holds no target or
amount) — add additive `warehouseOrder`, `warehouseReserves`,
`warehouseNeedsStock`, `warehousePriority` helpers plus optional
`maintainTargets`/`reserveAmounts` policy fields so the §17.3 request/maintain/
reserve semantics are representable and proven (decision 1); (3) `ReservationPool`
(logistics.ts:56-81) has no expiry — add a deterministic tick-based
`reserveWithExpiry`/`expireReservations` and lock it with tests
(`tests/unit/warehouse-reservation.test.ts`) so the decision-5 pool-expiry
requirement is deterministic and RNG/clock-free.

Output: `.planning/phases/07-warehouses-logistics/07-01-PLAN.md` plus the tasks
below.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/references/gates.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-warehouses-logistics/07-CONTEXT.md
@.planning/phases/07-warehouses-logistics/07-RESEARCH.md
@.planning/phases/07-warehouses-logistics/07-VALIDATION.md

Existing implementation: `src/sim/logistics.ts` — `WarehouseReorder`
(logistics.ts:13), `WarehousePolicy` (15-18), `defaultWarehousePolicy` (20-22),
`warehouseAccepts` (25-29, gates slots + refuse/empty), `ReservationPool`
(56-81, reserve/available/release/reserved, no expiry), `GranaryModel`
expireReservations (377-388, tick-based). Tests: `tests/unit/logistics.test.ts`
"warehouse orders & slots" (16-32: accept, capacity, refuse, empty, request;
maintain/reserve NOT asserted). Baseline: 467 tests / 63 files green, typecheck
clean (re-confirm the count on task 1). Per-task sampling follows
07-VALIDATION.md.
</context>

<tasks>

<task type="tracer">
  <name>Baseline + per-mode warehouse order matrix test (WARE-01, decision 1)</name>
  <files>tests/unit/warehouse-orders.test.ts</files>
  <read_first>src/sim/logistics.ts (WarehouseReorder 13, WarehousePolicy 15-18, defaultWarehousePolicy 20-22, warehouseAccepts 25-29), tests/unit/logistics.test.ts (warehouse orders & slots 16-32, commercial center 34-44), 07-CONTEXT.md decision 1, 07-RESEARCH.md section 1 WARE-01</read_first>
  <action>Read src/sim/logistics.ts and the existing warehouse tests in tests/unit/logistics.test.ts before editing. Establish the baseline first: run `npm run typecheck` and `npm run test`, record 467 tests passing (re-confirm and note any delta).
  Do NOT change src/sim/logistics.ts in this task — the audit of warehouseAccepts (logistics.ts:25-29) concludes its contract is already correct (slot gate at usedSlots >= slotCapacity, refuse/empty reject, everything else accepts); task 2 of this plan adds the semantic surface. Create tests/unit/warehouse-orders.test.ts importing defaultWarehousePolicy, warehouseAccepts, and the WarehouseReorder type from '../../src/sim/logistics'.
  Block 'per-mode order matrix (WARE-01 §17.3)' asserts, for a policy with slotCapacity 16: (1) the default absent order acts as accept — warehouseAccepts(policy, 'pottery', 0) === true; (2) each of the six modes maps to the documented boolean at usedSlots 0 — accept true, refuse false, request true, maintain true, empty false, reserve true — by iterating the array ['accept','refuse','request','maintain','empty','reserve'], setting policy.perCommodity.pottery = mode and asserting the expected boolean; (3) every non-refusing mode still fails strictly at the slot cap — for accept/request/maintain/reserve, warehouseAccepts(policy, 'pottery', 16) === false and at 15 === true; (4) refuse and empty fail even with 0 used slots; (5) defaultWarehousePolicy() exposes slotCapacity 16 and an empty perCommodity map; (6) two different commodities are independent — refusing 'clay' does not affect 'pottery' acceptance.
  Keep the existing tests in tests/unit/logistics.test.ts green unchanged (including its own warehouse orders block at 16-32).</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/warehouse-orders.test.ts && npm run test</automated>
  </verify>
  <done>Independent of the tracer to establish the 467-test baseline, warehouse-orders.test.ts passes covering the six-mode per-commodity matrix (accept/refuse/request/maintain/empty/reserve), the absent-order-defaults-to-accept, independent per-commodity gating, and slot-capacity boundaries (15 accepts, 16 refuses for every non-refusing mode); all previously-existing 467 tests remain green plus the new tests (total rises accordingly; record the measured delta); `npm run typecheck` clean.</done>
</task>

<task type="auto">
  <name>Additive warehouse order semantics: maintain target, reserve gate, need score (WARE-01, decision 1)</name>
  <files>src/sim/logistics.ts, tests/unit/warehouse-orders.test.ts</files>
  <read_first>src/sim/logistics.ts (WarehousePolicy 15-18, defaultWarehousePolicy 20-22, warehouseAccepts 25-29), tests/unit/warehouse-orders.test.ts (the matrix written by task 1), 07-CONTEXT.md decision 1, 07-RESEARCH.md Q1</read_first>
  <action>Read src/sim/logistics.ts and the matrix test from task 1 before editing. Add the §17.3 semantic surface to src/sim/logistics.ts, additive and deterministic, without removing or renaming any existing export (the module is imported by src/sim/runner.ts:30 and tests): (1) extend WarehousePolicy with two optional fields, `maintainTargets?: Partial<Record<string, number>>` and `reserveAmounts?: Partial<Record<string, number>>` — leave `defaultWarehousePolicy` unchanged (both fields stay undefined for the default policy); (2) export `warehouseOrder(policy: WarehousePolicy, commodity: string): WarehouseReorder` returning `policy.perCommodity[commodity] ?? 'accept'`; (3) export `warehouseReserves(policy: WarehousePolicy, commodity: string): boolean` returning true exactly when warehouseOrder is 'reserve'; (4) export `warehouseNeedsStock(policy: WarehousePolicy, commodity: string, stock: number): boolean` returning true when the order is 'request' OR ('maintain' and `stock < (policy.maintainTargets?.[commodity] ?? 0)`); (5) export `warehousePriority(policy: WarehousePolicy, commodity: string, stock: number): number` returning 1 when warehouseNeedsStock is true else 0 — the need score a Phase-8 runner wiring feeds the warehouse destination `need` field (runner.ts:1036). All five are pure functions of their arguments — no Math.random, no Date.
  Extend tests/unit/warehouse-orders.test.ts with a 'order semantics: request/maintain/reserve (WARE-01 §17.3)' block importing the new helpers: (1) warehouseOrder returns 'accept' for the default and the configured mode otherwise; (2) warehouseReserves is true only for 'reserve' and false for every other mode incl. accept/request/maintain; (3) warehouseNeedsStock: true for 'request' with any stock, true for 'maintain' below the target (policy.maintainTargets.pottery = 8, stock 7 → true; stock 8 → false), false for 'accept' and 'empty'; (4) warehousePriority is 1 exactly when warehouseNeedsStock is true, else 0; (5) a maintain-below-target policy builds a need score > 0 while a full warehouse (at slotCapacity) stays non-priority; (6) the existing task-1 matrix assertions still hold unchanged.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/warehouse-orders.test.ts && npx vitest run tests/unit/logistics.test.ts</automated>
  </verify>
  <done>warehouse-orders.test.ts (incl. the task-1 matrix) and logistics.test.ts both pass with the new additive WarehousePolicy fields and warehouseOrder/warehouseReserves/warehouseNeedsStock/warehousePriority helpers: maintain-below-target and request report need, reserve blocks non-priority claim, default accepts; existing exports and tests unchanged; typecheck clean.</done>
</task>

<task type="auto">
  <name>Deterministic ReservationPool expiry (WARE-01, decision 5)</name>
  <files>src/sim/logistics.ts, tests/unit/warehouse-reservation.test.ts</files>
  <read_first>src/sim/logistics.ts (ReservationPool 56-81, GranaryModel.expireReservations 377-388 for the tick-based pattern), tests/unit/logistics.test.ts (market reservation block 46-55), 07-CONTEXT.md decision 5, 07-RESEARCH.md Q7</read_first>
  <action>Read src/sim/logistics.ts before editing. Add a deterministic expiry path to ReservationPool, additive only — do not change the existing reserve/available/release/reserved signatures or semantics (tests/unit/logistics.test.ts:46-55 and tests/integration/supply-chains.test.ts:25-30 depend on them): (1) add a private per-commodity expiry ledger, e.g. `private expiry = new Map<string, number>()` mapping commodity to the tick at which its reserved units may return to availability; (2) export `reserveWithExpiry(commodity: string, amount: number, now: number, expiresIn = 30): boolean` — it delegates to the existing `reserve(commodity, amount)` and, when that returns true, records `expiry.set(commodity, now + expiresIn)`, returning the reserve result; (3) export `expireReservations(now: number): number` — for each commodity whose recorded expiry is <= now, release exactly the currently reserved amount back to availability via the existing `release` and delete the ledger entry, returning how many commodities were expired. The window is wall-clock-free: only the injected tick `now` and the constant `expiresIn` matter, mirroring GranaryModel.expireReservations (logistics.ts:377-388).
  Create tests/unit/warehouse-reservation.test.ts with a 'reservation pool expiry is deterministic (decision 5)' block: (1) reserveWithExpiry reserves and backs the amount out of available immediately (available drops like plain reserve); (2) an unexpired reservation still holds — expireReservations(now) before the recorded expiry expires nothing and available stays reduced; (3) at or after the expiry tick the units return to availability — exactly the reserved count is released and the commodity can be re-reserved; (4) determinism — building two pools with identical seeds of state and calling reserveWithExpiry('wheat', 1, 10, 30) then expireReservations(40) on both yields identical availability/reserved values, and repeated identical (now, expiresIn) calls expire the same set; (5) plain `reserve()` (no expiry) is unaffected — it never writes the expiry ledger and expireReservations never releases what plain reserve holds; (6) no Math.random or Date appears in the implementation (covered by the behavior assertions; the security register tracks it).</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/warehouse-reservation.test.ts && npm run test</automated>
  </verify>
  <done>warehouse-reservation.test.ts passes: reserveWithExpiry backs availability out immediately, expireReservations releases exactly the reserved units at the tick-based deadline (and only then), identical (now, expiresIn) inputs produce identical expiry outcomes across pools, plain reserve() is untouched; all previously-existing 467 tests remain green plus the new tests (record the measured delta); typecheck clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-commodity order → acceptance | A warehouse must resolve each commodity by its configured order; refuse/empty must gate, request/maintain/reserve must not be mistaken for refuse, and the unset default must accept |
| Maintain/reserve semantics | A request/maintain-below-target warehouse must be a priority (need) destination and a reserve order must gate non-priority claim — representable, not ambiguous with accept |
| Reservation expiry → availability | Expired reservations must return reserved units to availability deterministically; no wall clock may influence the expiry deadline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | per-commodity order bypass | high | mitigate | full six-mode matrix locks warehouseAccepts gating per mode (accept/refuse/request/maintain/empty/reserve + default + slot boundaries) in warehouse-orders.test.ts |
| T-07-02 | Repudiation | order-mode ambiguity (maintain/reserve) | medium | mitigate | warehouseOrder/warehouseReserves/warehouseNeedsStock/warehousePriority semantics asserted; maintain-below-target reports need, reserve gates claim |
| T-07-05 | Privacy | none (pure model determinism) | low | accept | ReservePool expiry is tick-based and asserted deterministic; src/sim/logistics.ts stays RNG/clock-free |
</threat_model>

<verification>
Run `npm run test` (full suite, golden + determinism + property included) and
`npm run typecheck`. Spot-check: `npx vitest run tests/unit/warehouse-orders.test.ts`
and `npx vitest run tests/unit/warehouse-reservation.test.ts` are green;
`tests/unit/logistics.test.ts` unchanged and green.
Per-task sampling follows `.planning/phases/07-warehouses-logistics/07-VALIDATION.md`.
</verification>

<success_criteria>
- Full per-mode order matrix proven for warehouseAccepts: accept/refuse/request/maintain/empty/reserve + default + slot-capacity boundaries (WARE-01, decision 1).
- §17.3 semantic surface added and proven: warehouseOrder, warehouseReserves, warehouseNeedsStock, warehousePriority with optional maintainTargets/reserveAmounts on WarehousePolicy (decision 1).
- ReservationPool has a deterministic tick-based expiry that returns units to availability; proven (decision 5).
- Full suite (467 tests) and typecheck remain green; no existing export renamed or resemantized.
</success_criteria>

<output>
Create `.planning/phases/07-warehouses-logistics/07-01-SUMMARY.md` when done.
</output>
